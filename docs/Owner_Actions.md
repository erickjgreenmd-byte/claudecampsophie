# Owner actions (spec E1, Owner_Actions.md at the package root)

Only concrete external actions that the builder cannot perform, each with the preparation already done.
Nothing here asks to reconfirm approved scope (prices, P17 promotions, one school per family and
full-price-only donations are settled).

| # | Action needed from the owner | Why the builder cannot do it | Prepared by the builder | Unblocks |
|---|---|---|---|---|
| 1 | **Decide iOS prices for 2–4 children.** Apple's USD price points cannot represent $49.98, $59.97, $69.96 (nearest: $49.49/$49.99/$50.00, $59.99/$60.00, $69.99/$70.00). Options: (a) accept nearest points on iOS only, (b) sell 2–4 child plans on iOS at nearest points and keep exact totals on Google/web, (c) change the approved totals everywhere. | Spec forbids silent rounding; it's a pricing decision (Owner_Actions "Exact paid prices") | `docs/Provider_Capability_Matrix.md` §1; code blocks any tier whose store price ≠ approved price. **Knock-on effect:** the approved donation rule requires a charge exactly equal to the approved tier price, so with (a) or (b) no 2–4-child iOS family would ever earn the $1 school donation unless the owner also defines the iOS store price as that channel's regular price | AC_CAPACITY_02, AC_CAPACITY_11, AC_PROMO_11, iOS store products |
| 2 | **Decide how iOS/Google monthly promos behave when a percentage isn't representable.** Apple offers use price points, not percentages, and one redemption per customer per offer. | Commercial promise vs store rules | Matrix §2–3: per-channel `unsupported` status, smallest compliant alternatives listed | AC_PROMO_06/07 on native channels |
| 3 | Give the build environment access to the **existing Supabase staging project** (CLI login or MCP with project scope). | No credentials exist in this environment | Migrations + RLS tests run against local PG16; `supabase/` ready for `supabase db push` | AC_CONN_01/02/06, deployed backend |
| 4 | Give access to **RevenueCat** (project, iOS/Android apps) and **App Store Connect / Play Console** product setup. | No access | Product-to-slot mapping schema, webhook handler, reconciliation logic | AC_BILLING_*, AC_CONN_03/04/05 |
| 5 | Give access to **Expo/EAS** (project + `EXPO_TOKEN` for CI) and Apple/Google signing. | No access; native builds impossible here | App config and EAS profiles in `apps/mobile` | AC_DEPLOY_02, M5 |
| 6 | Provide **OpenAI API project access** from an environment whose network policy allows api.openai.com, and **documented ZDR approval** for under-13 data. | Egress to api.openai.com is blocked here; ZDR is an account approval | Server-only adapter, ZDR gate that fails closed, usage metering | AC_ACCESS_03, AC_GRADING_11, measured costs |
| 7 | **Select and contract a verifiable-parental-consent provider.** | Commercial/legal choice | Consent adapter interface + labeled development mock that production rejects | AC_ACCESS_01/02, production child data |
| 8 | Provide a **Cloudflare account** (or approve an alternative runtime) and DNS access for PencilLift.com at deployment time. | No access | Workers API, cron/queue handlers, runbook | AC_CONN_02, AC_DEPLOY_04/06 |
| 9 | Supply an **authorized monthly spending cap** for AI (and campaign budget caps per promo template). | Spec forbids inventing the owner's cap | Spend ceiling + 50/80/100% alerts require an explicit budget and refuse to run without one | AC_FIN_09, AC_FIN_10 |
| 10 | Arrange **independent security/privacy review** and **educator review** of AI teaching. | Must be independent humans (spec V1) | Threat model, test evidence, evaluation harness design | AC_GRADING_12, release |
| 11 | Store accounts: Apple Developer Program / Google Play registration, agreements, tax/banking, D-U-N-S if organization. | Account-holder steps | Store metadata drafts later | AC_DEPLOY_05, M6–M7 |
| 12 | School payout: recipient verification details and a transfer method (kept disabled until provided). | No live banking details may be invented | Payout batches, reconciliation, disabled transfers | AC_PROMO_12 live payouts |

| 13 | **Approve the inactivity retention policy** (spec P4 proposes 12 months idle, then a notice, then deletion; the build uses a 30-day notice period). | A data-retention policy is the owner's decision | Notice + tombstone + purge implemented and tested (`inactivitySweep`, migration 0680); disabled until `INACTIVITY_DELETION_ENABLED=true` | AC_SECURITY_05 retention, privacy policy text |
| 14 | **Set up transactional email with Resend** (guardian invitations, inactivity notices and the safety flag email; job `safety_flag_email`): create a Resend account, add `pencillift`'s sending domain there and put the SPF and DKIM records it shows into the domain's DNS at GoDaddy (the mailboxes stay at GoDaddy; a mailbox cannot send from the Worker), then `wrangler secret put RESEND_API_KEY --env staging` / `--env production` and set `EMAIL_FROM` (for example `PencilLift <hello@<your-domain>>`) in `wrangler.toml` [env.*.vars]. Approve the three email templates' wording with #24 (they are drafts). Send one test of each template from staging and record it in docs/Connections.md. | Vendor account, DNS records and wording approval are the owner's; no credentials are ever pasted here | `EmailProvider` interface, the Resend adapter (fail-closed: reviewed copy only, idempotency key per flag, retryable vs final errors) and the outbox mock; readiness reports `email_provider` blocked until the key and sender exist | AC_ACCESS_09 invitations, the safety flag email, inactivity notices in production |
| 15 | **Legal review of the public pages** (privacy, terms, support, contact, account deletion) and the support mailbox/operator details marked "(to be confirmed)". | Legal sign-off and business details | Complete drafts with a visible draft banner until `VITE_LEGAL_REVIEWED=true` | AC_DEPLOY_04, store review |
| 16 | **Monetization eligibility** (only if P16 revenue is wanted): Amazon Associates determination for the actual child-directed property, sponsor contracts, approved creatives. | Program eligibility and contracts are external | Module ships with every kill switch OFF; approvals require real evidence references | AC_MON_09/10, any sponsor or affiliate revenue |
| 17 | **Public mobile configuration**: RevenueCat public SDK keys (iOS/Android), Supabase URL + publishable key, portal URL — set as `EXPO_PUBLIC_*` in EAS. | Accounts not available here | App reads them from `app.config.ts` extra and shows honest "not available in this build" states without them | Native purchase, promo code step, parent sign-in on devices |
| 18 | **Keep RevenueCat "restore behavior" on Transfer** (not the legacy alias/"keep with original" modes) in the RevenueCat project settings. | Project setting in the owner's RevenueCat account | Sync and webhooks re-verify every family a restore or TRANSFER names and revoke a purchase the provider no longer lists (RV-billing-1); alias mode would list one purchase under two families | AC_BILLING_03, one purchase = one family |
| 19 | **Create next month's AI spend budget row** before 00:00 UTC on the 1st (staging/production admit no AI stage in a month without one; BUG-092) (`spend_budgets`), or approve a carry-forward rule. | The cap is the owner's money decision; the system never invents one | Readiness reports `ai_spend_budget` blocked when the current UTC month has no row; scans stop at the ceiling | AC_SECURITY_06, AC_FIN cost controls |
| 20 | **Confirm on the hosted Supabase project** that migrations may create a trigger on `auth.sessions` and SECURITY DEFINER functions may read it (migration 0720: sign-out ends API access and step-up). | Needs the real project; the local shim mirrors Supabase's columns only | Implemented and tested against the shim; if the hosted role refuses, 0720 fails loudly and a Supabase Auth hook replaces the trigger | Spec P3 "logout invalidates access" in production |
| 21 | **Mark each deployed database once** after migrations, with the migration role: `insert into private.deployment (environment) values ('production') on conflict (singleton) do update set environment = excluded.environment` (`'staging'` on staging). | Needs the owner's Supabase projects | Migration 0770 refuses the production mark while any fixture or fake catalog row is live and then refuses new ones; readiness reports `database_environment` and `catalog_data` | AC_DEPLOY_07 production launch |
| 22 | **Secret-scan the exact release builds** before upload: `node scripts/scan-secrets.mjs --artifacts apps/web/dist` for the web build, and before each EAS build scan `npx expo config --type public --json` made with that build's EXPO_PUBLIC_* values (runbook §3). | Release values exist only in the owner's build environment | CI and `scripts/verify.sh` already build and scan release-shaped artifacts with a negative control (`scripts/scan-release-artifacts.sh`) | AC_SECURITY_04 for shipped binaries |
| 23 | **Check Supabase Storage `stat`** against staging: a present object (200 with a numeric size), a missing object (400 with statusCode '404', read as absent) and a missing bucket (an error, answered PROVIDER_UNAVAILABLE); record in docs/Connections.md. | Needs a Supabase project | Adapter and memory mock implemented and tested with a fake fetch (BUG-078) | Upload finalize size check in production |
| 24 | **Approve the child-safety package with an educator and counsel**: the child safety messages and parent wording (`safety-templates.v4`, screen `safety-screen.v4`), the parent email on every flag (job `safety_flag_email`; template status `draft_pending_owner_and_educator_approval`, surfaced by readiness item `safety_templates`), the two parent actions, and the runbook 5.1 procedure. **Owner decision 2026-09-25: the parent is the sole recipient of safety messages and the one who acts; no flag is held from the family; PencilLift does not contact authorities.** The lead advised against sending abuse, sexual-content and secrecy flags straight to a household that may contain the person named (Threat_Model T34) and was overruled. Counsel must confirm this is lawful in the launch states (mandated-reporting duties for a child-facing service) before launch. | Policy, legal and educational judgement | Templates, email, parent actions and runbook drafted and tested; readiness reports `safety_templates` blocked until approved | AC_SECURITY_02 launch |
| 25 | **Decide family read access while a report is held**: the child's own answer and the child's 'safety' feedback row remain readable by family members through the Supabase Data API (parental review rights, COPPA) even while the report itself is held. | Legal decision (COPPA parental access vs. child protection) | The PencilLift parent API and web never return 'safety' rows; only the Data API path remains | T34 |
| 26 | ~~Confirm the moderation-outage rule~~ Superseded by the 2026-09-25 decision: no flag is held, so an outage changes nothing about who sees a flag. | — | Closed | T33 |
| 27 | **Plan for the parent as the responder**: recall-first screening escalates common homework frustration ('I can't do this anymore') and a child's own body-safety lesson answers; under the 2026-09-25 decision every one of these reaches the parent by email and in their list, and the parent clears false alarms themself. Decide the support path for parents who cannot tell a false alarm from a concern (the owner-admin queue remains a support tool). | Support and communications commitments are the owner's | Parent actions built; admin queue with false-match clearing (BUG-110) | AC_SECURITY_02, P4 |
| 28 | **Decide on BUG-096 (answers the extraction model inserts into the child's transcription)**: labelled answers are withheld, but an unlabelled insertion such as '(84)' or '= 84' in the printed prompt's transcription still reaches the child. Options: accept the residual (record it as deferred_by_owner), or fund a fix (e.g. a second extraction pass or showing the child only the photo). | Product and child-learning risk decision; no sound automatic fix exists because printed prompts legitimately contain their keys | `withholdStatedAnswers` built and tested; Threat_Model T35 | AC_GRADING_06 |
| 29 | **Open the Amazon Appstore channel for Fire tablets**: create an Amazon developer account and the PencilLift Appstore listing; in RevenueCat add an Amazon app and put its public `amzn_…` key in `EXPO_PUBLIC_REVENUECAT_AMAZON_KEY` for the `preview-amazon` / `production-amazon` EAS profiles (never a `goog_` or secret key); create the four subscription items (1–4 children at $39.99 / $49.98 / $59.97 / $69.96) and record their ids as `store_product_mappings` rows with channel `amazon_appstore`; upload the APK by hand (EAS Submit does not target Amazon). Promo codes cannot be redeemed through the Amazon store (no offer codes); push on Fire would need Amazon Device Messaging later. | Accounts, listing and products are the owner's | Channel `amazon_appstore` in contracts, domain, DB (0800), API mapping and the mobile build flag; billing periods from Amazon events recorded like the other stores | AC_BILLING, spec P15 platforms |
| 30 | **Choose the iOS icon variant and the dark splash**: `icon-ios-1024.png` (teal tile, white rocket, the board's own cue; wired) or `icon-ios-light-1024.png` (white tile, full colour); the app is light-only today, so no dark splash is wired (the dark variant exists). | Brand taste | Both icons pass the circle/squircle/rounded-square mask checks (`brand/ASSETS.md`) | Store listing |
| 31 | **Decide who works the support queue**: today only `owner_admin` rows (aal2) can open `/admin/support`, assign, reply and resolve; a staff member with role `support` is accepted as an assignee but cannot sign in to the console. If support staff should work the queue, the lead adds `app.is_support_staff()` and a `requireSupportStaff` gate on `/v1/admin/support/*` (overview, revenue, subscriptions and settings stay owner-only). | Staffing decision | Support console, queue, keyset paging, internal notes and audit rows built and tested (`admin-ops.test.ts`) | Support operations |
| 32 | **Write the refund and complaint policy** the console applies: what a full or partial refund is granted for, who may issue one, the response-time target per case kind, and the wording of the parent notices (`SUPPORT_INTAKE_NOTICE`, `SUPPORT_REFUND_NOTICE`). Store refunds are issued by Apple, Google and Amazon and only reflected; a Stripe refund is issued in the Stripe dashboard and recorded on the case with its reference. Set the per-channel store fee rates in `/admin/revenue` (estimates for net revenue; never the provider's statement). | Policy, finance and legal wording | Case kinds, resolutions (`refunded_by_store`, `stripe_refund_issued` + reference, `no_refund`, …), fee-rate settings and the revenue view built and tested | Support operations, AC_BILLING reporting |

Account sign-up alone does not complete a row; each closes only with recorded evidence in `docs/Connections.md`.
