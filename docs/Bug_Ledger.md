# PencilLift bug ledger (spec E5.4, AC_ECC_05/06)

States: reported, confirmed, fixing, verifying, closed, blocked_external, deferred_by_owner. Close only with evidence.

| ID | Severity / requirement | State | Revision / environment | Expected vs actual / reproduction | Failed hypotheses | Root cause / fix | Regression evidence | Next |
|---|---|---|---|---|---|---|---|---|
| BUG-001 | Low / test infra + migration robustness (AC_DEPLOY_01) | closed | 371519b, local PG16 | Parallel test files: expected isolated DBs; actual `duplicate key value violates unique constraint "pg_authid_rolname_index"` in `schema_invariants.test.ts` | "`if not exists` guard is enough" — rejected: check-then-create races across sessions | Roles are cluster-wide; creation now catches `duplicate_object`/`unique_violation` in shim and `0001` (`pl_child`) | `pnpm test:db` 3 consecutive runs, 32/32 pass | — |
| BUG-002 | High / AC_SECURITY_04, T1 (anon could execute a SECURITY DEFINER helper) | closed | working tree after 0300, local PG16 | Expected: anon cannot execute `app.*` helpers. Actual: `schema_invariants.test.ts` → `['app.is_school_admin']` executable by anon | "`ALTER DEFAULT PRIVILEGES IN SCHEMA app REVOKE EXECUTE FROM PUBLIC` covers new functions" — rejected: per-schema defaults can only add to global defaults | 0001 now revokes EXECUTE from PUBLIC globally for the migration role; 0300 also revokes explicitly on `app.is_school_admin` | Invariant test failed before fix, `pnpm test:db` 73/73 after | — |
| BUG-003 | Low / process (AC_ECC_07: gate must run all checks) | closed | 84ec656 | Expected every commit to pass typecheck; actual: rewards test helper had TS2345 (`key = randomUUID()` inferred a UUID template type) and was committed because the pre-commit command ran lint but not typecheck | — | Helper annotated `key: string`; lead's pre-commit chain is now format → lint → typecheck → tests, and CI runs all four | `pnpm --filter @pencillift/db typecheck` exit 0 at 4393cc3 | — |
| BUG-004 | Low / process (AC_ECC_07) | closed | c1590e3 | Expected commit gate to stop on type errors; actual: `typecheck | grep error` hid the exit status and a TS2375 in `apps/web/src/routes.tsx` was committed (same failure class as BUG-003) | "Be more careful with the command chain" — rejected: a second recurrence shows discipline is not a control | Added `scripts/verify.sh` (set -euo pipefail; format → lint → typecheck → tests → finance) as the single pre-commit gate; routes helper typed explicitly | `scripts/verify.sh` exit 0 at the fix commit | — |
| BUG-005 | Medium / AC_PROMO_11 (school change) | closed | working tree (P17 parent routes) | Expected: choosing the current school while a change is pending cancels the pending change. Actual: HTTP 500; the sync updated the current row's end month (to open-ended) before deleting the pending row, so the intermediate state overlapped and the exclusion constraint fired | — | Apply the planned history in order: delete dropped (not-yet-effective) rows, then move end months, then insert | `apps/api/tests/promotions.test.ts` "re-changing replaces the pending one" failed before, passes after | — |
| BUG-006 | **High** / AC_BILLING_01, AC_CONN_04 (paid family gets no capacity) | closed | working tree (billing webhooks) | Expected: every family buying a plan gets its paid capacity. Actual (found by `webhooks.test.ts`): the RevenueCat mapping keyed subscriptions as `rc:{store}:{product}`, which repeats across families; the second family's upsert hit the global unique key and the family-guarded `ON CONFLICT … WHERE` turned it into a silent no-op, so a paying family would get 0 slots | "Test-order flake" — rejected: reproduced deterministically by running the describe block | Subscription key now includes the family's opaque billing ref; the upsert returns rows and throws when nothing was written, failing the webhook (provider retries) instead of skipping | `[BUG-006]` regression tests (two families same product; cross-family id fails loudly) fail before, pass after | Verify real RevenueCat entries are per-subscriber when credentials exist |
| BUG-007 | High / AC_ACCESS_03 (child data to AI without real ZDR evidence) | closed | working tree (packages/ai) | Expected: a boolean-like value is never accepted as ZDR evidence. Actual: `checkChildDataGate` accepted `{reference: "true"}` because it only required 4 characters | — | Reference must be ≥ 6 characters and not a switch-like placeholder (true/approved/enabled/…); verification date must parse and not be in the future | `packages/ai/src/ai.test.ts` "a boolean or placeholder is not evidence" failed before, passes after | — |
| BUG-008 | **High** / AC_PROMO_12, AC_SECURITY_05 (financial snapshot and audit metadata unreadable) | closed | 1c9851d, local PG16 | Expected: `donation_accruals.eligibility_snapshot` and `audit_events.metadata` hold JSON objects. Actual: `jsonb_typeof = 'string'` — postgres.js serialises a JS string bound to a `::jsonb` parameter as a JSON string scalar, so `${JSON.stringify(x)}::jsonb` double-encoded every write (6 call sites). Found while asserting `metadata.channel` in a new webhook test | "The test DB lacks type fetching" — rejected: probe script shows the same result with default postgres.js options | Call sites bind `${JSON.stringify(x)}::text::jsonb`; migration 0630 adds a check on every jsonb column rejecting scalars; schema invariant requires that check on any future jsonb column | `webhooks.test.ts` unmatched-discount test failed before, passes after; `schema_invariants.test.ts` `[BUG-008]` rejects the double-encoded insert | Re-run after vertical agents land (their writes are now checked by the DB) |
| BUG-009 | Low / AC_ECC_07 (flaky test) | closed | 408bf6d | Expected deterministic webhook tests. Actual: the promo-code fixture built codes as `ABCDEFGHJK` + one random digit; `promo_codes.code_normalized` is globally unique, so two calls collided ~10% of runs | — | Fixture uses `generatePromoCode(cryptoRandom)` (50-bit codes) like the other suites | Found by inspection; suite passes 16/16 | — |
| BUG-010 | Medium / AC_PROMO_07, AC_PROMO_13 (campaign caps under-count) | closed (mitigated) | 408bf6d | Expected: every store-applied promo discount is attributable to a campaign. Actual: a shared App Store code redeemed outside the app (App Store URL) arrives as a discounted renewal with no PencilLift redemption; reconciliation silently ignored it, so caps/budget under-count | — | Reconciliation now writes a `promo.unmatched_discount` audit event (channel, period) for owner review; donation already correctly zero because the period is discounted | `webhooks.test.ts` "store promo discount with no PencilLift redemption is flagged" | Owner console view for unmatched discounts (Progress backlog); per-family Apple codes remove the gap |
| BUG-011 | Medium / spec P3 (PIN reset skipped the PIN entry) | closed | working tree (web auth), jsdom | Expected: after re-entering the password the parent chooses a new PIN. Actual: the second step rendered the same `AccountForm` component in the same tree position, so React kept the first step's "succeeded" state and showed "new PIN saved" without asking for or sending a PIN | — | Each step's form gets its own `key`, so state resets between steps | `auth.test.tsx` "re-authenticates ... then saves the PIN" failed before, passes after | — |
| BUG-012 | **High** / AC_ACCESS_04/08 (child logged out on real devices) | closed | 8555062, found in lead integration review | Expected: one child session per device. Actual: the homework screens ran their own refresh-token rotation (`src/homework/child-session.ts`) beside the family session; both keep separate in-memory access tokens, so two screens refreshing at once present the same refresh token twice, which the API correctly treats as reuse and revokes the child's session | — | `src/homework/child-api.ts` now uses the app's single `childSession` (single-flight refresh); the duplicate refresher was deleted; the app session layer is the only token source | Code inspection + mobile typecheck; single-flight behaviour covered by `src/family/child-session.test.ts` | Device test when builds exist |
| BUG-013 | **High** / spec P4, AC_CAPTURE_02 (location metadata sent to the AI provider; privacy page claim untrue) | closed | 69e0a31 scan pipeline, found by the public-site adversarial review (`RV-public-site-1`) | Expected: "Location and camera details are removed from photos before they are processed." Actual: the scan job base64-encoded the stored bytes unchanged, so a JPEG with Exif/GPS reached the model; content was also never checked against its declared type | — | New `services/image-metadata.ts` walks JPEG segments / PNG chunks and drops Exif, GPS, XMP, IPTC, comments and text chunks; anything that does not parse as the declared type asks for a retake (allowance released) instead of being forwarded | `tests/public-site.review.test.ts` RV-public-site-1 failed before, passes after; `image-metadata.test.ts`; scan test "location metadata never reaches the model" | Stored originals keep metadata until the 30-day purge (the mobile client re-encodes, which normally drops it); `source_pages.metadata_stripped` stays false until storage is rewritten |
| BUG-014 | **High** / AC_PROMO_06, AC_PROMO_08 | closed | review of lead code (`wf_c99461cc-949`, RV-lead-billing-p17-1), 48dcbe4 | A lapsed family's code never confirms: reconciliation compared the lapse-suffixed key `first:<channel>:<lapsedEnd>` with the bare `first:<channel>`, so the redemption stayed pending forever and blocked future codes | — | Lapse-suffixed first-period keys match the first charge-bearing period starting after the lapse, whether the store reports an initial purchase or a renewal | `apps/api/tests/lead-billing-p17.review.test.ts` / `supabase/tests/lead-billing-p17.review.test.ts` failed before, pass after | Store sandbox evidence when accounts exist |
| BUG-015 | **High** / AC_PROMO_05, AC_PROMO_07 | closed | review of lead code (`wf_c99461cc-949`, RV-lead-billing-p17-2), 48dcbe4 | A provider_pending redemption whose target renewal never happened (subscription expired) was never resolved: the family was blocked for good and a cap/budget slot leaked | — | `resolveUnreachableRedemptions` rejects it from provider-derived facts (ended at/before the target, target month elapsed, or no first period within 30 days); runs in webhooks and the scheduled sweep | `apps/api/tests/lead-billing-p17.review.test.ts` / `supabase/tests/lead-billing-p17.review.test.ts` failed before, pass after | Store sandbox evidence when accounts exist |
| BUG-016 | Medium / AC_PROMO_12 | closed | review of lead code (`wf_c99461cc-949`, RV-lead-billing-p17-3), 48dcbe4 | A refund delivered before its charge event was dropped, so $1 accrued for a refunded month | — | Refunds/chargebacks for an unrecorded period are parked in `pending_refunds` (migration 0700) and applied when the period is recorded | `apps/api/tests/lead-billing-p17.review.test.ts` / `supabase/tests/lead-billing-p17.review.test.ts` failed before, pass after | Store sandbox evidence when accounts exist |
| BUG-017 | **High** / AC_BILLING_*, AC_PROMO_11 | closed | review of lead code (`wf_c99461cc-949`, RV-lead-billing-p17-4), 48dcbe4 | RevenueCat SANDBOX transactions (TestFlight/review) were booked as production billing periods and accrued school donations | — | The webhook compares the event environment with the runtime; production requires PRODUCTION; mismatches are ignored | `apps/api/tests/lead-billing-p17.review.test.ts` / `supabase/tests/lead-billing-p17.review.test.ts` failed before, pass after | Store sandbox evidence when accounts exist |
| BUG-018 | Medium / AC_CONN_05 | closed | review of lead code (`wf_c99461cc-949`, RV-lead-billing-p17-5), 48dcbe4 | A Stripe event whose processing threw stayed 'received' and its retry was answered 'duplicate', so a renewal coupon was never attached | — | Failures are marked 'failed' with a retryable 503; 'received' events older than a 5-minute lease are re-opened | `apps/api/tests/lead-billing-p17.review.test.ts` / `supabase/tests/lead-billing-p17.review.test.ts` failed before, pass after | Store sandbox evidence when accounts exist |
| BUG-019 | Medium / AC_PROMO_12 | closed | review of lead code (`wf_c99461cc-949`, RV-lead-billing-p17-6), 48dcbe4 | Stripe chargebacks never reversed the donation: a Dispute references a charge, never an invoice | — | Charges/disputes resolve to their invoice through the Stripe API (charge.invoice or invoice payments), then to our recorded period; won disputes reinstate once | `apps/api/tests/lead-billing-p17.review.test.ts` / `supabase/tests/lead-billing-p17.review.test.ts` failed before, pass after | Store sandbox evidence when accounts exist |
| BUG-020 | Low / AC_PROMO_12 | closed | review of lead code (`wf_c99461cc-949`, RV-lead-billing-p17-7), 48dcbe4 | A Stripe partial refund was booked as a full refund | — | Refunds below the charged amount are recorded as partial with the real amount | `apps/api/tests/lead-billing-p17.review.test.ts` / `supabase/tests/lead-billing-p17.review.test.ts` failed before, pass after | Store sandbox evidence when accounts exist |
| BUG-021 | Medium / AC_PROMO_06 | closed | review of lead code (`wf_c99461cc-949`, RV-lead-billing-p17-8), 48dcbe4 | A Stripe renewal invoice listing proration lines first lost its coupon (period read from lines[0]) | — | The non-proration subscription line with the latest start defines the period and price | `apps/api/tests/lead-billing-p17.review.test.ts` / `supabase/tests/lead-billing-p17.review.test.ts` failed before, pass after | Store sandbox evidence when accounts exist |
| BUG-022 | Medium / AC_PROMO_10 | closed | review of lead code (`wf_c99461cc-949`, RV-lead-billing-p17-9), 48dcbe4 | School report amounts revealed 1-4 family groups (eligible*100 - accrued/paid) | — | Amount-derived group sizes join the complementary suppression in `school_month_report` (0690) | `apps/api/tests/lead-billing-p17.review.test.ts` / `supabase/tests/lead-billing-p17.review.test.ts` failed before, pass after | Store sandbox evidence when accounts exist |
| BUG-023 | Low / AC_ACCESS_10 | closed | review of lead code (`wf_c99461cc-949`, RV-lead-billing-p17-10), 48dcbe4 | A family tombstoned during a webhook's provider fetch still had entitlements, capacity and periods written (also in donation accrual) | — | Ledger transactions lock the family row and abort when it is tombstoned | `apps/api/tests/lead-billing-p17.review.test.ts` / `supabase/tests/lead-billing-p17.review.test.ts` failed before, pass after | Store sandbox evidence when accounts exist |
| BUG-024 | Low / process (AC_ECC_07) | closed | 48dcbe4 | Expected: no commit with lint errors. Actual: `git add`/`git commit` sat on a separate line after a `&&` chain containing ESLint, so the failed lint did not stop the commit (two unnecessary type assertions pushed) and the ledger update in the same chain was skipped | "Chain every step with &&" — rejected as a control: a third occurrence of the L-004 class shows discipline is not enough | Versioned pre-commit hook `scripts/git-hooks/pre-commit` (prettier + eslint on staged files + secret scan) enabled with `core.hooksPath`; lint errors fixed by validating Stripe responses with zod | Hook blocks a staged file with a lint error (checked manually) | — |
