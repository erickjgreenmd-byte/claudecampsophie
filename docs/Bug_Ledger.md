# PencilLift bug ledger (spec E5.4, AC_ECC_05/06)

States: reported, confirmed, fixing, verifying, closed, blocked_external, deferred_by_owner. Close only with evidence.

| ID | Severity / requirement | State | Revision / environment | Expected vs actual / reproduction | Failed hypotheses | Root cause / fix | Regression evidence | Next |
|---|---|---|---|---|---|---|---|---|
| BUG-001 | Low / test infra + migration robustness (AC_DEPLOY_01) | closed | 371519b, local PG16 | Parallel test files: expected isolated DBs; actual `duplicate key value violates unique constraint "pg_authid_rolname_index"` in `schema_invariants.test.ts` | "`if not exists` guard is enough" — rejected: check-then-create races across sessions | Roles are cluster-wide; creation now catches `duplicate_object`/`unique_violation` in shim and `0001` (`pl_child`) | `pnpm test:db` 3 consecutive runs, 32/32 pass | — |
| BUG-002 | High / AC_SECURITY_04, T1 (anon could execute a SECURITY DEFINER helper) | closed | working tree after 0300, local PG16 | Expected: anon cannot execute `app.*` helpers. Actual: `schema_invariants.test.ts` → `['app.is_school_admin']` executable by anon | "`ALTER DEFAULT PRIVILEGES IN SCHEMA app REVOKE EXECUTE FROM PUBLIC` covers new functions" — rejected: per-schema defaults can only add to global defaults | 0001 now revokes EXECUTE from PUBLIC globally for the migration role; 0300 also revokes explicitly on `app.is_school_admin` | Invariant test failed before fix, `pnpm test:db` 73/73 after | — |
| BUG-003 | Low / process (AC_ECC_07: gate must run all checks) | closed | 84ec656 | Expected every commit to pass typecheck; actual: rewards test helper had TS2345 (`key = randomUUID()` inferred a UUID template type) and was committed because the pre-commit command ran lint but not typecheck | — | Helper annotated `key: string`; lead's pre-commit chain is now format → lint → typecheck → tests, and CI runs all four | `pnpm --filter @pencillift/db typecheck` exit 0 at 4393cc3 | — |
| BUG-004 | Low / process (AC_ECC_07) | closed | c1590e3 | Expected commit gate to stop on type errors; actual: `typecheck | grep error` hid the exit status and a TS2375 in `apps/web/src/routes.tsx` was committed (same failure class as BUG-003) | "Be more careful with the command chain" — rejected: a second recurrence shows discipline is not a control | Added `scripts/verify.sh` (set -euo pipefail; format → lint → typecheck → tests → finance) as the single pre-commit gate; routes helper typed explicitly | `scripts/verify.sh` exit 0 at the fix commit | — |
| BUG-005 | Medium / AC_PROMO_11 (school change) | closed | working tree (P17 parent routes) | Expected: choosing the current school while a change is pending cancels the pending change. Actual: HTTP 500; the sync updated the current row's end month (to open-ended) before deleting the pending row, so the intermediate state overlapped and the exclusion constraint fired | — | Apply the planned history in order: delete dropped (not-yet-effective) rows, then move end months, then insert | `apps/api/tests/promotions.test.ts` "re-changing replaces the pending one" failed before, passes after | — |
| BUG-006 | **High** / AC_BILLING_01, AC_CONN_04 (paid family gets no capacity) | closed | working tree (billing webhooks) | Expected: every family buying a plan gets its paid capacity. Actual (found by `webhooks.test.ts`): the RevenueCat mapping keyed subscriptions as `rc:{store}:{product}`, which repeats across families; the second family's upsert hit the global unique key and the family-guarded `ON CONFLICT … WHERE` turned it into a silent no-op, so a paying family would get 0 slots | "Test-order flake" — rejected: reproduced deterministically by running the describe block | Subscription key now includes the family's opaque billing ref; the upsert returns rows and throws when nothing was written, failing the webhook (provider retries) instead of skipping | `[BUG-006]` regression tests (two families same product; cross-family id fails loudly) fail before, pass after | Verify real RevenueCat entries are per-subscriber when credentials exist |
| BUG-007 | High / AC_ACCESS_03 (child data to AI without real ZDR evidence) | closed | working tree (packages/ai) | Expected: a boolean-like value is never accepted as ZDR evidence. Actual: `checkChildDataGate` accepted `{reference: "true"}` because it only required 4 characters | — | Reference must be ≥ 6 characters and not a switch-like placeholder (true/approved/enabled/…); verification date must parse and not be in the future | `packages/ai/src/ai.test.ts` "a boolean or placeholder is not evidence" failed before, passes after | — |
| BUG-008 | **High** / AC_PROMO_12, AC_SECURITY_05 (financial snapshot and audit metadata unreadable) | closed | 1c9851d, local PG16 | Expected: `donation_accruals.eligibility_snapshot` and `audit_events.metadata` hold JSON objects. Actual: `jsonb_typeof = 'string'` — postgres.js serialises a JS string bound to a `::jsonb` parameter as a JSON string scalar, so `${JSON.stringify(x)}::jsonb` double-encoded every write (6 call sites). Found while asserting `metadata.channel` in a new webhook test | "The test DB lacks type fetching" — rejected: probe script shows the same result with default postgres.js options | Call sites bind `${JSON.stringify(x)}::text::jsonb`; migration 0630 adds a check on every jsonb column rejecting scalars; schema invariant requires that check on any future jsonb column | `webhooks.test.ts` unmatched-discount test failed before, passes after; `schema_invariants.test.ts` `[BUG-008]` rejects the double-encoded insert | Re-run after vertical agents land (their writes are now checked by the DB) |
| BUG-009 | Low / AC_ECC_07 (flaky test) | closed | 408bf6d | Expected deterministic webhook tests. Actual: the promo-code fixture built codes as `ABCDEFGHJK` + one random digit; `promo_codes.code_normalized` is globally unique, so two calls collided ~10% of runs | — | Fixture uses `generatePromoCode(cryptoRandom)` (50-bit codes) like the other suites | Found by inspection; suite passes 16/16 | — |
| BUG-010 | Medium / AC_PROMO_07, AC_PROMO_13 (campaign caps under-count) | closed (mitigated) | 408bf6d | Expected: every store-applied promo discount is attributable to a campaign. Actual: a shared App Store code redeemed outside the app (App Store URL) arrives as a discounted renewal with no PencilLift redemption; reconciliation silently ignored it, so caps/budget under-count | — | Reconciliation now writes a `promo.unmatched_discount` audit event (channel, period) for owner review; donation already correctly zero because the period is discounted | `webhooks.test.ts` "store promo discount with no PencilLift redemption is flagged" | Owner console view for unmatched discounts (Progress backlog); per-family Apple codes remove the gap |
| BUG-011 | Medium / spec P3 (PIN reset skipped the PIN entry) | closed | working tree (web auth), jsdom | Expected: after re-entering the password the parent chooses a new PIN. Actual: the second step rendered the same `AccountForm` component in the same tree position, so React kept the first step's "succeeded" state and showed "new PIN saved" without asking for or sending a PIN | — | Each step's form gets its own `key`, so state resets between steps | `auth.test.tsx` "re-authenticates ... then saves the PIN" failed before, passes after | — |
| BUG-012 | **High** / AC_ACCESS_04/08 (child logged out on real devices) | closed | 8555062, found in lead integration review | Expected: one child session per device. Actual: the homework screens ran their own refresh-token rotation (`src/homework/child-session.ts`) beside the family session; both keep separate in-memory access tokens, so two screens refreshing at once present the same refresh token twice, which the API correctly treats as reuse and revokes the child's session | — | `src/homework/child-api.ts` now uses the app's single `childSession` (single-flight refresh); the duplicate refresher was deleted; the app session layer is the only token source | Found and fixed by inspection: no failing reproduction was captured before the fix (the two-refresher race needs two live screens on a device). The single-flight refresh is covered by `src/family/child-session.test.ts`; the duplicate refresher is deleted. Later (final pass): `child-session.test.ts` '[BUG-012 reproduction] two independent refreshers on one device revoke the child session' reproduces the failure mode against a rotating, reuse-revoking fake server (the pre-fix shape: two sessions over one stored token), and `src/lib/token-sources.test.ts` fails if a second module calls the refresh endpoint or constructs a child session | Device reproduction when builds exist |
| BUG-013 | **High** / spec P4, AC_CAPTURE_02 (location metadata sent to the AI provider; privacy page claim untrue) | closed | 69e0a31 scan pipeline, found by the public-site adversarial review (`RV-public-site-1`) | Expected: "Location and camera details are removed from photos before they are processed." Actual: the scan job base64-encoded the stored bytes unchanged, so a JPEG with Exif/GPS reached the model; content was also never checked against its declared type | — | New `services/image-metadata.ts` walks JPEG segments / PNG chunks and drops Exif, GPS, XMP, IPTC, comments and text chunks; anything that does not parse as the declared type asks for a retake (allowance released) instead of being forwarded | `tests/public-site.review.test.ts` RV-public-site-1 failed before, passes after; `image-metadata.test.ts`; scan test "location metadata never reaches the model" | Stored originals keep metadata until the 30-day purge (the mobile client re-encodes, which normally drops it); `source_pages.metadata_stripped` stays false until storage is rewritten |
| BUG-014 | **High** / AC_PROMO_06, AC_PROMO_08 | closed | review of lead code (`wf_c99461cc-949`, RV-lead-billing-p17-1), 48dcbe4 | A lapsed family's code never confirms: reconciliation compared the lapse-suffixed key `first:<channel>:<lapsedEnd>` with the bare `first:<channel>`, so the redemption stayed pending forever and blocked future codes | — | Lapse-suffixed first-period keys match the first charge-bearing period starting after the lapse, whether the store reports an initial purchase or a renewal | `apps/api/tests/lead-billing-p17.review.test.ts` / `supabase/tests/lead-billing-p17.review.test.ts` failed before, pass after | Store sandbox evidence when accounts exist |
| BUG-015 | **High** / AC_PROMO_05, AC_PROMO_07 | closed | review of lead code (`wf_c99461cc-949`, RV-lead-billing-p17-2), 48dcbe4 | A provider_pending redemption whose target renewal never happened (subscription expired) was never resolved: the family was blocked for good and a cap/budget slot leaked | — | `resolveUnreachableRedemptions` rejects it from provider-derived facts (ended at/before the target, target month elapsed, or no first period within 30 days); runs in webhooks and the scheduled sweep | `apps/api/tests/lead-billing-p17.review.test.ts` / `supabase/tests/lead-billing-p17.review.test.ts` failed before, pass after | Store sandbox evidence when accounts exist |
| BUG-016 | Medium / AC_PROMO_12 | closed | review of lead code (`wf_c99461cc-949`, RV-lead-billing-p17-3), 48dcbe4 | A refund delivered before its charge event was dropped, so $1 accrued for a refunded month | — | Refunds/chargebacks for an unrecorded period are parked in `pending_refunds` (migration 0700) and applied when the period is recorded | `apps/api/tests/lead-billing-p17.review.test.ts` / `supabase/tests/lead-billing-p17.review.test.ts` failed before, pass after | Store sandbox evidence when accounts exist |
| BUG-017 | **High** / AC_BILLING_*, AC_PROMO_11 | closed | review of lead code (`wf_c99461cc-949`, RV-lead-billing-p17-4), 48dcbe4 | RevenueCat SANDBOX transactions (TestFlight/review) were booked as production billing periods and accrued school donations | — | The webhook compares the event environment with the runtime; production requires PRODUCTION; mismatches are ignored | `apps/api/tests/lead-billing-p17.review.test.ts` / `supabase/tests/lead-billing-p17.review.test.ts` failed before, pass after | Store sandbox evidence when accounts exist |
| BUG-018 | Medium / AC_CONN_05 | closed | review of lead code (`wf_c99461cc-949`, RV-lead-billing-p17-5), 48dcbe4 | A Stripe event whose processing threw stayed 'received' and its retry was answered 'duplicate', so a renewal coupon was never attached | — | Failures are marked 'failed' with a retryable 503; 'received' events older than a 5-minute lease are re-opened | `apps/api/tests/lead-billing-p17.review.test.ts` / `supabase/tests/lead-billing-p17.review.test.ts` failed before, pass after | Store sandbox evidence when accounts exist |
| BUG-019 | Medium / AC_PROMO_12 | closed | review of lead code (`wf_c99461cc-949`, RV-lead-billing-p17-6), 48dcbe4 | Stripe chargebacks never reversed the donation: a Dispute references a charge, never an invoice | — | Charges/disputes resolve to their invoice through the Stripe API (charge.invoice or invoice payments), then to our recorded period; won disputes reinstate once | `apps/api/tests/lead-billing-p17.review.test.ts` / `supabase/tests/lead-billing-p17.review.test.ts` failed before, pass after | Store sandbox evidence when accounts exist |
| BUG-020 | Low / AC_PROMO_12 | closed | review of lead code (`wf_c99461cc-949`, RV-lead-billing-p17-7), 48dcbe4 | A Stripe partial refund was booked as a full refund | — | Refunds below the charged amount are recorded as partial with the real amount | `apps/api/tests/lead-billing-p17.review.test.ts` / `supabase/tests/lead-billing-p17.review.test.ts` failed before, pass after | Store sandbox evidence when accounts exist |
| BUG-021 | Medium / AC_PROMO_06 | closed | review of lead code (`wf_c99461cc-949`, RV-lead-billing-p17-8), 48dcbe4 | A Stripe renewal invoice listing proration lines first lost its coupon (period read from lines[0]) | — | The non-proration subscription line with the latest start defines the period and price | `apps/api/tests/lead-billing-p17.review.test.ts` / `supabase/tests/lead-billing-p17.review.test.ts` failed before, pass after | Store sandbox evidence when accounts exist |
| BUG-022 | Medium / AC_PROMO_10 | closed | review of lead code (`wf_c99461cc-949`, RV-lead-billing-p17-9), 48dcbe4 | School report amounts revealed 1-4 family groups (eligible*100 - accrued/paid) | — | Amount-derived group sizes join the complementary suppression in `school_month_report` (0690) | `apps/api/tests/lead-billing-p17.review.test.ts` / `supabase/tests/lead-billing-p17.review.test.ts` failed before, pass after | Store sandbox evidence when accounts exist |
| BUG-023 | Low / AC_ACCESS_10 | closed | review of lead code (`wf_c99461cc-949`, RV-lead-billing-p17-10), 48dcbe4 | A family tombstoned during a webhook's provider fetch still had entitlements, capacity and periods written (also in donation accrual) | — | Ledger transactions lock the family row and abort when it is tombstoned | `apps/api/tests/lead-billing-p17.review.test.ts` / `supabase/tests/lead-billing-p17.review.test.ts` failed before, pass after | Store sandbox evidence when accounts exist |
| BUG-024 | Low / process (AC_ECC_07) | closed | 48dcbe4 | Expected: no commit with lint errors. Actual: `git add`/`git commit` sat on a separate line after a `&&` chain containing ESLint, so the failed lint did not stop the commit (two unnecessary type assertions pushed) and the ledger update in the same chain was skipped | "Chain every step with &&" — rejected as a control: a third occurrence of the L-004 class shows discipline is not enough | Versioned pre-commit hook `scripts/git-hooks/pre-commit` (prettier + eslint on staged files + secret scan) enabled with `core.hooksPath`; lint errors fixed by validating Stripe responses with zod | Hook blocks a staged file with a lint error (checked manually) | — |
| BUG-025 | **High** / AC_GRADING_01/02, P6 (restated problem graded correct) | closed | domain grading review (`wf_937b0690-525`, RV-grading-1), adad1b7 | Expected: an answer that restates the problem ('35 ÷ 5', '2³') is not a final answer. Actual: `judge()` compared values only, so any expression evaluating to the key was deterministic 'correct' and final | — | Expression-form answers are unresolved `UNEVALUATED_EXPRESSION` unless simplest form is requested; the division checker uses the detailed parser on every path. Three acceptance rows that encoded the defect were corrected (cited) | `packages/domain/src/grading/review.test.ts` + `review.neighbors.test.ts` failed before, pass after | Scan-level regression once answer-guard fix lands |
| BUG-026 | Medium / AC_GRADING_02 (parser and unit ambiguities) | closed | RV-grading-2..5, adad1b7 | '1/2%' read as 50; '25%' key vs bare 25 inconsistent; '2-½' guessed as 3/2; liquid 'oz' forced to mass | — | '%' binds to a whole fraction; percent keys normalized once; hyphenated glyph mixed numbers parsed; ambiguous 'oz' resolved by the key's dimension | grading review tests fail before, pass after | — |
| BUG-027 | Medium / AC_PROMO_12, P17 school privacy (small group revealed by subtraction) | closed | RV-donations-4, 5452e23 | Expected: a school cannot learn about fewer than 5 families. Actual: 6 active − 5 eligible exposed the one discounted family; amounts ($1/family) were also counts | — | Complementary suppression over counts and family-equivalent amounts in `reporting.ts` (DB side already in 0690) | `packages/domain/src/donations/review.test.ts` + property test | — |
| BUG-028 | Medium / AC_REWARDS_03, P3 step-up (parent spends child points without unlock) | closed | RV-rewards-4 (domain), a1541b6 | `requestRedemption` accepted any parent principal | — | Parent requests need `recentAdultUnlock === true` (STEP_UP_REQUIRED) | domain rewards review + property tests | — |
| BUG-029 | **High** / AC_ACCESS_08, P3 (child mode reaches parent data) | closed | RV-rewards-3 (mobile), a1541b6 | The mobile approvals screen took its client from the parent token source, which stays registered in child mode, so a deep link showed every balance and live Approve buttons | — | Screen gated by `useParentAccess()`; server-side fail-closed for parent tokens in child mode is part of the identity fix set | `apps/mobile/src/rewards/review.test.ts` RV-rewards-3 | Identity fix (b): parent token sources refuse in child mode |
| BUG-030 | Medium / AC_REWARDS_04, P12 no links in reward text | closed | RV-rewards-1/5, a1541b6 | amazon.fr/?tag=, amzn.eu/d/… and hidden hosts passed | — | NFKC + default-ignorable stripping, scheme/www/Amazon-any-suffix/host+path refused in contracts and domain | `apps/api/tests/rewards.review.test.ts` + neighbours | — |
| BUG-031 | Medium / AC_SECURITY_05, AC_UX_02, P13 (public copy promised unimplemented behavior) | closed | RV-public-site-2/3/4, 51df77e | Deletion page told parents to export first (no export files are produced yet), implied any parent can delete the family (owner only), and omitted retained consent/audit records | — | Copy derived from the enforcing code (route rule, purge header, handler registry) with copy tests | `AccountDeletionPage.review.test.tsx`, `LegalPages.test.tsx` | Update copy when export_build is wired |
| BUG-032 | Medium / AC_UX_01, WCAG 1.4.11 | closed | RV-public-site-5, 51df77e | Global focus ring gold #ffb84d = 1.63:1 on the page | — | Ring is navy (12.4:1) | `LandingPage.review.test.tsx` RV-public-site-5 | — |
| BUG-033 | Low (grouped) | closed (RV-rewards-6 in BUG-033a) | RV-donations-1..3, RV-rewards-1..3/6 (domain), RV-rewards-2/4/5/7/8 (vertical), RV-grading-6/7 | Unknown settlement status earned $1; won chargeback restored $1 after a partial refund; invisible transfer reference accepted; invisible answers earned points; release larger than reserve minted points; NUL → 500; no unlock route from STEP_UP; stale requests actionable; no minus key on iOS; child copy over-promised; passage flag under-reported; nullable key fields threw | — | Allow-lists and fail-closed parsing per module (see commits) | Each module's review tests fail before, pass after | — |
| BUG-034 | Low / process (AC_ECC_07) | closed | 39ca165 (fixed by 0d67577) | Expected: every commit builds on its own. Actual: the learning-runtime commit imported `@pencillift/domain/scheduling`, which was untracked, so 39ca165 does not typecheck in isolation; every gate ran against the combined working tree | "Stage whole directories" — rejected: the dependency crossed packages and other agents' files are mid-edit | Pre-commit runs `scripts/check-staged-tree.sh`, which typechecks the staged tree with workspace links re-pointed at the index copy (a first version resolved `@pencillift/*` through pnpm's real-path symlinks into the working tree and was corrected) | Removing `packages/domain/src/scheduling` from the index makes the script fail with TS2307 in `learning-jobs.ts`/`learning.ts`; restored index passes | — |
| BUG-035 | Medium / AC_LEARNING_07, AC_LEARNING_02 | closed | domain learning review (RV-learning-1..3), 39ca165 | With 3–5 weekly weaknesses the Thursday review never used prerequisites/current material and ignored the teacher's test scope; a silent UTC day boundary counted one evening as two sessions, so a skill read 'strong' | "A per-skill cap of 2 is enough" — rejected: 3 skills × 2 filled all 6 slots | One question per distinct concept before any repeat; a reserved test-scope slot; zone-less day counting uses 24-hour windows that can only undercount | `packages/domain/src/learning/review.test.ts`; `learning-jobs.test.ts` Thursday assertion updated (it pinned the defect) | — |
| BUG-036 | Low / AC_LEARNING_04 | closed | RV-learning-4/5, 39ca165 | Daily set could lose its confidence item to another slot's backfill; a strong skill with fresh repeated errors was dropped | — | Slot chains run before any backfill (tiers); repeated-error rule checked before status exclusion | learning review tests | — |
| BUG-037 | **High** / P6, AC_GRADING (answer leak to child) | closed | answer-guard review (RV-answer-guard-1/2), e76b9f3 | Zero-padded numerals over 40 digits and not-strictly-valid UTF-8/UTF-16 encodings were dropped unscanned, so a hint could carry the exact answer | — | Bound counts significant digits only; lenient UTF-8 and UTF-16 decoders scan what strict decoding rejects (fail closed) | `packages/domain/src/answer-guard/review.test.ts` | — |
| BUG-038 | Medium / P6, P12, P16 (guard coverage gaps) | closed | RV-answer-guard-3..9, e76b9f3 | \\cfrac and {,} idioms, locale-ambiguous answers, modal reveal phrasing, index-form MC keys, private grading keys and P16 commercial DTOs, and ideographic-dot short links passed the guard | — | Readings and cues extended; forbidden keys cover grading internals, explicit correct-answer fields and commercial fields; URL detection normalizes ideographic stops | answer-guard review tests; false-positive check on 1,523 lines of repo prose | — |
| BUG-039 | Low (grouped) | closed | RV-answer-guard-10..16, RV-scheduling-2/3 | TLD-free short links, non-ASCII digits, array extra keys, raw keys in reasons, 'a in b', ordinals, apostrophe-less targets; notification audience/flags not allow-listed; loose top-up key parsing | — | Per-module allow-lists and parsers | module review tests | — |
| BUG-040 | Medium / P8 time zones (AC_LEARNING_08) | closed | RV-scheduling-1, 0d67577 | A U+2212 minus offset ('−05:00') passed the fixed-offset rejection and shifted releases an hour | — | Any Unicode sign/dash lookalike at the start of a zone is refused | `packages/domain/src/scheduling/review.test.ts` | — |
| BUG-041 | **High** / AC_ACCESS_09 (removed guardian keeps a credential) | closed | RV-family-1, 8fd957c | A removed guardian's unexpired pairing code still minted a 30-day child session | — | Removal retires the guardian's unredeemed codes in the same transaction | `apps/api/tests/family.review.test.ts`, `guardians.test.ts` neighbour (mutation-checked) | — |
| BUG-042 | **High** / AC_ACCESS_04, AC_UX_02 (child flow unreachable) | closed | RV-family-3, 8fd957c | No screen could assign an unused paid slot to a draft child, so pairing and the whole child flow were unreachable | — | Web and mobile call `POST /v1/children/:id/activate` | `ChildrenPage.review.test.tsx`, mobile family tests | — |
| BUG-043 | Medium / AC_ACCESS_09, AC_ACCESS_08 | closed | RV-family-2/4/5, 8fd957c | Concurrent invitation acceptance put one adult in two families; PIN reset unlinked; pending consent had no restart | — | Per-adult transaction lock (partial unique index lands with the identity fixes); reset link; restart control | family review tests (12/12 raced before, 0/48 after) | — |
| BUG-044 | Medium / AC_ACCESS_10, AC_CAPTURE_01/06, P11 | closed | RV-homework-1..5, c7ed013 | Resume skipped consent/slot gate; failed storage removal on cancel left photos past retention; released slots still scanned; lost finalize response failed forever; overrides never closed review | — | Gate on resume; commit then remove with per-row confirmation; one paid-profile rule; finalized status recognised; overrides settle the scan | `apps/api/tests/homework.review.test.ts`, web/mobile review tests | — |
| BUG-045 | Medium / AC_UX_02 | closed | RV-homework-9 + RV-homework-6..8, c7ed013 | No parent upload path; misleading allowance and retry copy; confirmations vanished on refresh | — | Parent uploader on web; copy from real states; refreshing queries keep data | web HomeworkPage review tests | Parent upload on mobile (not built) |
| BUG-033a | Low / AC_UX_02 | closed | RV-rewards-6, 8fd957c | Mobile rewards, planner, resources and privacy screens had no link; typed routes are not part of tsc so nothing caught it | — | Parent home links every tool; `apps/mobile/src/navigation.test.ts` fails on dead links and orphan screens | navigation test failed with 4 orphans before, passes after | 8fd957c's navigation test needs `planner.tsx` from the learning UI commit |
| BUG-046 | **High** / AC_BILLING_03, AC_CAPACITY_03 (one purchase, two families; released children stuck) | closed | billing review (RV-billing-1/2), 5dfbc70 + bd53b73 | A RevenueCat restore/transfer left capacity on the old family; after a lapse or downgrade, released children stayed `active` without a slot and could never be reassigned. The webhook path had the same gap as sync | "Sync-only fix is enough" — rejected: webhooks called applySnapshots directly, so the gap stayed until a manual sync | Whole-family reconciliation shared by sync, webhooks and TRANSFER events (vanished subscriptions revoked, former holders re-verified, slotless children to draft) | `billing.review.test.ts`, `webhooks-reconcile.test.ts` (3 tests red before, green after) | Store sandbox evidence when accounts exist |
| BUG-047 | **High** / AC_CAPACITY_11, approved prices | closed | RV-billing-4, 5dfbc70 | A store price differing from the approved table (App Store $49.99 for 2 children) was sold with only a notice | — | Server computes `priceCheck`; a differing tier is blocked | mobile billing review tests | Real store price points (Owner Actions) |
| BUG-048 | Medium / AC_BILLING_05, AC_SECURITY_04 | closed | RV-billing-3/5/6/7, 5dfbc70 | Server picked kept children on an incomplete downgrade list; billing-retry did not block a parallel purchase; a secret-shaped RevenueCat key could reach the bundle; SDK stayed bound after sign-out | — | Explicit keep list; retry state blocks purchase; only appl_/goog_ keys; SDK logged out on sign-out/child mode | billing review tests | — |
| BUG-049 | Medium / AC_MON_* (P16) | closed | RV-MON-01..09, 52ef288 | Impression cap raced; prefetch and dismissed cards billed; client-declared platform chose merchant mode; live sponsor rename changed ad text; import dedupe bypassable by source; weak eligibility evidence; affiliate links without a linking mechanism | — | Serialized cap check, view timing and dismissal gates, server-known property, creative-bound label, source-free dedupe, strict evidence, recorded linking mechanism | `monetization.review.test.ts` (API + domain) | 0640 edited in place (never applied) |
| BUG-050 | Medium / AC_PROMO_09/14, AC_UX_02 | closed | RV-p17-ui-1..8, d177f05 | Non-redeemable campaigns shown redeemable; budget accepted that the listing contract rejected; PATCH could not clear code settings; blocked App Store cells left pending; generic school errors; wrong batch month; focus loss; misleading school-change message | "Cap the console at $100k" — replaced: one contract bound for input and listing | Shared `MAX_PROMO_BUDGET_CENTS`; PATCH replaces code settings as a group; UI guard removed | `p17-ui.review.test.ts`, web/mobile review tests | — |
| BUG-051 | **High** / AC_LEARNING_01/02, AC_LEARNING_08 | closed | RV-learning-api-1/6, ccb600a | A re-served bank question counted as new evidence (inflated mastery); a tick racing a schedule change cancelled the new review job permanently | — | `attempts.evidence_key`; schedule re-read under lock before cancelling | `learning.review.test.ts` | Points policy for re-served items (owner) |
| BUG-052 | Medium / AC_LEARNING_07/09, AC_REWARDS_01 | closed | RV-learning-api-2/3/4/5/7/8, RV-learning-db-1, RV-learning-bank-1..3, ccb600a | Oldest evidence kept; guessed-through sets earned completion; concurrent last answers left sets open; DST skipped a week; unchecked AI intro; duplicate base reviews; child could read unreleased reviews; ambiguous spelling items; wrong distractors; overstated coverage | — | Newest-first evidence, meaningful-attempt rule, set lock, calendar weeks, intro validator, unique index, release-instant RLS, bank fixes | learning review tests (API, DB, domain) | — |
| BUG-053 | **High** / AC_ACCESS_10, AC_SECURITY_05 (deletion did not stop running work) | closed | review of lead code (RV-lead-jobs-ai-3), 91fc8c2 | A family or child deletion during a running scan still sent the child's homework to the AI and wrote child rows; request_deletion cancelled only queued jobs | — | Tombstone moves assignments to 'deleted' (every CAS fails); `assertActive` before each AI stage and inside each write batch under FOR SHARE; consent re-checked | `lead-jobs-ai.review.test.ts` RV-3 (both), scan-process consent/archive tests; checker probes G/G2 | — |
| BUG-054 | **High** / AC_SECURITY_05, P4 purge (derived data survived deletion) | closed | RV-lead-jobs-ai-4/5/6/20 + checker follow-ups, 91fc8c2 | Export files were orphaned by the purge; pages with soft-deleted rows or late uploads (signed URL valid 2 h) were never removed; expired exports were never removed | "storage_removed_at is final" — rejected by the checker: a late PUT recreated the object after the stamp | Purge removes export files; `storage_removed_at` + upload-window trigger schedules a second pass; expired-export sweep | RV-4/5/6/20 review tests; scheduled tests; checker probes A/P/H | — |
| BUG-055 | **High** / P6 answer protection (answers in child hints) | closed | RV-lead-jobs-ai-7/8 + checker follow-up, 91fc8c2 | The model-free prompt key was never protected (hint '12 × 7 is 84' released); keys like 'B) 3/6', 'x = 4', 'A and C' protected only literal text or the first letter | — | Protect the deciding key, template on key disagreement, parse letters/equations/every number, fail closed on unparseable or multi-candidate keys | RV-7/8 review tests; scan-process tests; checker probes C/D | — |
| BUG-056 | **High** / P11 usage limits (allowance farming) | closed | RV-lead-jobs-ai-17/18, 91fc8c2 | Deleting a child deleted its usage reservations, so delete-and-re-add reset the family's monthly pages; purged child ids stayed in jobs and usage rows | — | Reservations kept pseudonymised and counted; purge deletes the scope's jobs and nulls usage child ids; late usage rows pseudonymised under the family lock | RV-17/18 review tests (API + DB); checker probes B/R | — |
| BUG-057 | Medium / AC_CAPTURE_06/07, AC_SECURITY_06 (job ledger and cost) | closed | RV-lead-jobs-ai-1/2/9/10/15/19 + 22dba32 | Batch claims let leases expire and jobs run twice; dead letters left scans and reservations stuck; retries re-extracted; concurrent stages overshot the spend cap (scans, then learning); one failing sweep stopped the tick; recheck overrode parent overrides | — | One claim per job with fenced completion; per-kind dead-letter compensation; extract once; spend holds (scans and learning); isolated tick steps; parent override authoritative | review tests; scheduled/learning ceiling tests (2 calls before, 1 after) | — |
| BUG-058 | Medium / P4 retention (inactivity deletion) | closed | RV-lead-jobs-ai-11..14 + checker follow-up, 91fc8c2 | Notices stamped without a delivered email; stale notices deleted active families; paying families (including store grace period) could be deleted; sweep only in a 5-minute window | — | Stamp after verified send; notice must be newer than last activity (re-checked under lock); one `family_may_be_charged` rule; once-per-day durable marker | RV-11..14 review tests; checker probes L/M/N | Owner Action 13 (policy approval) |
| BUG-059 | Medium / AC_CAPTURE_02, P4 (image metadata) | closed | RV-lead-jobs-ai-16 + checker follow-up, 91fc8c2 | PNG then JPEG stripping were deny-lists; vendor carriers (FPXR/JFXX APPn, JPGn) passed GPS text through | — | Allow-lists for both formats; JFIF APP0 rewritten to fixed fields | image-metadata tests; checker probe O | — |
| BUG-060 | Medium / AC_ACCESS_08, AC_ACCESS_04, P3 (identity and access) | closed | RV-lead-identity-access-1..11 + checker follow-ups, fc2eb8b | Concurrent wrong PINs never locked out (and could clear a lock); one adult could own two families; whole-family export downloadable without step-up; pairing guesses unlimited per IPv6 /64, then a global budget that ignored in-flight guesses and let anyone stop all pairing; stale-session PIN-reset budget abuse; mixed clocks; header-only body limit; JWKS per request with outage shown as 'sign in again'; torn keychain writes; archived child's codes still paired; logout did not end API access | "Peek then hit" budget — rejected by the checker (40 concurrent guesses passed at 199/200) | Row-locked PIN attempts; partial unique index; step-up for exports; per-network + reserved shared budget pausing only spending sites; DB clock; streamed body limit; module JWKS cache; generation-based storage; code-retiring trigger; live-session check on the shared verifier | lead-identity review tests (API, DB, mobile); second check: sign-out on 14 route families; lead address-spelling test | Owner Action 20 (auth.sessions trigger on hosted Supabase) |
| BUG-061 | **High** / AC_GRADING_01/02, P6 (restated problem graded correct at scan level) | closed | lead integration check after BUG-025 | Expected: '35 ÷ 5' for '35 ÷ 5 =' is never graded correct. Actual: the domain now returns unresolved UNEVALUATED_EXPRESSION, but the scan pipeline then accepted two confident model 'correct' verdicts, so the child was told it was right (same for a word problem via the model key) | "The domain fix covers it" — rejected: resolveGrading lets model agreement decide whenever the deterministic check is unresolved | When the key is a plain value (prompt-computed, or a non-expression model key), an unevaluated-expression answer goes to parent review; prompts that ask for an expression are unaffected | `scan-process.test.ts`: restated computation and word-problem tests failed before, pass after; mutation (rule off) makes both fail; expression-prompt test stays correct | — |
| BUG-062 | Low / AC_ECC_07 (test timing) | closed | full gate at f308356 under load (coverage agents running) | `bank/review.test.ts` RV-learning-bank-3 timed out at 5.57 s (default 5 s) | "The coverage report became slow" — rejected by measurement: `bankCoverage` takes 0.1 ms per call (the parent route is unaffected); the test itself runs 18 full-bank generations (~3.3 s at load average 7 on 4 CPUs) | Explicit 60 s timeout, the same budget `bank.test.ts` uses for the same work; assertions unchanged | Test passes in isolation; full gate re-run on an idle machine (docs/Test_Evidence.md) | — |
| BUG-063 | **Critical** / AC_DEPLOY_07, every route using array parameters | closed | found by the coverage defect-fix checker (billing-gates slice), lead-verified | Expected: the deployed Worker runs the same queries the tests run. Actual: `apps/api/src/index.ts` built its Postgres client with `fetch_types: false`, so postgres.js could not serialize JavaScript arrays and every array parameter (`${ids}::uuid[]`, `= any(${list})`, 30+ call sites: purge, learning jobs, promotions, capacity changes, monetization) would fail in production with "malformed array literal". All suites passed because the test harness built its client with default options | "Hyperdrive needs fetch_types off" — rejected: Cloudflare recommends it only for schemas without array types | One factory `createPostgresClient` (fetch_types on, prepare off) used by the Worker and by the API test harness, so every API test now runs with production options; a guard test fails if any other API source file constructs a client | Lead probe: 4 array forms fail with the old options, pass with the new; `pg-client.test.ts` mutation-checked (fails with fetch_types off); full API suite 609 tests through the production factory | Hyperdrive smoke test on staging (Owner Action 8) |
| BUG-064 | Medium / AC_SECURITY_06, E4 Cost controls (spend ceiling overshoot) | closed | coverage defect-fix workflow wf_aea7bbc1-d46 (spend-strict slice) + lead follow-up | Expected: no AI stage runs when it could push recorded spend past the owner's monthly cap. Actual: admission checked only spent + live holds < cap, so the stage that crossed the cap still ran (a review test even asserted the crossing), and the 100% alert fired only after the overshoot; a cap beyond 2^53 micros made a learning job throw instead of keeping the bank set | "One stage of overshoot is inherent" — rejected: the stage's upper-bound estimate is known before the call | `acquireSpendHold` admits only when spent + holds + estimate ≤ cap (domain `evaluateSpend`), raises the 100% alert on the first refusal, rejects non-finite estimates, and throws the typed `SpendCeilingUnevaluable` when amounts cannot be compared exactly; learning personalization keeps the reviewed bank items on either refusal (logged `SPEND_CEILING` / `SPEND_UNEVALUABLE`) | `spend-ceiling.test.ts` (new), scan/learning cap tests assert recorded ≤ cap, RV-10 review test strengthened to 'no request at one micro of headroom'; the undecidable-budget learning test fails with the fallback removed and passes with it; API suite 612/612 | — |
| BUG-065 | **High** / AC_CAPACITY_02, Owner action #1 (price gate skippable) | closed | coverage defect-fix workflow wf_aea7bbc1-d46 (billing-gates slice) | Expected: a capacity change to a tier whose verified store price differs from the approved $39.99 + $9.99 × (n−1) is refused before any store opens. Actual: the check ran only when the client sent the optional `channel`; omitting it, or naming a store other than the family's managing store, skipped the gate | — | `storesToCheck` always includes the managing store and the named store; with neither known, every store that would be offered (web only while enabled) is checked; `assertApprovedStorePrice` refuses any differing verified price in that set | Four new `billing.test.ts` cases; mutation (returning only the named store) fails two of them; API suite 612/612 | Real store prices remain owner-verified (Owner_Actions #1) |
| BUG-066 | Medium / AC_MON_04, P16 reporting (ad-eligible cohort) | closed | coverage defect-fix workflow wf_aea7bbc1-d46 (billing-gates slice) | Expected: the owner's ad-eligible cohort is exactly the families the server may show a sponsor card. Actual: the report's SQL restated the ad-free rule across every billing environment (a sandbox entitlement hid a production family) and without the grace window `grantsAccess` applies | — | The summary selects ad-free entitlements in this runtime's billing environment and filters them with the domain `grantsAccess`, the same predicate `familyIsAdFree` serves with | `admin-monetization.test.ts` cohort case fails with the environment filter removed; `runtime.test.ts` runs the query through the Worker's own client (uuid[] parameter) | — |
| BUG-067 | Medium / AC_DEPLOY_07, AC_ACCESS_02 (mock consent outside development) | closed | coverage defect-fix workflow wf_aea7bbc1-d46 (billing-gates slice) | Expected: the labeled consent mock is never wired outside development/test. Actual: the Worker always wired `createDevelopmentConsentMock()`; production readiness blocked it, but staging served the mock and could record mock consent | — | Config selects `development_mock` only in development/test, `unavailable` (refuses start/status) in staging/production without an adapter, `configured` only for an implemented adapter; the Worker refuses any mock outside development/test; `runtime.test.ts` keeps the adapter lists equal | `runtime.test.ts` + `config.test.ts`: three cases fail with the environment rule removed | Staging consent start reports PROVIDER_UNAVAILABLE wording (low; Release_Readiness) |
| BUG-068 | Medium / AC_CAPTURE_01, AC_UX_02 (PDF import offered, always failed) | closed | coverage defect-fix workflow wf_aea7bbc1-d46 (capture-ui slice), c0aab46 | Expected: the child scan screen offers only formats the scan job can read. Actual: mobile offered 'Add a PDF' (expo-document-picker) and accepted PDF and un-re-encoded HEIC pages; every such scan ended `failed_final FORMAT_NEEDS_CONVERSION` | "Web has the same defect" — rejected: the web uploader already filtered to readable types | Mobile validation and the limits card use `readableTypes(limits)` (allowed ∩ `HOMEWORK_READABLE_MIME_TYPES`); the PDF source and picker are gone; copy says PDF can't be added yet | `scan-session.test.ts`, `upload.test.ts`, new `scan-screen.test.ts` (failed on the old code) | Remove the now-unused `expo-document-picker` dependency |
| BUG-069 | Medium / AC_GRADING_03 (rubric feedback never shown) | closed | coverage defect-fix workflow wf_aea7bbc1-d46 (capture-ui slice), c0aab46 | Expected: after step-up, a parent sees rubric feedback for written work. Actual: web labelled items 'see rubric feedback' but never rendered `solution.rubric`; the child app said 'Feedback is ready' for rubric items that have no feedback rows | — | Typed `homeworkRubricSchema` (additive); web `RubricFeedback` region (✓ Met / ○ Not yet, text not colour) inside step-up only, plus 'No rubric feedback was recorded' when absent; child copy depends on whether guarded feedback exists | `HomeworkPage.test.tsx` rubric block (3, failed before), `homework-rubric.test.ts` (real Postgres: step-up gets rubric, no step-up gets 403, child DTO has none), `result-view.test.ts` | The scan job coaches only incorrect items, so written work gets no child feedback (recorded gap) |
| BUG-070 | Medium / AC_CAPACITY_08, P11 (inactive profiles lose history) | closed | coverage defect-fix workflow wf_aea7bbc1-d46 (archived-history slice), 622d74b, ae9f088 | Expected: parents keep read access to an archived or downgraded child's history. Actual: learning reads returned NOT_FOUND for archived children, rewards listed only active children; making them readable also exposed a child whose deletion is under way through the answer key and review export | "`status <> 'archived'` means 'not being deleted'" — rejected: deletion marks the child archived until the purge runs | `ownedChild(c, 'read' / 'write')`: reads admit draft/active/archived in a live family and exclude open deletion requests; writes refuse archived with CHILD_ARCHIVED; `ownedSet` guards set-level routes; rewards overview and history use `notBeingDeleted`; the balance DTO carries `status` so the portal renders archived/draft balances read-only | `archived-history.test.ts` (history byte-identical before/after archive; writes refused and change nothing; deletion-in-progress invisible everywhere); rewards API/web/mobile tests | Archived schedule preview still lists future releases (low) |
| BUG-071 | Medium / P11 'stop paid AI for inactive profiles' | closed | archived-history checker note, lead fix b0496a8 | Expected: no paid AI for an archived or downgraded child. Actual: a parent transcription correction queued a paid `scan_process` recheck without the capture entitlement check | — | The correction route calls `assertCanCollect` after the correctable-status check | `archived-history.test.ts` 'a transcription correction queues no paid recheck for an archived or downgraded child' | — |
| BUG-072 | Medium / AC_PROMO (school reports), P17 privacy | closed | coverage defect-fix workflow wf_aea7bbc1-d46 (p17-money slice), e52b6a9 (migration 0730) | Expected: a school's month shows what was actually paid and which families' payments stand, and never lets a school viewer derive a group of 1–4 families. Actual: `paid_cents` ignored reversals and reinstatements netted in later paid batches and dropped batches moved to 'adjusted'; refunded and charged-back periods counted as active/paying; with corrected figures the old difference list leaked 1-family groups | "Accuracy alone is safe" — rejected: an accuracy-only intermediate migration failed the new suppression tests (2 of 6) | `app.school_month_donation_figures` (paid = accruals + adjustments in 'paid'/'adjusted' batches); counts only standing periods; one rule `app.reveals_small_group` hides when any figure or pairwise difference is 1–499 cents; helpers not executable by clients | `school_report_accuracy.test.ts` (6, each failed before), `admin-promotions.test.ts` refund-after-payout case | Caller-chosen `p_zone` on the counts RPC (see BUG-074) |
| BUG-073 | Medium / P17 'record late settlement against the original period' | closed | coverage defect-fix workflow wf_aea7bbc1-d46 (p17-money slice), e52b6a9 | Expected: a settlement arriving months later still earns the $1 for its original program month. Actual: the accrual window was hard-coded to the run month and the two before, so later settlements never accrued | — | `DONATION_SETTLEMENT_LOOKBACK_MONTHS = 12`; only families with work left are evaluated; the donation month stays the program month of `period_start` | `p17-jobs.test.ts` (5, all failed before: five-month-late settlement, reruns and concurrency keep one $1 per month, bounded lookback across the zone boundary, scheduled tick) | One family whose plan throws aborts the run (pre-existing, recorded) |
| BUG-074 | Medium / AC_PROMO_10, P17 'schools see only authorized aggregate figures' | closed | p17-money checker note (pre-existing since 0690), lead fix | Expected: a school viewer can never derive a group of 1–4 families. Actual: `school_month_report_counts` took a caller-chosen zone and is granted to `authenticated`, so a school admin calling the Data API directly could read the same month under two zones and difference the results, isolating families whose period started in the offset band; suppression works only within one call | "Pin the zone to UTC" — rejected: donation months use the configured program zone, so counts would disagree with the report | Migration 0740: the API states the program zone for the report transaction (`pencillift.program_zone`, same pattern as `pencillift.request_now`, not settable by Data API clients); a school viewer's call must use exactly that zone, so a direct call is refused; the owner (exact figures) may pass any zone. The owner report route states the zone | `school_report.test.ts`: other zones and a call with no stated zone are refused (fails with the check removed), the stated zone is served, the owner reads any zone, clients cannot call the helper; accuracy and admin-promotions suites pass | No school-viewer portal exists yet; its route must state the zone the same way |
| BUG-075 | Medium / AC_GRADING_03, P6 'for writing tasks evaluate a rubric and provide feedback' | closed | coverage gap after BUG-069, lead fix | Expected: a child gets rubric feedback on written work, never a false grade or model-written text to copy. Actual: the scan job wrote child feedback only for 'incorrect' items, so written work reached the child with no feedback at all (the parent saw the rubric after step-up) | "Show the rubric notes" — rejected: notes are free model text and can contain a rewritten sentence to copy | `childRubricFeedback` (apps/api/src/jobs/rubric-feedback.ts): only criterion labels, in fixed wording ('Next time, work on: …' / 'You did this well: …'), unmet first, at most 4; a label is used only when it is one line, ≤ 80 characters and 12 words, with no quotation marks, links, emails or control characters; nothing is shortened into shape. No AI call; crash replay writes once per transcription. Grading prompt v2 asks for short child-readable criterion labels | `rubric-feedback.test.ts` (6); `scan-process.test.ts` 'written work gets rubric feedback in fixed wording; the model’s notes never reach the child' (fails with the wiring removed) and 'no child-safe label gets no rubric rows'; API suite 620/620 | Label wording quality depends on the real model (unmeasured) |
| BUG-076 | **High** / AC_GRADING_07/08, P6 'no original problem's final numeric value in hints' | closed | coverage defect-fix workflow wf_aea7bbc1-d46 (guard-expressions slice, two rounds + adversarial check) + lead integration | Expected: a hint never discloses the protected answer, in any form. Actual: the guard read only literal values, so 'The answer is 6 × 7.' (42), 'six times seven', '7+7+7+7+7+7' reached the child; round 1 added an expression reader but (check) it was opt-in and no production caller opted in, and it missed tight LaTeX ('$6\times7$'), markdown/quoted/currency/unit operands ('**6** × **7**', '$20 − $8', '6 cm × 7 cm'), vertical arithmetic, implicit products, superscripts and bracket groups; a hostile packet cost ~22× CPU | "Evaluate expressions only where a caller opts in" — rejected by the lead at integration: a missed opt-in fails open for every future child-facing call site | `answer-guard/expressions.ts`: bounded tokenizer + exact-rational parser (never eval) reading LaTeX (\times, \cdot, \div, \frac), transparent decorations and unit words, vertical arithmetic, juxtaposition, bracket groups, Spanish 'veces'; ranges/dates are not subtraction; unboundable or ambiguous chains fail closed; MAX_PARSES = 1,000 per text with a per-scan cache (hostile packet 2.6× instead of 22×). Lead: evaluation is ON unless explicitly false; only the bank's problem-statement self-check (bank/validate.ts) opts out; both API call sites state it explicitly | `expressions.test.ts`, `expressions-shapes.test.ts` (85; 73 failed before round 2); `scan-process.test.ts` 'a hint that discloses the key as an expression … falls back to the template' (fails with the default off and the option removed); `guard-call-sites.test.ts` (fails when an API call site drops the option or a new site opts out); domain 2296/2296 | Documented non-operators ('of', 'by', ':'), count nouns outside UNIT_WORDS and scientific notation are not read (expressions.ts header) |
| BUG-077 | Medium / AC_REWARDS_01, P9 'configurable earning rules' | closed | gap-closure workflow wf_882d1210-e26 (reward-rules slice, adversarial check ok) | Expected: each family sees and can change its published earning rules, and awards follow them. Actual: `public.reward_rules` had no API, no UI and no client write path, so every family ran on defaults it never saw | — | Migration 0750 (`parent_set_reward_rules`, audited upsert, a 500 ms anti-farming floor enforced in the RPC, the check constraint and the API; `pl_child` reads the point values only); `GET/PUT /v1/reward-rules` (PUT needs a recent adult unlock like other reward writes; future awards only); the child rewards view returns `earningRules`; web RewardsPage 'How points are earned' with an edit form; mobile child 'How you earn points' | `reward-rules.test.ts`, `reward_rules.test.ts` (DB), RewardsPage rules tests, mobile view-model tests; API 69/69, DB 30/30, web 30/30, mobile 29/29 on the combined tree | Parent mobile rewards screen does not show the rules; clamp logic duplicated in rewards.ts and learning.ts (low); API and mobile must ship together (strict child schema) |
| BUG-078 | **High** / AC_CAPTURE_02 (decompression bombs, unverified stored bytes) | closed | gap-closure workflow wf_882d1210-e26 (capture-limits slice, three passes; second adversarial check medium items fixed or applied by the lead) | Expected: file signature, byte limits and image dimensions are validated and malformed or oversized files fail safely before any decoding or AI call. Actual: a 61-byte PNG declaring 50,000 × 50,000 was stripped and sent to the AI provider; JPEGs with no, several or late frame headers and PNGs without image data were accepted; finalize trusted the client-declared size (only `exists`); HEIC/PDF were accepted at registration and always failed later, after allowance was used | "The provider rejects huge images" — rejected: nothing may reach the provider unvalidated, and the Worker would decode first | `image-metadata.ts` parses PNG IHDR / JPEG SOFn / DHP and enforces 10,000 px per side and 60 MP (contracts `HOMEWORK_IMAGE_LIMITS`), exactly one SOF before SOS, non-empty IDAT, ≤ 4 components and ≤ 100 scans (TOO_COMPLEX), drops DNL; storage `stat` (Supabase object info endpoint; memory mock measures real bytes) lets finalize refuse size mismatches and objects over 15 MB (UPLOAD_MISMATCH, removed, resendable) and treat an unreadable answer as an outage; registration refuses HEIC/PDF with FORMAT_NOT_SUPPORTED_YET; mobile and web refuse over-limit pictures before upload (lead wired scan.tsx; two fixtures updated: a PDF page now PNG, the EXIF review JPEG made decodable) | `image-metadata.test.ts`, `capture-limits.test.ts` (scan job refuses a bomb with 0 AI requests), mobile scan-session/upload, web uploader tests; API 106/106, mobile 43/43, web 29/29 | sha256 not verified against stored bytes; storageReader reads without a size cap; bit depth and iCCP inflation unbounded; Supabase `stat` untested against a live project (Connections) |
| BUG-079 | **High** / AC_DEPLOY_07, AC_BILLING (mock billing outside development) | closed | gap-closure workflow wf_882d1210-e26 (release-gates slice) | Expected: without a RevenueCat or Stripe key, staging/production never answer from a mock. Actual: `buildRuntime` quietly fell back to `createSubscriberStateMock()` / `createStripeClientMock()`; in staging a RevenueCat webhook answered 200 from the empty mock state and the stale-entitlement sweep revoked paid plans | "Routes already refuse the mock" — rejected: only /billing/sync did; webhooks and the sweep read the mock | `selectBillingProviders`: labeled mocks only in development/test, an unavailable provider (refuses every call) elsewhere, reported by readiness (`billing_provider`, `web_billing_provider`); /billing/sync answers NOT_CONFIGURED for the unavailable provider (lead) | `runtime.test.ts`, `config.test.ts`, `readiness.test.ts`, `billing.test.ts` 'staging without a RevenueCat key says billing is not connected' (failed before the lead fix) | Storage and email still fall back to mocks in staging (recorded follow-up) |
| BUG-080 | Medium / AC_DEPLOY_07 fake-catalog clause | closed | gap-closure workflow wf_882d1210-e26 (release-gates slice, check found the serve path open; lead applied the fixer's serve-path patch and added its test) | Expected: production never serves fixture or fake catalog data. Actual: readiness had no catalog check, nothing refused fixture rows, and the serve path returned whatever the database held | — | Migration 0770: one definition of a fake row per catalog (fixture labels, RFC 2606/6761 reserved hosts, resources claiming availability without a passing link check), per-catalog counts for readiness (`catalog_data`), a `database_environment` mark whose production value refuses fake rows on write and is itself refused while any exist; the production serve path leaves fake resources and campaigns out (`withoutFakes`) even before the mark | `readiness.test.ts`, `release-gates.test.ts`, `serve-without-fakes.test.ts` (fails with the filter removed); DB schema invariants and monetization 36/36 | Owner marks each deployed database (Owner action #21) |
| BUG-081 | **High** / AC_SECURITY_04 (secret scan blind spots) | closed | gap-closure workflow wf_882d1210-e26 (release-gates slice, two passes) | Expected: static and dynamic secret scans catch credentials wherever they would ship. Actual: only tracked files were scanned; the service-role detector matched one base64 alignment of the role claim; values after JS escapes, percent-encoding or joining characters, non-compact JWTs, compressed files, symlinked dirs and UTF-16 text were missed; CI built with no public build variables, so a secret in VITE_*/EXPO_PUBLIC_* reached no gate | "A regex on 'service_role' base64 is enough" — rejected: base64 has three alignments and JWT JSON need not be compact | `scan-secrets.mjs --artifacts` (decodes JWT payloads, start-of-value anchoring, gzip/brotli/zstd, UTF-16, symlinks, source maps' original sources; unreadable archives fail); `scan-release-artifacts.sh` builds web, Worker (dry run), Expo web export and the native public config with release-shaped fake public values, scans them and runs a negative control that must find planted keys; wired into CI and verify.sh | `release-gates.test.ts` (scanner cases); local run: 'Artifact secret scan passed (69 files, 11.2 MiB in 4 directories)' and the negative control found both planted keys | EAS native binaries and deployed endpoints are not scanned (need a build/deployment) |
| BUG-082 | Medium / AC_DEPLOY_07, P4 retention ('notify the parent' before inactivity deletion) | closed | lead, follow-up to BUG-079 (release-gates notFixed) | Expected: outside development/test no provider answers from a mock. Actual: staging `buildRuntime` fell back to the in-memory storage mock and the email outbox mock, so guardian invitations reported 'pending' with nothing sent, and the inactivity sweep (blocked only in production) stamped a notice from the outbox and could delete a staging family 30 days later | — | `selectStorageAndEmail`: labeled mocks only in development/test; staging without Supabase keys gets a storage provider that refuses every call, and email is unavailable everywhere outside development/test until an adapter exists; the inactivity sweep refuses a mock outbox outside development/test | `runtime.test.ts` (3 new, 2 failed before), `scheduled.test.ts` 'outside development and test a mock outbox is never a notice' (failed before: notified 1; fails again with the production-only guard restored); runtime/scheduled/lead-jobs-ai/guardians 110/110 | Guardian invitations cannot be sent in staging until an email adapter exists (Owner action #14) |
| BUG-083 | Low / AC_ECC_07, AC_MON_16 (nondeterministic test) | closed | CI run #18 at b6e9998 (web job red; reproduced locally 1 in 12 under CPU load) | Expected: the sponsor-card viewability test is deterministic. Actual: 'reports viewed once, only after 50% visibility for 1000 ms' sometimes saw no /viewed call | "No observer exists at show()" — first probe (24 + 20 loaded runs) always found one, so the probe could not confirm it; the mechanism was then established from the libraries: Testing Library's findBy resolves after a real setTimeout(0), while React flushes the passive effect that creates the observer on a separately scheduled callback, and their order is not guaranteed under load | The test double reports only on show(), unlike a real IntersectionObserver (which reports the current state on observe()); `renderWithCard` now waits until the card is observed before faking timers and driving visibility. Product code unchanged (a real observer delivers the initial state) | 25 loaded runs after the fix: 0 failures (before: 1 in 12); web suite green | — |
| BUG-084 | **High** / AC_SECURITY_02, P4 (no severe-risk handling) | closed (software); templates and escalation await owner/educator/counsel approval | child-safety workflow wf_47dc67b7-ae8 (fix → adversarial check not ok: 3 high, 5 medium → re-fix) + lead integration | Expected: moderation before and after generation, safety templates for severe-risk input, adult report management and escalation (spec P4). Actual: nothing screened child answers or child-facing model output; a child writing 'I want to die' got normal AI tutoring. After pass 1 the check still found contractions ('I'll hurt myself'), first-person disclosures exempted by the school subject ('I was raped' in reading), canonical phrasings ('want to die', 'kms') and caregiver threats missed, a victim's words given the anger template, model outputs like 'We are best friends!' passed, homoglyph bypasses, and parent copy claiming what the child saw | "A subject such as health or history makes a disclosure educational" — rejected: a first-person statement is never exempted | `@pencillift/domain/safety`: bounded normalization (NFKC, leetspeak, small capitals, dotless i, contraction expansion), first-person rules and signatures for self-harm, abuse, violence, sexual content, secrecy and personal contact; educational context only for third-person topics the printed question raised; model-output rules for companion persona, contact and diagnosis. The scan screens every answer and prompt before coaching: severe → no AI call for that question, one reviewed 'safety' template for the child (988, Childhelp 1-800-422-4453, 911), one escalated system report without homework text (0760); abuse/sexual/secrecy reports are held from the family until an owner releases them; model coaching, practice intros and rubric labels that screen severe fall back. Lead: stale feedback filtered after a correction (BUG-087), readiness blocks on unapproved templates and missing provider moderation, a 'Get help' button on the flagged question | `packages/domain/src/safety` 460 tests (corpus; 71 pass-2 cases failed before; bank false positives 0 severe in 19,149 samples); `safety_screening.test.ts` (DB 12); `safety-screening.test.ts` (API 21; mutation-checked); PrivacyControlsPage and result-view tests; domain 2756, DB 243, API 716, mobile 395, web 405 on the combined tree | English plus a few Spanish phrases only; fiction and quotes escalate (a person reviews); provider moderation blocked (no OpenAI key); templates, family hold and escalation steps are drafts (Owner action #24) |
| BUG-085 | Medium / P4 deletion (purge blocked by a report) | closed | found by the child-safety fixer (pre-existing since 0600) | Expected: deleting a child purges its data. Actual: `app.purge_family_data` deletes child_feedback before safety_reports, and `safety_reports.feedback_id` had no ON DELETE action, so any report linked to a feedback row (a child's own report from a hint) rolled the whole purge back | — | 0760 recreates the foreign key with ON DELETE SET NULL; the report is deleted with the child's other data | `safety_screening.test.ts` '[BUG] a report linked to a feedback row does not block purging that child' (repro on the old schema failed with the FK violation) | — |
| BUG-086 | Low / AC_SECURITY_02 honesty | closed | child-safety fixer | Expected: a report's source is shown truthfully. Actual: the parent privacy page labelled every non-child report 'Reported by a parent' | — | Exhaustive switch; a system report reads 'Flagged by PencilLift' with honest copy from contracts (`PARENT_SAFETY_FLAG_COPY`: what the product shows, 'PencilLift sent no automatic alert') | `PrivacyControlsPage.test.tsx` (failed before) | — |
| BUG-087 | Medium / AC_GRADING_06, AC_SECURITY_02 (stale child feedback) | closed | child-safety fixer note, lead fix | Expected: after a grown-up corrects a transcription, the child sees only feedback for the corrected answer. Actual: the child detail route returned every feedback row, so a hint or safety notice written for the misread answer stayed | — | The child route keeps feedback created at or after the question's `corrected_at` (read in the existing narrowly scoped service read) | `homework.test.ts` 'after a correction the child sees only feedback written for the current transcription' (fails with the filter removed) | — |
| BUG-088 | Medium / AC_CAPTURE_02 (stored bytes unverified at scan time) | closed | capture-limits residual (BUG-078 Next), lead fix | Expected: what reaches the AI is exactly what was registered and finalized. Actual: the registered sha256 was verified nowhere, and the scan's storage reader buffered the whole object with no cap, so a replaced or corrupted object (or a misconfigured bucket) reached decoding and the AI | — | The scan compares every stored page's size and sha256 with its registration before stripping metadata; a mismatch or an object over the 15 MB page cap (refused from its declared length or while streaming, never buffered whole) ends in needs_rescan PAGE_MISMATCH and releases the allowance, with no AI call. Five test fixtures that registered placeholder sizes/hashes now register the bytes their storage returns (lead fixture updates, marked) | `scan-process.test.ts` 'stored bytes that differ from what was registered never reach the AI' (fails with the hash comparison disabled) and 'the storage reader stops at the page byte cap'; API 720/720 | — |
| BUG-089 | Low / AC_ECC_07, AC_ECC_15 (gate audit weakly guarded) | closed | coverage gap (AC_ECC_07), lead fix | Expected: the local pre-commit gate runs the same test-count audit as CI, and the audit is itself proven to fail. Actual: `scripts/verify.sh` never ran `assert-test-count.mjs`, nothing tested that the audit fails on a low count, a failed/skipped test or a missing report, and the floors trailed the real counts after this session's additions | — | verify.sh runs the audit after the tests; the audit's report directory is overridable only for its self-test; `gate-audit.test.ts` feeds synthetic reports (at floor passes; one below floor, a failure, a skip or a missing report fails); floors raised to ~95% of today's counts (domain 2600, api 680, db 230, web 385, mobile 375) | `gate-audit.test.ts` 3/3 | — |
| BUG-090 | **High** / AC_ECC_07, CLAUDE.md 'domain code takes now as input' (clock split) | closed | round-2 checkers (CI time bomb), lead fix | Expected: work enqueued inside the database is due by the application's clock. Actual: SQL functions and five API inserts took `run_after` from the database's now() while the dispatcher compares with the application clock; with the test clock pinned at 2026-09-24T15:00Z, 14 API tests failed on every run after 15:00 UTC real time (export, deletion purge, retention), and CI would have been red from then on | "Pin the test clock later" — rejected: it only moves the cliff | Migration 0780: `app.request_instant()` (stated `pencillift.request_now`, else now()) is the jobs.run_after default; the API states its clock before `request_deletion` and `inactivity_delete_family`, and passes `run_after` explicitly at every job insert; the test harness states the pinned clock in every transaction (`requestNow`); export fixtures use the test clock | The 14 failures reproduced at 16:10 UTC and pass after (export-build, scheduled, lead-jobs-ai 69/69); `request_instant.test.ts` (DB 3) | — |
| BUG-091 | **High** / AC_GRADING_03/07, P6 (rubric labels unguarded) | closed with documented residuals (round 5) | final lead review wf_2e80c3c2-fd2 / round 2 wf_7f4e9851-8cb (LJA-F1, CHK-LJA-F1-residual) / round 3 wf_f4c2e9e6-b25 (R3-RL-1..5) / round 4 wf_6cfbbb0d-fb7 (check: R4-RL-A high, a new 'describes the answer' veto re-exposed sample completions; R4-RL-B medium; unsafe third-person labels shown, R4-RL-OOA medium) / round 5 wf_d45926ba-696 (check not ok → one re-fix → recheck: one low regression R5F-RL-1 fixed by the lead) | Expected: rubric rows never state the private key or a completed sentence to copy. Actual: labels reached the child with no answer guard; after round 1, completed third-person sentences still passed | — | Rows pass the answer guard (expressions read) against protected forms of the key and every quoted or labelled example in the parent-only solution; labels must be criterion-shaped (third-person criterion verb or short noun phrase, no clause, no colon, no first person). Round 5: the describes-the-answer veto and the broad include/contain and plural e.g. vetoes removed (fail closed); example cues searched at every start so a new early cue cannot swallow a later span; the write/say cue is linear at any whitespace length; each label is screened as the model wrote it, in the imperative the child reads and in the first person, plus a short risk-topic list; joined words and CamelCase read as separate words | `rubric-feedback.test.ts`, `scan-process.test.ts` (64/64 mutations killed in round 5; the lead's R5F-RL-1 test fails with the bounded lookbehind); 12/12 round-4 veto forms protected (2/12 at e123e5c); 26/26 unsafe third-person labels dropped with a SAFETY_ code (19 shown at e123e5c); keep rate 92/95 realistic criteria unchanged | Lexical heuristic, not a parser (limits in the doc comment); cue-dense synthetic notes make the guard slow (seconds; fails closed on timeout); some unsafe paraphrases pass the list — provider moderation of criteria is the control |
| BUG-092 | **High** / AC_SECURITY_06 (spend cap open at month rollover) | closed | final lead review wf_2e80c3c2-fd2 / round 2 wf_7f4e9851-8cb (LJA-F2) | Expected: no uncapped AI outside development/test. Actual: with no budget row for the new UTC month every AI stage was admitted, and readiness only reported it | — | Outside development/test a month without an owner budget admits no stage (`SpendBudgetMissing`): scans pause with SPEND_CEILING, practice keeps bank items; Owner action #19 (set the row before 00:00 UTC on the 1st) | `spend-ceiling.test.ts`, `scan-process.test.ts` LJA-F2 (failed before) | Alert for a missing next-month row is not built |
| BUG-093 | Medium / P11 'stop paid AI for inactive profiles' | closed | final lead review wf_2e80c3c2-fd2 / round 2 wf_7f4e9851-8cb (LJA-F3, CHK-LJA-F3) | Expected: no paid AI for a downgraded (draft) child. Actual: queued scans and rechecks still ran AI after a downgrade | — | `assertActive(..., paidAi)` before every model call and before a spend hold; an initial scan ends failed_final/CHILD_NOT_ACTIVE with the allowance released; a recheck keeps its results; a severe-risk correction is still answered with no AI call | `scan-process.test.ts` downgrade cases (failed before) | A downgrade during grading discards the unverified grading (product decision recorded) |
| BUG-094 | Medium / AC_SECURITY_06 (admission estimate not an upper bound) | closed | final lead review wf_2e80c3c2-fd2 / round 2 wf_7f4e9851-8cb (LJA-F4) | Expected: an admitted stage cannot exceed the cap. Actual: the estimate was a fixed per-item token heuristic | — | `inputTokenUpperBound` counts UTF-8 bytes of instructions, schema and text plus 1,500 tokens per image; an oversized request is refused as STAGE_LIMIT before it is sent | `scan-process.test.ts` LJA-F4 cases | IMAGE_INPUT_TOKEN_BOUND unverified until a provider key exists |
| BUG-095 | Medium / AC_SECURITY_06, AC_FIN_09 (metering failure releases the hold) | closed | final lead review wf_2e80c3c2-fd2 / round 2 wf_7f4e9851-8cb (LJA-F5) | Expected: a billed stage's cost always counts. Actual: if recording usage failed, the hold was released anyway, so the cost was never counted | — | `settleSpend` records usage and removes the hold in one transaction; on failure the hold is kept at the billed amount until the next month | `scan-process.test.ts`, `learning-jobs.test.ts` LJA-F5 (failed before) | — |
| BUG-096 | Medium / AC_GRADING_06 (answers inserted into the child's transcription) | confirmed (mitigated; residual awaits an owner decision, Owner action #28) | final lead review wf_2e80c3c2-fd2 / round 2 wf_7f4e9851-8cb (LJA-F7, CHK-LJA-F7) | Expected: the child's view of the printed prompt never shows an answer the extraction model inserted. Actual: model text was shown verbatim | "Compare the prompt with the key" — rejected: printed prompts legitimately contain the key ('Which is bigger, 7 or 3?') | `withholdStatedAnswers` blanks values labelled as the answer (answer/ans/answer key/solution with a separator) and bracketed answers | `homework.test.ts` LJA-F7 cases | Unlabelled insertions ('(84)', '= 84') still show (Threat_Model T35) |
| BUG-097 | Low / AC_SECURITY_06 (hold ignored across midnight UTC) | closed | final lead review wf_2e80c3c2-fd2 / round 2 wf_7f4e9851-8cb (LJA-F8) | Expected: every live hold counts. Actual: admission counted only holds of the current month's period key | — | Every unexpired hold counts whatever its period | `spend-ceiling.test.ts` (fails with the period filter restored) | — |
| BUG-098 | Medium / P6 (re-themed word problem states its key) | closed | final lead review wf_2e80c3c2-fd2 / round 2 wf_7f4e9851-8cb (LJA-F9) | Expected: a re-themed prompt never states its answer. Actual: the bank self-check reads expressions off, so a context such as 'forty plus five' (key 45) passed | — | The re-themed prompt is guarded again with expressions on; failures keep the bank item | `learning-jobs.test.ts` LJA-F9 (fails with the guard removed) | — |
| BUG-099 | Low / AC_GRADING_06 (coaching beside a parent override) | closed | final lead review wf_2e80c3c2-fd2 / round 2 wf_7f4e9851-8cb (LJA-F10) | Expected: after a parent override the child sees no coaching written for the machine verdict. Actual: the old hints stayed | — | The child route keeps only feedback created after the override (safety notices always shown) | `homework.test.ts` LJA-F10 (failed before) | — |
| BUG-100 | Low / P11 (allowance not released after a failed release) | closed | final lead review wf_2e80c3c2-fd2 / round 2 wf_7f4e9851-8cb (LJA-F11) | Expected: an unreadable or mismatched scan never consumes allowance. Actual: if the release failed after the needs_rescan transition, a retry left it reserved | — | A retry that finds needs_rescan releases the reservation idempotently | `scan-process.test.ts` LJA-F11 (failed before) | — |
| BUG-101 | Medium / AC_DEPLOY_07, AC_ACCESS_01 (fake consent accepted in staging) | closed | final lead review wf_2e80c3c2-fd2 / round 2 wf_7f4e9851-8cb (LRD-1, R2 LRD-1) + lead (consent start) | Expected: a development-mock consent record never unlocks child data outside development/test. Actual: homework capture, child activation, the scan job and practice personalization accepted it everywhere except production; consent start wrote mock records in staging | — | `acceptsTestProviderConsent` (development/test only) at every gate; consent start refuses a mock outside development/test; a source guard checks every gate | `runtime.test.ts` LRD-1 cases, `scan-process.test.ts`, `guardians.test.ts` staging case (failed before) | — |
| BUG-102 | Medium / AC_DEPLOY_07, P10 (link check survives a URL change) | closed | final lead review wf_2e80c3c2-fd2 / round 2 wf_7f4e9851-8cb (LRD-2) | Expected: a changed merchant URL is unchecked until checked again. Actual: the old 'ok' check stayed, bypassing 0770's unchecked-availability rule; a check racing a URL change could record the old URL's result | — | 0770 trigger resets availability and the check on any URL change; the link-check route writes only if the URL is unchanged, else 409 | `serve-without-fakes.test.ts`, `admin-monetization.test.ts` race case | — |
| BUG-103 | Medium / AC_SECURITY_04 (secret scan gaps) | closed | final lead review wf_2e80c3c2-fd2 / round 2 wf_7f4e9851-8cb (LRD-3) | Expected: the scans find credential shapes that ship. Actual: Stripe test secret/restricted keys, database URLs with passwords and base64 data-URI payloads were missed | — | New rules (sk_test_, rk_test_, database URLs with inline passwords, excluding loopback, reserved hosts and placeholders) and data-URI decoding, in tracked and artifact modes | `release-gates.test.ts` LRD-3 cases | — |
| BUG-104 | Low / AC_DEPLOY_07 (malformed storage config threw) | closed | final lead review wf_2e80c3c2-fd2 / round 2 wf_7f4e9851-8cb (LRD-4) | Expected: a malformed configuration answers NOT_CONFIGURED. Actual: a bad SUPABASE_URL or short service key threw inside the Worker | — | loadConfig validates them; storage selection and buildRuntime fail closed as NOT_CONFIGURED | `runtime.test.ts` three-layer cases | — |
| BUG-105 | Medium / AC_DEPLOY_07 (bare deploy is a development Worker) | closed | final lead review wf_2e80c3c2-fd2 / round 2 wf_7f4e9851-8cb (LRD-5) | Expected: a deploy without --env never serves mocks. Actual: APP_ENV defaulted to development and wrangler.toml's top-level vars set it, so `wrangler deploy` without --env would wire every mock | — | APP_ENV is required; only env.staging/env.production set it; local dev uses the gitignored `.dev.vars` (example committed) | `runtime.test.ts` bare-deploy case | — |
| BUG-106 | Medium / AC_ACCESS (device pairing), AC_ECC_07 (a failure called a flake) | closed | lead, 4a07ac9 + working tree; seen once as `[201,201,503]` in 'three overlapping "new code" requests' | Expected: overlapping "new code" requests for one child all answer 201. Actual: one answered 503 PROVIDER_UNAVAILABLE. Reproduced deterministically: a second connection holds the child row (as the 0720 insert trigger does) and then retires the live code; the request, which had already locked that code row, was chosen as the deadlock victim (40P01 → 503) | "Test-machine load flake" — rejected: the lock-order test reproduces it on every run | Lock-order inversion: the route updated `child_pairing_codes` (locks code rows) and then inserted, whose trigger locks the child row; the trigger locks the child row and then updates the code rows. The route now takes the child row (`for no key update`) before touching any code row | `child-auth-hardening.test.ts` 'takes the child row before any code row …' (503 before the fix, 201 after; 5 repeated runs green) | Swept the other parent-locking triggers (0001 adult limit fires only on `update of status` to active and on insert; 0200 slot capacity): their writers take the family row first or never fire them; no other inversion found (L-021) |
| BUG-107 | **High** / AC_SECURITY_02, P4 (screened only after grading) | closed | final lead review wf_2e80c3c2-fd2 (RV-child-safety-5, CHK-CS-1) | Expected: a severe-risk answer is never sent to the tutor model. Actual: the deterministic screen ran in finish(), after grading and verification, so every severe answer reached the model; after the job-level fix the child route still hid the notice until results existed | "Screening the output is enough" — rejected: spec P4 requires moderation before generation | `screenBeforeGrading` runs before `grade()` in the initial scan and every recheck; a severe question gets the reviewed template, one escalated system report and no AI call; the child route always returns the latest 'safety' row whatever the scan status | `safety-screening.test.ts` (failed before), CHK-CS-1 route case | Provider moderation not wired (round 5) |
| BUG-108 | **High** / AC_SECURITY_02, T34 (held flags reachable by the household) | closed | wf_2e80c3c2-fd2 (RV-child-safety-6, RV-child-safety-8) | Expected: an abuse/sexual/secrecy flag stays hidden from the family until a reviewer releases it. Actual: a child's 'Get help' report about the flagged question was visible at once, and the reviewer's resolution note was readable by family members | — | 0760: a child report about a question with a held system report starts held (`child_report_content`); `resolution_note` removed from the authenticated column grant (service-role admin API only) | `safety_screening.test.ts` (DB), `safety-screening.test.ts` | Data API read of the child's own answer while held: Owner action #25 |
| BUG-109 | **High** / AC_SECURITY_02 (lexicon recall vs. precision oscillation) | closed with documented residuals (round 5) | wf_2e80c3c2-fd2 (RV-child-safety-1..4, 10..14) → rounds 2–4 (wf_7f4e9851-8cb, wf_f4c2e9e6-b25, wf_6cfbbb0d-fb7; CHK-CS, CHK2-CS, CHK3-CS, CHK4-CS) → round 5 wf_d45926ba-696 (check ok; screen safety-screen.v4) | Expected: first-person severe disclosures are caught, everyday and educational text is not. Actual per round: round 1 missed first-person statements with a tier-B word, contractions ('I'll end my life'), named-person threats and model-output variants; each round's precision devices (exclusion tails, Roman-numeral reading, adjective 'ill' veto, lesson guards, negation across words, quote words) then cancelled disclosures an earlier round caught (CHK2-CS-1/2, CHK3-CS-1/2/4, CHK4-CS-1/2/3/7); round-4 check: 55 of 126 canonical first-person disclosures still not severe (CHK4-CS-8) and held codes on printed prompts (CHK4-CS-4/5) | "One more exclusion fixes the false positive" — rejected: every exclusion cancelled a real disclosure somewhere (L-022) | Rounds 1–4: 1,096 domain safety tests, pinned corpora, digest per version, bank 0 severe; lead policy from round 4 favours recall (a reviewer clears false matches, BUG-110). Round 5: recall-only changes, held codes never from printed prompts, an automated no-regression sweep over every earlier probe, provider moderation. Round 5: held codes (abuse, sexual, secrecy) never come from a printed prompt (one central condition); no guard holds a phrase that names the child; round-4 holders, quote words, wrist accident tails and the body-safety guard removed; negation reaches only across "to" and adverbs; "try to" plans; 36 plain first-person rules for the canonical misses | `packages/domain/src/safety` 1,314 tests (CHILD_SEVERE_PASS7 pins all 126 canonical disclosures, 55 missed at e123e5c); no-regression sweep over 2,188 earlier probe inputs: 0 lost first-person catches, 0 held codes on 220 printed prompts (23 at e123e5c), bank 0 severe in 16,478 cases, everyday/prompt corpus 0 severe; 11 mutation classes each caught | Recall-first cost: more reviewer-cleared matches (homework frustration, a child's own body-safety lesson; Owner action #27); endorsement phrases that do not name the child are still held by the lesson guard (CHK5-CS-R1); conditional abuse reports with "if" are cancelled (CHK5-CS-R2); paraphrases outside the lists are missed — provider moderation is the control (BUG-111) |
| BUG-110 | Medium / AC_SECURITY_02, P4 'human review procedures' (no way to clear a false match) | closed | round 3 wf_f4c2e9e6-b25 (CHK2-CS-5), round 4 (CHK3-CS-8) | Expected: a reviewer can clear a wrong word match so the question is graded. Actual: a system report could only be resolved; the question stayed ungraded with the safety notice; after round 3 a partial clearance left a notice beside hints | — | 0760 `resolution` ('false_match') with a shape check; admin PATCH clears and, for a visible report, the recheck grades the question; a held report cleared as a false match is never released; the child sees no hints while any uncleared system report on the question remains | `safety_screening.test.ts`, `safety-screening.test.ts` clearing cases | Runbook 5.1 procedure unapproved (Owner action #24) |
| BUG-111 | **High** / AC_SECURITY_02, spec P4 'moderation before and after generation' (no provider moderation) | closed (software); live use blocked_external | round-4 check CHK4-CS-8 (a word list missed 55 of 126 canonical disclosures) → lead decision; built in round 5 wf_d45926ba-696 (check ok) | Expected: provider moderation before generation on the child's words and after generation on child-facing output. Actual: only the deterministic word list ran; readiness said 'not wired' | "More word-list rounds will close the gap" — rejected after four oscillating rounds (L-022) | `@pencillift/ai` moderation.ts (OpenAI omni-moderation transport, batching, timeout, category mapping; labeled mock flags only an explicit marker; a refusing client without a key). The scan sends the child's answers (never the printed prompt) before grading and coaching and rubric criteria after generation; learning jobs moderate re-themed stories and intros. A provider violence flag on the child's words is held as abuse+violence. Fails closed: an error means no model call (retryable → retry; final → MODERATION_NOT_AVAILABLE). Same ZDR/consent gate as grading; readiness `ai_moderation` ready only with a real client | `provider-moderation.test.ts` 25 (24 fail at e123e5c), `moderation.test.ts` 31; 13 API and 10 unit mutants killed | Live endpoint never called (no key, no egress); batch cap 32 and 10 s timeout unmeasured; the outage rule (held) needs owner confirmation (Owner action #26) |
| BUG-112 | Low / AC_ECC_07, audit integrity (screen version reused for different rules) | closed | lead review of round 5 | Expected: a stored report's `screen_version` names exactly one rule set; the digest test says 'never edit an existing line'. Actual: round 5 changed the rules and re-pinned `safety-screen.v3`, which was already committed in 441eb6a with another digest | — | Version bumped to `safety-screen.v4`; the committed v3 digest restored beside it | `screen.test.ts` digest test (v3 and v4 pinned) | — |
| BUG-113 | **High** / AC_BILLING (every real store event's billing period dropped), P17 (school-contribution months never counted) | closed | lead scoping of the Amazon channel; fixed in brand-platforms wf_b0522f4a-5e0 (amazon-stores) | Expected: a RevenueCat webhook event maps its `store` to a billing channel. Actual: the maps in providers/billing.ts and services/billing-sync.ts had lowercase keys (the REST subscriber vocabulary) while RevenueCat webhooks send `APP_STORE`, `PLAY_STORE`, `AMAZON`, `STRIPE`, `PROMOTIONAL`; `mapRevenueCatEventToPeriod` returned null for every real event, so no billing periods, no P17 donation months and no revenue rows were recorded while capacity still arrived through the subscriber fetch. Never seen because only the labeled mock (lowercase fixtures) had ever run | — | One normalizing function maps both spellings case-insensitively and adds `amazon` → `amazon_appstore`; the refund path includes Amazon | `webhooks.test.ts` 'records the billing period for an event whose store is spelled APP_STORE' (red first), billing tests | The webhook vocabulary is corroborated by the installed SDK typings only (RevenueCat docs unreachable from this environment); the first sandbox webhook must be checked against it (Owner action #4) |
| BUG-114 | **High** / AC_PRIVACY deletion purge (a purge that silently stopped pseudonymising usage rows and deleting the child's jobs and exports) | closed | lead, isolated worktree gate on the first ops slice (4 DB tests red: jobs_hardening RV-lead-jobs-ai-17/18, lead-jobs-ai.review, parent_flag_review job purge) | Expected: migration 0820 adds two support-case deletes to `app.purge_family_data` and changes nothing else. Actual: the lead generated 0820 from the 0620 text, but 0710 had already re-created the function with the jobs/usage/export handling; the truncated `grep … \| head -5` hid 0710, so 0820 would have reverted it in every environment that applied it | "0820 is additive" — it was a full `create or replace` from stale text | 0820 regenerated from the latest definition (0710) with the same two deletes; the header names the source migration | The four tests green again; `support_cases_purge.test.ts` 2/2 (red without 0820) | The gate is the only thing that caught it; L-026 |
| BUG-115 | Medium / AC_ECC_07 (a green suite that turns red with the calendar) | closed | store-gap checkers (`wf_b0de02d8-ca2`) saw `scheduled.test.ts` 'a 30-day-old upload that never finished is closed…' fail at 929/930 with the dispatcher at HEAD | Expected: the scan-retention sweep test is independent of real time. Actual: `apps/api/tests/helpers.ts` pins the request clock at 2026-09-24T15:00Z while the fixture's `source_pages.created_at` is the database default now(); the sweep compares created_at with clock − 30 days, so from 2026-09-25T15:00Z real time the 31-day jump lands under 30 days and the row is not swept | "the account-close change broke the sweep" — it fails identically with dispatcher.ts at HEAD | The fixture stamps `source_pages.created_at` and `assignments.created_at` with the pinned clock (the API's own writes use the request instant; only fixture rows inserted through database defaults drift) | `scheduled.test.ts` 36/36 at 2026-09-25T15:30Z; the same test failed before the change | Any other fixture that relies on a database default timestamp beside the pinned clock is a latent copy (L-027) |
| BUG-116 | Medium / AC_SECURITY (input validation), every create/update route | closed | hardening bug hunt `wf_2a9a8aa6-7c4` round 1 (confirmed by three lenses), fixed in `wf_8b66cd30-e71` (API-AUTH-R1-01) | Expected: unstorable text answers 400. Actual: U+0000 in any free-text field (family name, nickname, override reason, correction text, subject name, notes, study text, practice answer, safety note, support subject/message, device label) reached Postgres (SQLSTATE 22021) and answered an unhandled 500; only rewards had been fixed (RV-rewards-2) | "Fix each route's schema by hand" — rejected: the next free-text field would repeat it | One helper, `freeTextSchema`/`textHasControlCharacter` (`packages/contracts/src/common.ts`), refuses control characters other than tab/LF/CR in every free-text request field, staff fields included (`admin-ops.ts` reply and reference, lead); central fallback maps 22021/22P05 to VALIDATION_FAILED in `apps/api/src/errors.ts` | `apps/api/tests/input-hardening.review.test.ts` [API-AUTH-R1-01] (one test per route family, tabs/newlines allowed, unit test of the mapping; mutation: predicate → false fails 4 tests, mapping off fails the unit test); `admin-ops.test.ts` NUL and ESC in a staff reply → 400 | — |
| BUG-117 | Medium / AC_ACCESS (parent sees all their data), spec P10 | closed | hardening bug hunt `wf_2a9a8aa6-7c4` round 1 (confirmed by three lenses), fixed in `wf_8b66cd30-e71` (API-AUTH-R1-02) | Expected: a parent can reach every scan and every points entry. Actual: GET /v1/assignments and GET /v1/points/history were hard-capped at 100 rows with no cursor, so older rows became unreachable | "Raise the cap" — rejected: any cap without a cursor loses rows eventually | Keyset paging: `after=<created_at_micros>_<id>` on assignments (order created_at desc, id desc, 101-row read, additive `nextCursor`), `before=<ledger id>` on points history (BigInt-parsed, int64-bounded); web Homework and Rewards pages gain 'Show older' controls that append pages, de-duplicated by id | `apps/api/tests/list-paging.review.test.ts` (130 rows incl. shared timestamps walked in exactly 2 pages, no repeats or gaps; malformed/overflow/far-future cursors 400; mutations: limit without +1 and ignored `before=` both fail); 4 web tests | — |
| BUG-118 | Low / AC_SECURITY (admin robustness) | closed | hardening bug hunt `wf_2a9a8aa6-7c4` round 1 (confirmed by three lenses), fixed in `wf_8b66cd30-e71` (API-AUTH-R1-03) | Expected: a syntactically valid but overflowing admin cursor answers 400. Actual: cursor micros beyond int64 or an interval overflow passed the regex and answered 500 on GET /v1/admin/safety-reports and /v1/admin/support/cases | "Bound to `now`" — rejected: rows carry the database wall clock while tests pin the request clock, so genuine cursors would be refused | `cursorMicros(digits, now)` parses with BigInt, refuses values above 9223372036854775807 or more than ten years past the request clock; the same helper guards the new assignments cursor | `input-hardening.review.test.ts` [API-AUTH-R1-03] (four overflow shapes are 400 on both queues; a well-formed past cursor pages) | — |
| BUG-119 | Low / spec 'every endpoint defines a limit', AC_SECURITY | closed | hardening bug hunt `wf_2a9a8aa6-7c4` round 1 (confirmed by three lenses), fixed in `wf_8b66cd30-e71` (API-AUTH-R1-04) | Expected: every parent create route has a per-family limit and child profiles a cap. Actual: draft child profiles, rewards, subjects, test dates, study materials and schedule updates had no per-family rule; child refresh had no per-network rule; a stuck button or script could bloat a family's views; PUT /v1/adult/pin hashed with PBKDF2 on every call with no rule | — | Named rules in `apps/api/src/middleware/rate-limit.ts` (childCreatePerFamily 12/day, rewardCreatePerFamily 40/h, subjectCreatePerFamily 20/h, testDateCreatePerFamily 30/h, studyMaterialCreatePerFamily 30/h, scheduleUpdatePerFamily 30/h, childRefreshPerNetwork 240/h) applied on each route; hard cap `CHILD_PROFILE_LIMIT = 12` non-archived profiles under the family row lock (rule `CHILD_PROFILE_LIMIT`); lead follow-up `pinSetPerUser` 10/h counted only for calls that would replace the PIN | `input-hardening.review.test.ts` [API-AUTH-R1-04] (13th profile 422, room after archive, 429 with Retry-After per route); `adult-pin.test.ts` 'PIN set/change budget' (eleventh replacement 429; a weak PIN never counts) | — |
| BUG-120 | Low / COPPA framing, spec (K-8, under 13) | closed | hardening bug hunt `wf_2a9a8aa6-7c4` round 1 (confirmed by three lenses), fixed in `wf_8b66cd30-e71` (API-AUTH-R1-05); scope decision recorded as Threat_Model T38 | Expected: no child profile can be declared outside the under-13 launch scope. Actual: the contract accepted grade 9-12 and the age band 14-18, for which the child-mode, consent and AI gates are not designed (`mayIncludeUnder13('14-18')` is false) | "Narrow the database check too" — deferred: pre-launch there is no data, and a wider check lets a later expansion avoid a data rewrite; the release checklist confirms no such row exists (`Deployment_Runbook.md` §3.1) | `AGE_BANDS = ['5-7','8-10','11-13']`, `GRADE_LEVEL_MAX = 8` in `packages/contracts/src/family.ts`; POST /v1/children validates with the contract; both add-child forms build their pickers from it; the wider `packages/ai` and domain safety band lists stay as supersets | `input-hardening.review.test.ts` [API-AUTH-R1-05] (grade 9-12 and 14-18 refused, grade 8 / 11-13 accepted; mutations restoring 14-18 or max 12 fail) | — |
| BUG-121 | **High** / Apple 5.1.1, AC_ACCESS (a shared device keeps a parent signed in) | closed | hardening bug hunt `wf_2a9a8aa6-7c4` round 1 (confirmed by three lenses), fixed in `wf_8b66cd30-e71` (MOB-R1-01) | Expected: a parent can sign out of the mobile app. Actual: no sign-out control existed; the Supabase session stayed in the device keychain indefinitely | — | `signOutParent` in `apps/mobile/src/lib/mode.ts` (clear caches → server relock while the token exists → forget the in-memory unlock → `parentAuth.signOut` → clear again → mode `signed_out` → reset navigation to the welcome screen; a paired child stays paired); `SignOutButton` on the parent home and unlock screens | `apps/mobile/src/lib/mode.test.ts` 'parent sign-out (MOB-R1-01)' (ordering, offline still clears, pairing survives); `src/family/screens.test.ts` (both screens render the button; runtime wires the full path) | — |
| BUG-122 | **High** / AC_ACCESS (honest failure copy) | closed | hardening bug hunt `wf_2a9a8aa6-7c4` round 1 (confirmed by three lenses), fixed in `wf_8b66cd30-e71` (MOB-R1-02) | Expected: an offline or rate-limited sign-in says so; 'Forgot password' reports a failed request. Actual: every sign-in failure said 'That email and password did not match.' and the reset screen claimed a link was on its way even when the request failed | — | `signInErrorMessage` in `apps/mobile/src/lib/parent-auth.ts` branches on the auth-js error kind (retryable fetch/offline, 429 or rate-limit code, email not confirmed, invalid credentials, 5xx, generic); `sendPasswordReset` returns ok/message and the screen shows the failure | `apps/mobile/src/lib/parent-auth.test.ts` (real supabase-js against a labeled fake fetch: offline, 429, invalid credentials, unconfirmed email, reset offline/429/200) | — |
| BUG-123 | **High** / AC_ACCESS (screens must not hang), spec P4 'Stop sending' | closed | hardening bug hunt `wf_2a9a8aa6-7c4` round 1 (confirmed by three lenses), fixed in `wf_8b66cd30-e71` (MOB-R1-03); PUT-leg timeout by the lead | Expected: a stalled connection fails with a retry offer and 'Stop sending' stops a stalled scan POST. Actual: the API client had no timeout or abort, so child and parent screens spun forever and the stop button could not interrupt a stalled request | "Use AbortSignal.timeout" — rejected: missing on Hermes | `createApiClient(..., { timeoutMs })` with `DEFAULT_REQUEST_TIMEOUT_MS` 20 s built from AbortController + setTimeout, raced against the fetch (`packages/contracts/src/client.ts`; NETWORK errors carry rule TIMEOUT or ABORTED); `uploadScan` threads the scan signal into every API call; lead: `putBytes` in `native-io.ts` gets its own 120 s timer, and every child copy layer words a timeout as 'taking longer than usual' via `isRequestTimeout` | `packages/contracts/src/client.test.ts` 'api client timeouts and cancellation' (5 tests); `apps/mobile/src/homework/upload.test.ts` (signal passed to every call; abort reported as cancelled); copy tests in result-view, result-copy, child-view-model, child-help, support view-model | — |
| BUG-124 | Medium / AC_PERF (memory on Fire tablets) | closed | hardening bug hunt `wf_2a9a8aa6-7c4` round 1 (confirmed by three lenses), fixed in `wf_8b66cd30-e71` (MOB-R1-04) | Expected: at most one page's bytes live in JS memory. Actual: `uploadScan` read and held every page (up to 10 × 15 MB plus base64 transients) before sending any | — | First pass keeps only `{uri, mimeType, byteSize, sha256}`; `sendPage` re-reads a page just before its PUT and drops it after; a page whose size changed since fingerprinting is refused; stored pages are not re-read on resume (`apps/mobile/src/homework/upload.ts`) | `upload.test.ts` 'never holds every page's bytes at once' (max 1 live buffer, exact order), resume and changed-file cases | — |
| BUG-125 | Medium / honesty of copy (spec P4), store review | closed | hardening bug hunt `wf_2a9a8aa6-7c4` round 1 (confirmed by three lenses), fixed in `wf_8b66cd30-e71` (MOB-R1-05); web twin by the lead | Expected: the planner promises only what the app does. Actual: both planners offered 'gentle practice reminders on the child's device' and quiet hours although the app never registers for or delivers notifications | — | The Reminders card (mobile `planner.tsx`, web `ScheduleSection.tsx`) states that reminders and quiet hours are not available in this version and that no notification is sent; the stored schedule fields round-trip unchanged | `screens.test.ts` 'honest reminder copy'; `apps/web/src/pages/app/LearningPlannerPage.test.tsx` (no reminder or quiet-hours checkbox, copy present, body unchanged) | — |
| BUG-126 | Medium / AC_ACCESS (pairing recovery) | closed | hardening bug hunt `wf_2a9a8aa6-7c4` round 1 (confirmed by three lenses), fixed in `wf_8b66cd30-e71` (MOB-R1-06) | Expected: a device a parent disconnected can take a new code at once. Actual: `isPaired()` stayed true until the next refresh, so /pair said the device was connected and refused a new code | — | The connected branch of `pair.tsx` offers 'Use a different code', which awaits `childSession.logout()` (best-effort POST /v1/child/logout, then unpair) and shows the code form; no second token refresher (L-007) | `screens.test.ts` 'a stale pairing can always be replaced' (wiring; the file contains no refresh code) | — |
| BUG-127 | Medium / AC_ACCESS (child navigation), spec P4 (captured pages) | closed | hardening bug hunt `wf_2a9a8aa6-7c4` round 1 (confirmed by three lenses), fixed in `wf_8b66cd30-e71` (MOB-R1-07) | Expected: every child screen has a way home and the hardware back button never discards captured pages. Actual: child screens hid the header with no on-screen back or home, and Back while the camera was open dropped every page | — | `ChildNav` (Home + optional Back, 44 pt targets) composed by `Screen childNav=` or rendered under the brand row on every child screen; `scan.tsx` handles `hardwareBackPress` while the camera is open by closing the camera and keeping the pages | `screens.test.ts` 'a way home from every child screen' (4 tests); `scan-screen.test.ts` 'the camera and the hardware Back button' (2 tests); `navigation.test.ts` unchanged | — |
| BUG-128 | Medium / AC_ACCESS (signed-out parent dead end) | closed | hardening bug hunt `wf_2a9a8aa6-7c4` round 1 (confirmed by three lenses), fixed in `wf_8b66cd30-e71` (MOB-R1-08) | Expected: a signed-out parent on a configured build is offered the sign-in. Actual: the unlock screen and every gated parent screen said 'Parent sign-in isn't connected on this device yet' with no route to sign in | — | `useParentAccess` yields `no_session` only when sign-in is configured and no token source exists, else `not_configured`; `SignInPrompt` replaces to the sign-in screen, which returns to the unlock | `screens.test.ts` 'a signed-out parent is offered the sign-in' (2 tests) | — |
| BUG-129 | Low / spec P3 (PIN on every entry into the parent area) | closed | hardening bug hunt `wf_2a9a8aa6-7c4` round 1 (confirmed by three lenses), fixed in `wf_8b66cd30-e71` (MOB-R1-09); privacy screen gated by the lead | Expected: after a restart the parent area needs the PIN before any parent screen loads data. Actual: mode `parent` persisted across restarts, so a deep link opened parent read screens with the stored Supabase session and no PIN; the privacy screen bypassed the shared gate entirely | "Persist the unlock window" — rejected: it would survive the restart the finding is about | In-memory (never persisted) `parentUnlockedUntilMs` recorded from the server's `unlockedUntil`, required by `useParentAccess` (a locked device is sent to the unlock screen), cleared on child mode, unpair, sign-out and naturally on a cold start; the welcome screen enters child mode explicitly; lead: `(parent)/privacy.tsx` goes through `useParentAccess` and loads nothing before it opens. Known limit: the window lapses at the server TTL (300 s) even in the foreground and re-entry lands on the parent home | `mode.test.ts` 'the client-side unlock lives in memory only' (4 tests); `screens.test.ts` 'parent screens need the PIN after a restart' (gate, unlock window, welcome, privacy screen) | — |
| BUG-130 | Low / Amazon Appstore copy (Fire tablet builds) | closed | hardening bug hunt `wf_2a9a8aa6-7c4` round 1 (confirmed by three lenses), fixed in `wf_8b66cd30-e71` (MOB-R1-10); retention twin by the lead | Expected: the deletion warning names this build's store. Actual: it said 'App Store or Google Play' on every build, the retention lines too | — | `deletionStoreWarning()` builds the warning from `storeChannelForBuild()` and `STORE_LABEL` (a build for no store names all three); `PRIVACY_RETENTION_LINES` names all three stores | `screens.test.ts` 'the deletion warning names this build's store' (2 tests); `parent-privacy.test.ts` retention line | — |
| BUG-131 | Medium / AC_BILLING (owner revenue view), P17 revenue definition | closed | hardening bug hunt `wf_2a9a8aa6-7c4` round 1 (confirmed by three lenses), fixed in `wf_81f5d47c-d55` (BILL-R1-1) | Expected: a lost Stripe dispute reduces net revenue like a refund. Actual: `applyRefund` set `refunded_cents` only for kind 'refund'; the dispute handler passed null, so a charged-back period kept `refunded_cents` 0 and the owner revenue view (and the admin case detail) showed the money as kept, with the store-fee estimate charged on it | — | `applyRefund` treats a chargeback as a refund of the dispute amount or the whole charge (`greatest(refunded_cents, coalesce(amount, charged_amount_cents))`), the Stripe webhook passes the dispute's `amount`, the revenue definition note says so. The checker then found a regression of the first fix (a WON dispute left the amount refunded, so net revenue stayed short and the $1 donation was never reinstated); re-fix: a first chargeback adds the disputed amount on top of earlier refunds, capped at the charge, and a won dispute subtracts it, leaving `partially_refunded` when a genuine partial refund remains, else `settled` | `apps/api/tests/billing-hardening.review.test.ts` BILL-R1-1 (dispute → refunded and net −charge; direct chargeback null/partial/repeat; parked chargeback replay; won dispute gives the amount back and reinstates the donation; lead: partial refund 1000 → dispute 3998 → won 3998 through the webhook stays `partially_refunded`/1000, which fails when the webhook passes no amount) | — |
| BUG-132 | **High** / AC_BILLING (a re-enabled subscription lapses at period end) | closed | hardening bug hunt `wf_2a9a8aa6-7c4` round 1 (confirmed by three lenses), fixed in `wf_81f5d47c-d55` (BILL-R1-2) | Expected: turning auto-renew back on is observed at the next sync. Actual: the RevenueCat mapping derived the ordering instant from the newest marker (unsubscribe_detected_at); once RevenueCat cleared it, the next snapshot ordered older than the stored row and the ledger discarded it as stale, so the family stayed cancelled_active and lost access at period end | "Order by the fetch instant always" — rejected: replayed webhook events must keep their own timestamps | `applySnapshots` orders a snapshot from a LATER complete fetch whose material state differs from the stored row by its fetch instant (`orderCompleteFetch`); same-instant fetches keep the fail-closed tie rule, earlier fetches stay ignored, identical state only refreshes; the domain ledger is untouched. Checker follow-up: a later IDENTICAL fetch still carried the older provider instant and was discarded, so `fetched_at` never advanced and the sweep re-fetched the family every tick; it is now ordered by the stored observation so it records a refresh | `billing-hardening.review.test.ts` BILL-R1-2 (cancel → uncancel through the webhook route returns to active with auto-renew on; capacity kept; a later identical fetch advances `fetched_at`) | — |
| BUG-133 | Medium / AC_BILLING (webhook-loss safety net starves) | closed | hardening bug hunt `wf_2a9a8aa6-7c4` round 1 (confirmed by three lenses), fixed in `wf_81f5d47c-d55` (BILL-R1-3) | Expected: the stale-entitlement sweep reaches every family whose renewal webhook was lost. Actual: refunded families matched `period_end < now` forever, and with `order by f.id limit 25` the same 25 were re-fetched every tick while later families were never re-verified | — | The sweep excludes every terminal status (`TERMINAL_STATUSES` exported), requires `fetched_at` older than one hour for the past-period clause and orders by the oldest `fetched_at`, so the batch rotates | `billing-hardening.review.test.ts` BILL-R1-3 (13 refunded + 26 stale + a lost renewal behind them: tick 1 fetches exactly 25 without it, tick 2 reaches it, refunded rows never fetched) | — |
| BUG-134 | Medium / AC_BILLING (family transfers), candidate pending owner confirmation (Owner action #39) | closed (software); docs check blocked_external | hardening bug hunt `wf_2a9a8aa6-7c4` round 1 (confirmed by three lenses), fixed in `wf_81f5d47c-d55` (BILL-R1-4); the RevenueCat docs are unreachable from the build network | Expected: a RevenueCat TRANSFER event, whose documented sample carries `transferred_from`/`transferred_to` and no `app_user_id`, is processed. Actual: the body schema required `app_user_id` on every type, so a real TRANSFER would have answered 400 and left both families as they were, unrecorded | — | The body schema is a union: TRANSFER requires non-empty `transferred_from` and `transferred_to` with `app_user_id` optional; every other type keeps `app_user_id` required. A JSON body the schema refuses leaves one trace in `billing_provider_events` (status ignored, error code UNEXPECTED_SHAPE, id/type/digest only) before the 400; `docs/Provider_Capability_Matrix.md` §2b records the candidate field list | `billing-hardening.review.test.ts` BILL-R1-4 (the documented TRANSFER body re-verifies both families; a refused shape is traced) | Owner action #39: confirm the field list against the docs and send one sandbox TRANSFER to staging |
| BUG-135 | Low / AC_BILLING, P17 donation rule (currency mix) | closed (software); market decision with the owner (Owner action #38) | hardening bug hunt `wf_2a9a8aa6-7c4` round 1 (confirmed by three lenses), fixed in `wf_81f5d47c-d55` (BILL-R1-5) | Expected: a charge in another currency is never compared with the USD price table or summed as USD cents. Actual: `chargedCents` held minor units of whatever currency the store reported; revenue summed currencies together, the donation rule compared £39.99 with $39.99, promotions filled discount fields from it | "Model every currency now" — deferred: the launch market is the United States (spec); per-storefront price tables are a product decision | `REVENUE_CURRENCY = 'USD'`: a non-USD period is stored with its currency and audited once (`billing.unexpected_currency`, no amounts), revenue sums cover USD periods only and the notes name the excluded count per channel; lead: `BillingPeriodFact.currency`, donation rule `usd_currency` → `NOT_USD_CURRENCY` (rules version v2) and the accrual prefilter `p.currency = 'USD'` | `billing-hardening.review.test.ts` BILL-R1-5 (GBP renewal stored, audited, excluded with a note; USD-only shows none); `packages/domain/src/donations/eligibility.test.ts` (GBP full price → NOT_USD_CURRENCY; no currency = USD) | Owner action #38 |
| BUG-136 | Medium / COPPA consent withdrawal (spec P3), AC_SECURITY_05 | closed | hardening bug hunt `wf_2a9a8aa6-7c4` round 1 (confirmed by three lenses), fixed in `wf_81f5d47c-d55` (CS-R1-01) | Expected: withdrawing consent ends the child's own use of PencilLift. Actual: POST /v1/consent/withdraw only marked the record and cancelled queued jobs; child sessions stayed valid, new devices could pair, and practice sets kept being generated from the child's evidence | "Move children to draft" — rejected: releasing paid slots is a billing decision, not a consent one | The withdrawal transaction revokes every child session (reason consent_withdrawn) and device with the archive path's statements and retires unredeemed pairing codes; `consentAllowsChildAccess` (services/consent.ts) blocks pairing-code creation, /child/pair, /child/refresh (401 without rotating the token) and practice enqueue whenever the family's latest consent record exists and is not verified. Documented residual: a family with no record at all is not blocked (unreachable in production because activation requires verified consent; the strict `hasVerifiedConsent` gate would need the DB fixture to seed a verified mock record) | `apps/api/tests/consent-withdrawal.review.test.ts` (6 tests); `guardians.test.ts`; `homework.test.ts` consent gate (child paired before withdrawal, pairing-code 422 after) | — |
| BUG-137 | Medium / AC_SECURITY_02 (a filed safety flag never reaches the parent) | closed | hardening bug hunt `wf_2a9a8aa6-7c4` round 1 (confirmed by three lenses), fixed in `wf_81f5d47c-d55` (CS-R1-02) | Expected: a safety-flag email already queued is sent even if consent is withdrawn first. Actual: the withdrawal cancelled every queued job not on the keep-list, which predated the parent-recipient decision and lacked `safety_flag_email`; a cancelled job never dead-letters, so the report stayed not_sent forever | — | `safety_flag_email` added to `JOBS_KEPT_ON_CONSENT_WITHDRAWAL` with a comment naming the owner decision of 2026-09-25 (the payload is a report id only) | `guardians.test.ts` (a queued safety_flag_email survives withdrawal); `consent-withdrawal.review.test.ts` | — |
| BUG-138 | Low / honesty of the public privacy copy (COPPA notice) | closed | hardening bug hunt `wf_2a9a8aa6-7c4` round 1 (confirmed by three lenses), fixed in `wf_81f5d47c-d55` (CS-R1-03) | Expected: the privacy page promises only controls that exist. Actual: it said parents can delete homework photos sooner than 30 days and listed 'Delete homework photos' as a separate right; the only early removals are cancelling an unprocessed scan and deleting the child's data or the account | "Build a photo-only delete control" — deferred: retention is 30 days and the two existing paths cover the need; revisit if parents ask | The photo bullet names the three real removal paths; the separate right is removed; DraftOnly markers untouched | `apps/web/src/pages/public/LegalPages.test.tsx` 'accurately describes the implemented privacy design' | — |
| BUG-139 | Medium / AC_PERF (job ledger grows without bound, hot child queries scan it) | closed | hardening bug hunt `wf_2a9a8aa6-7c4` round 1 (confirmed by three lenses), fixed in `wf_81f5d47c-d55` (DB-R1-01); migration 0840 | Expected: per-request queries on `public.jobs` use an index and terminal rows are pruned. Actual: only the partial ready index existed; child practice/review status reads, scan-version counts, consent cancellation and the purge scanned every job of every family, and nothing ever deleted terminal jobs | — | Indexes `jobs_family_child (family_id, child_id, kind, created_at desc)` and `jobs_running (locked_until) where status = 'running'`; `app.prune_terminal_jobs(interval)` (SECURITY DEFINER, service role only) deletes succeeded/cancelled/dead_letter jobs older than the interval except deletion_purge and account_close; lead: `job_retention` tick step at 90 days (`JOB_RETENTION_DAYS`, kept above the 30-day raw scan retention). The database checker then found the retention step could break transcription corrections: `homework.ts` derived the next `scan:<id>:vN` key from a COUNT of kept jobs, so once v1 was pruned and v2 kept the route re-derived v2 and answered 409; both scan routes now take the highest kept version + 1 | `supabase/tests/hardening_r1_db.test.ts` [DB-R1-01] (index definitions; EXPLAIN of the hot queries with bulk rows in other families; prune keeps the two audit kinds); `apps/api/tests/job-retention.review.test.ts` (tick prunes old terminal rows, keeps young, non-terminal and audit rows); `homework.test.ts` 'a correction after the first version was pruned gets a fresh key' (fails with the count) | — |
| BUG-140 | Medium / AC_PRIVACY deletion flow (owner stuck until the purge runs) | closed | hardening bug hunt `wf_2a9a8aa6-7c4` round 1 (confirmed by three lenses), fixed in `wf_81f5d47c-d55` (DB-R1-02); migration 0840 | Expected: after requesting family deletion a parent can start a new family at once. Actual: memberships stayed active on the tombstoned family until the purge job ran, so `create_family` refused ('adult already belongs to a family'); a dead-lettered purge left the parent stuck indefinitely | "Make create_family ignore tombstoned families" — rejected: the unique index and RLS helpers all key on status; releasing at request time is the single change | `public.request_deletion` (family scope) and `app.inactivity_delete_family` revoke the family's active memberships in the tombstone transaction (revoked_at = deletion_requested_at), regenerated from their latest 0710 definitions (L-026); the purge's later revoke is a no-op; lead: the deletion view, account closure and the account-close job treat a membership the deletion itself released like an active one while that deletion is open. The database checker then found an owner who deleted family A and started family B could close their account while B was live (the first owner membership, A, was taken); account closure now takes a live owned family first and refuses with FAMILY_DELETION_REQUIRED | `hardening_r1_db.test.ts` [DB-R1-02] (request → membership revoked at the request instant, create_family succeeds before the purge, purge completes); `privacy.test.ts`, `privacy.review.test.ts` [RV-privacy-5], `account-close.test.ts` owner path and 'a live second family blocks the closure' (purge pending and completed; the pending case fails without the fix) | — |
| BUG-141 | Low / scheduling robustness (pseudo-zones through the Data API) | closed | hardening bug hunt `wf_2a9a8aa6-7c4` round 1 (confirmed by three lenses), fixed in `wf_81f5d47c-d55` (DB-R1-03); migration 0840 | Expected: the family time-zone guard accepts only IANA zones the scheduler can use. Actual: `pg_timezone_names` lists 'localtime', 'Factory' and 'posixrules' (and posix/*, right/* where shipped), which the guard accepted and `Intl.DateTimeFormat` rejects, so a direct PostgREST update could break the family's daily and Thursday scheduling | — | The pseudo-zones are deleted from `private.known_time_zones` and refused by `app.families_require_iana_timezone()` on the live fallback too (regenerated from the latest 0720 definition) | `hardening_r1_db.test.ts` [DB-R1-03] (each pseudo-zone refused for an unlocked member's direct update; real zones kept) | — |
| BUG-142 | Low / AC_PERF (safety-report list scans every family) | closed | hardening bug hunt `wf_2a9a8aa6-7c4` round 1 (confirmed by three lenses), fixed in `wf_81f5d47c-d55` (DB-R1-04); migration 0840 | Expected: the parent's safety-report list reads one family's rows by index. Actual: `safety_reports` had only the open-status partial index and the system-report unique index, so the privacy screen's list was a full scan plus a top-N sort | — | Index `safety_reports_family (family_id, created_at desc)`, which also serves the child purge's family/child delete | `hardening_r1_db.test.ts` [DB-R1-04] (with 3,000 reports in each of two families the list query plans on the index, no Seq Scan) | — |
| BUG-143 | **High** / guardian invitations (spec P2), AC_ACCESS | closed | hardening bug hunt `wf_2a9a8aa6-7c4` round 1 (three-lens verification), fixed in `wf_90c8faf6-975` (WEB-R1-01) | Expected: an invited adult who must sign in first still accepts the invitation. Actual: the sign-in prompt built its return path from pathname and search only, so `/app/guardians#accept=<token>` lost the one-time token through password sign-in, magic link and sign-up | — | The return path keeps the fragment (still same-origin through `safeNextPath`); sign-up and the magic link honour `next` for their email redirects; the invitation page strips the hash once captured. Owner action #40: the Supabase redirect allow-list must match portal paths | `apps/web/src/pages/app/GuardiansPage.invite.test.tsx` (4 tests end to end from a signed-out invitation link) | Owner action #40 |
| BUG-144 | Medium / AC_UX_02 meaningful states, spec P14 | closed | hardening bug hunt `wf_2a9a8aa6-7c4` round 1 (three-lens verification), fixed in `wf_90c8faf6-975` (WEB-R1-02) | Expected: a stale page chunk after a deploy or a render error keeps the portal usable. Actual: no route error boundary, so React Router replaced the whole app with its unstyled 'Unexpected Application Error!' page and raw error text | — | A branded `RouteError` boundary under the shell (brand bar, nav and footer stay) with Reload and 'Go to the family dashboard', never raw error text; a chunk-load failure reloads once, guarded by a session marker (no loop); a shell-level fallback keeps the brand bar | `apps/web/src/components/RouteError.test.tsx` (5 tests). Residual: no test pins that the nav survives when only the inner boundary is removed | — |
| BUG-145 | Medium / WCAG 2.2 AA contrast (spec accessibility) | closed (web in round 2b, mobile in round 2c as BUG-163) | hardening bug hunt `wf_2a9a8aa6-7c4` round 1 (three-lens verification), fixed in `wf_90c8faf6-975` (WEB-R1-03) | Expected: white button text at least 4.5:1. Actual: `.btn` drew white 16 px bold text on #008D87 at 4.07:1 on every primary action | — | `.btn` fill and border use `--teal-text` (#00706B, 5.95:1); `--teal` stays for focus rings and non-text accents | `apps/web/src/styles.test.ts` (computes the ratio from styles.css with the ui-tokens contrast helper; failed at 4.07) | — (mobile fixed as BUG-163) |
| BUG-146 | Medium / Amazon Appstore channel in the signed-in portal | closed | hardening bug hunt `wf_2a9a8aa6-7c4` round 1 (three-lens verification), fixed in `wf_90c8faf6-975` (WEB-R1-04) | Expected: Fire-tablet families see their store. Actual: the portal's price table, purchase and cancel wording, promo store options and privacy notices named only the App Store and Google Play | — | Store names, columns and options come from `channelSchema.options` (`components/stores.ts`); notices say 'the store that bills you (App Store, Google Play or Amazon Appstore)' | `apps/web/src/pages/app/StoreChannels.test.tsx`; tightened `PrivacyControlsPage.test.tsx` and `SubscriptionPage.test.tsx` assertions (the old ones encoded the missing column; comments name WEB-R1-04) | — |
| BUG-147 | Medium / honesty of copy (P17 promotions) | closed | hardening bug hunt `wf_2a9a8aa6-7c4` round 1 (three-lens verification), fixed in `wf_90c8faf6-975` (WEB-R1-06) | Expected: the portal never invites a parent to redeem a code no path can redeem. Actual: the Subscription page sent parents to enter a monthly promo code and the promotions page offered 'PencilLift web billing', while every path ended in 'not available yet' | — | The promotions page says up front that codes are previewed here and redeemed in the app once that step exists; the Subscription invitation is gone; the web-billing option shows only when web billing is enabled (the redemption-safety tests still run with it enabled) | `StoreChannels.test.tsx`; `SchoolAndPromotionsPage.test.tsx` and `.review.test.tsx` render with `webBillingEnabled` (comments name WEB-R1-06) | Flip `WEB_BILLING_ENABLED` with the API flag |
| BUG-148 | Medium / AC_ACCESS (email links opened elsewhere fail silently) | closed (a mobile-started reset finishes on the web since round 2c, BUG-162) | hardening bug hunt `wf_2a9a8aa6-7c4` round 1 (three-lens verification), fixed in `wf_90c8faf6-975` (WEB-R1-07) | Expected: a verification, magic-link or reset email opened in another browser explains what happened. Actual: PKCE links opened elsewhere left `?code=` unexchanged with no error, and error parameters were never read; the parent landed on a sign-in prompt or a bare reset page | "Switch to the implicit flow" — rejected: PKCE stays | `AuthLinkNotice` reads error, error_code and error_description from query and hash, treats an unexchanged `?code=` or a hash token as a failed link, and tells a signed-out adult to open the link in the browser that requested it or request a new one (on /app, every portal page, /sign-in and /update-password) | `apps/web/src/components/AuthLinkNotice.test.tsx` (9 tests) and page tests | — (BUG-162) |
| BUG-149 | Low / public support copy | closed | hardening bug hunt `wf_2a9a8aa6-7c4` round 1 (three-lens verification), fixed in `wf_90c8faf6-975` (WEB-R1-08) | Expected: the FAQ describes the self-serve PIN reset. Actual: it told parents to email support | — | FAQ: sign in, Security, Reset your parent PIN, confirm the account password; support only if sign-in fails | `apps/web/src/pages/public/LaunchCopy.test.tsx` | — |
| BUG-150 | Low / admin support policy form | closed | hardening bug hunt `wf_2a9a8aa6-7c4` round 1 (three-lens verification), fixed in `wf_90c8faf6-975` (WEB-R1-09) | Expected: the owner's Save policy button is styled and 44 px. Actual: `className="primary"` (no such class) rendered a bare browser button | — | `className="btn"` | `SupportAdminPage.test.tsx` 'Support policy form styling' | — |
| BUG-151 | Low / layout of confirmation checkboxes | closed | hardening bug hunt `wf_2a9a8aa6-7c4` round 1 (three-lens verification), fixed in `wf_90c8faf6-975` (WEB-R1-10) | Expected: checkboxes at natural size. Actual: three checkboxes (account-close confirmation, payout recipient verified, partial refunds) inherited `width:100%; min-height:44px` | — | One global rule for checkbox and radio inputs; redundant inline overrides removed in the fixer's files | `styles.test.ts` (checkbox/radio rule; text-input rule unchanged) | Optional cleanup of four inline overrides elsewhere |
| BUG-152 | Low / store review (support URL says the app cannot be downloaded) | closed (software); flag set by the owner | hardening bug hunt `wf_2a9a8aa6-7c4` round 1 (three-lens verification), fixed in `wf_90c8faf6-975` (WEB-R1-11) | Expected: pre-launch notices disappear for the review build. Actual: Landing, Pricing and Terms hard-coded 'not yet available' copy with no switch | — | `VITE_STORE_LIVE` (exactly `true`, like `VITE_LEGAL_REVIEWED`) hides the notices; runbook §3.5 and the store checklist row name it | `LaunchCopy.test.tsx` (both states per page); `apps/web/src/lib/config.test.ts` | Set for the review build (Release_Readiness store checklist) |
| BUG-153 | Low / referential integrity (Data API path) | closed | hardening bug hunt round 1 (DB-R1-05), fixed in `wf_90c8faf6-975`; migration 0850 | Expected: a test date, study material or scan can only name a subject of the same child. Actual: the keys bound subjects to the family, so a Data-API insert could attach child B's subject to child A's rows | — | `unique (id, child_id)` on child_subjects and child-bound foreign keys on the three tables; the migration refuses to apply (naming the counts) if a cross-child row already exists (runbook §3.1) | `supabase/tests/hardening_r1_db2.test.ts` [DB-R1-05] | — |
| BUG-154 | Low / dead ON DELETE actions | closed | hardening bug hunt round 1 (DB-R1-06), fixed in `wf_90c8faf6-975`; migration 0850 | Expected: foreign-key actions that can succeed. Actual: support case and message authors were `on delete set null` while the parent-author checks require them, so the action could only fail with a confusing check violation | — | Plain references (NO ACTION) with column comments: account closure is a soft delete (0830) and the id stays as a pseudonym | `hardening_r1_db2.test.ts` [DB-R1-06] | — |
| BUG-155 | Low / AC_PERF (session tables never pruned) | closed | hardening bug hunt round 1 (DB-R1-07), fixed in `wf_90c8faf6-975`; migration 0850 | Expected: ended child sessions, refresh tokens and adult step-ups are pruned and indexed. Actual: every refresh, pairing and unlock added rows nobody deleted, and the purge scanned refresh tokens without an index | — | Indexes `child_refresh_tokens_session` and `child_sessions_family`; `app.prune_session_rows(interval)` (service only, database clock, a live session keeps every token); lead: wired into identity housekeeping at 30 days | `hardening_r1_db2.test.ts` [DB-R1-07]; `apps/api/tests/auth-session.test.ts` 'prunes child sessions that ended over 30 days ago… never a live one' | — |
| BUG-156 | **High** / AC_PRIVACY deletion (spec P4, E4), Apple 5.1.1(v) | closed | hardening bug hunt round 1 (JOBS-R1-01), fixed in `wf_90c8faf6-975` | Expected: a family deletion and the account closure behind it always finish. Actual: five failed purge attempts (about 25 minutes of storage trouble) dead-lettered the purge for good; the request stayed open, the data stayed, and the owner's closure deferred forever | — | `deletion_purge` and `account_close` re-queue a successor `<key>:retry<n>` six hours after a dead letter, with an error-level log code and an audit row (no personal data); lead: account closure counts a retry successor as live | `apps/api/tests/jobs-hardening.review.test.ts` JOBS-R1-01 (dead letter, successor, later healthy tick completes the purge and the request) | Alert on DELETION_PURGE_DEAD_LETTER / ACCOUNT_CLOSE_DEAD_LETTER (runbook §4) |
| BUG-157 | Medium / scan reliability (Worker memory, provider payload) | closed (server and mobile in round 2b; web client in round 2c, BUG-160) | hardening bug hunt round 1 (JOBS-R1-02), fixed in `wf_90c8faf6-975` | Expected: a multi-page scan fits the Worker. Actual: extraction held every page several times over and sent all pages in one request (10 × 5 MB pages ≈ 67 MB body, +378 MB RSS measured) against a 128 MB isolate, so the job died and retried to failed_final | — | A 15 MiB per-scan bound (`HOMEWORK_SCAN_MAX_TOTAL_BYTES`, rule SCAN_TOO_LARGE) enforced at registration and in the job; extraction strips, hashes and encodes each page once (measured peak 40 MiB at the bound); a provider 4xx for the request is permanent, not retried; mobile downscales to 2000 px before the JPEG encode | `homework.test.ts` JOBS-R1-02 cases; `jobs-hardening.review.test.ts` request-size case; `packages/ai/src/request-size.test.ts`; `apps/mobile/src/homework/native-io.test.ts` | Platform limits are candidates until the owner confirms them (web client and child copy: BUG-160, BUG-164) |
| BUG-158 | Medium / AI spend cap (spec P16 ceiling) | closed | hardening bug hunt round 1 (JOBS-R1-03), fixed in `wf_90c8faf6-975` | Expected: an attempt the provider may bill counts against the caps. Actual: timed-out, network-failed and 5xx attempts were metered at zero, so the stage cap admitted more attempts and the monthly ceiling under-counted exactly during a slow-provider period | — | Unknown-usage attempts are metered at their upper-bound estimate (`usageEstimated`), counted in the stage's spent and the ceiling; a 4xx or 429 refused before the model runs stays at zero; the settle path keeps the hold when rows cannot be written (Cost_Analysis note) | `packages/ai/src/run-metering.test.ts` (6 tests); `jobs-hardening.review.test.ts` | Optional: persist the estimate flag for later reconciliation |
| BUG-159 | Low / operability (runbook promised pipeline codes) | closed | hardening bug hunt round 1 (JOBS-R1-04), fixed in `wf_90c8faf6-975` | Expected: `last_error_code` holds the pipeline code the runbook names. Actual: it held the exception class name ('RetryableFailure', 'Error') | — | `ledgerErrorCode` stores a payload-free code (`^[A-Z][A-Z0-9_]{1,63}$`) or the class name, never a message; `JobFailure(code)`; lead: the safety-flag email handler throws coded failures | `jobs-hardening.review.test.ts` JOBS-R1-04; `safety-flag-email.test.ts` (EMAIL_SEND_FAILED) | — |
| BUG-160 | Medium / scan capture on the web (a regression of BUG-157's bound) | closed | round-2b fixers' and checkers' follow-ups, fixed in `wf_1eaa94df-496` (round 2c) (R2C-WEB-1) | Expected: ordinary phone photos fit one web scan. Actual: after the 15 MiB per-scan bound, the web portal uploaded picked photos as they were (3-6 MB each), so a scan of four or more pages was refused with SCAN_TOO_LARGE after the parent had picked them | — | `downscaleForUpload` (apps/web/src/lib/image-downscale.ts) shrinks each measured JPEG/PNG to at most 2000 px on the longest side as JPEG 0.85 (drops EXIF/GPS; never throws, falls back to the original); the result is cached per file so a retry sends identical bytes; `homeworkScanFits` refuses an oversize set before any API call; SCAN_TOO_LARGE shows the API's parent copy and keeps the pages selected; the limits line names the bound. Residuals: an original over 15 MB is still refused before shrinking; every measured photo is re-encoded (privacy over size) | `HomeworkPage.test.tsx` 'bounded web scans' (5 tests, failed before); `apps/web/src/lib/image-downscale.test.ts` (8 tests) | — |
| BUG-161 | Medium / P17 promotions: an Amazon template silently lost its channel | closed | round-2b fixers' and checkers' follow-ups, fixed in `wf_1eaa94df-496` (round 2c) (R2C-WEB-2); completed by the lead from the fixer's patch after the checker found the first fix incomplete | Expected: an owner can target the Amazon Appstore in a promo campaign and keep it. Actual: the offer-mapping grid, the template form's channel checkboxes and the save helper all listed three channels literally; saving an existing Amazon template through the edit form dropped `amazon_appstore` without an error | The first fix changed only the offer grid — rejected by the checker | Every list reads `channelSchema.options`: the offer grid, the Billing channels checkboxes and the `ordered` save helper | `CampaignsSection.test.tsx`; `PromotionsAdminPage.test.tsx` (offers every channel, creates an Amazon template, keeps Amazon on edit); `template-form.test.ts` (contract order, Amazon round trip) | — |
| BUG-162 | Medium / AC_ACCESS (password recovery started on the phone) | closed (software); allow-list with the owner (Owner action #40) | round-2b fixers' and checkers' follow-ups, fixed in `wf_1eaa94df-496` (round 2c) (R2C-WEB-3) | Expected: a password reset requested in the mobile app can be finished. Actual: the mobile client's implicit-flow link lands on the web's /update-password with tokens in the hash, which the PKCE web client ignores, so the parent reached a dead end | "Move the mobile client to PKCE" — rejected: the code verifier would live in the app while the link opens in a browser | On /update-password only, a hash with `type=recovery` and both tokens is handed to `setSession` (verified by the auth server), the tokens leave the address bar at once, and the page names the account by masked email with a 'Not your account?' link before the form; a refusal shows the failed-link notice; every other page and flow is unchanged (PKCE kept). Residual by design: a valid recovery link replaces an account already signed in on that browser | `apps/web/src/pages/auth/UpdatePasswordPage.test.tsx` (12 tests incl. the lead's guard cases: an error or a missing session refuses the link even with an email present) | Owner action #40 covers the redirect |
| BUG-163 | Medium / WCAG 2.2 AA contrast in the mobile app (twin of BUG-145) | closed | round-2b fixers' and checkers' follow-ups, fixed in `wf_1eaa94df-496` (round 2c) (R2C-MOB-1); two screens outside the fixer's files fixed by the lead | Expected: white button text at least 4.5:1. Actual: mobile buttons, selected chips and selected answers drew white text on `colors.teal` at 4.07:1 on eight screens | — | Every fill behind light text uses `colors.tealText` (5.9:1); `colors.teal` stays on borders, focus rings and text-free progress fills | `apps/mobile/src/family/contrast.test.ts`, widened by the lead to every screen and component source (28 files) so a new screen cannot reintroduce it; fails when the welcome button's old fill is restored | — |
| BUG-164 | Low / child copy for an oversize scan | closed | round-2b fixers' and checkers' follow-ups, fixed in `wf_1eaa94df-496` (round 2c) (R2C-MOB-2) | Expected: a child whose pages are too large together hears it in child words, before anything is sent. Actual: the child saw the generic failure line, after the server refused the scan | — | `RULE_COPY.SCAN_TOO_LARGE` ('These pictures are too big to send together. Ask a grown-up to help.') and a `homeworkScanFits` pre-check after the fingerprint pass, before any API call (no draft, no allowance reserved). Residual: 'Try again' repeats the same refusal until the pages are retaken | `apps/mobile/src/homework/upload.test.ts` 'a scan too large to send together' | — |
| BUG-165 | **High** / spec P3, AC_ACCESS_07 (parent area stayed open on the device) | closed | round-2 hunt findings, fixed in `wf_56274b45-95e` (round 3) (MOB-R2-01) | Expected: leaving the app, or tapping 'Lock parent area', closes the parent area on the device. Actual: both only revoked the server step-up — the open parent screen kept its already-loaded family data in component state (with a working Refresh), and every other parent screen still opened without a PIN until the 300 s window ran out, because `useParentAccess` checked the unlock once, at mount | — | One lock action, `lockParentAreaOnDevice(effects)` in `src/lib/mode.ts`: forget the client unlock, clear the adult caches, `resetNavigationToUnlock()` (dismissAll + replace, so the screen and its state are unmounted), then the server relock — a failed relock never leaves the screens up. Used by the backgrounding handler and by one shared `<LockParentAreaButton />` that replaced both bespoke buttons; `useParentAccess` re-checks on every AppState 'active'. The Android store purchase sheet is exempt through a `whileStorePurchaseOpen()` pair that always clears | `src/lib/mode-r2.review.test.ts` 'one lock action for backgrounding and both Lock buttons' (4), `src/lib/app-session.test.ts` (2), `src/family/screens-r2.review.test.ts` (6); all failed before | Both of the acceptance checker's residuals on this fix are closed in the same round: the store-purchase exemption now lapses after 5 minutes (`STORE_PURCHASE_EXEMPTION_MS`), so a purchase promise that never settles cannot hold the lock open, and each parent-gate check supersedes the one still in flight, so two overlapping checks cannot race to `setAccess`. `mode-r2.review.test.ts` 'the exemption lapses…' failed before its fix; the supersede is a source assertion, since the vitest project cannot render a react-native hook |
| BUG-166 | Medium / AC_ACCESS_04 (child session judged on the wrong clock) | closed | round-2 hunt findings, fixed in `wf_56274b45-95e` (round 3) (MOB-R2-02) | Expected: a child keeps working while their pairing is valid. Actual: the device compared its own clock with a server-clock expiry, so a tablet 20 minutes slow presented tokens the API had already expired (and one 401 forgot the pairing), while a tablet 20 minutes fast refreshed constantly | — | The API states the lifetime, not only the deadline: additive `accessTokenExpiresInSeconds` on `childTokenResponseSchema`, returned by `/v1/child/pair` and `/v1/child/refresh`; `child-session.ts` stores `device clock at receipt + lifetime`. `withChildTokenRetry` drops a stale cached token and retries once through the SAME single-flight refresher (no second refresher, L-007); only the refresh itself being refused forgets the pairing | `src/family/child-session-r2.review.test.ts` (6: correct clock, 20-min slow, 20-min fast, retry, second refusal, pairing kept) + `apps/api/tests/mobile-r2.review.test.ts` (2); failed before | — |
| BUG-167 | Medium / spec P3 (a fast device clock locked the parent out) | closed | round-2 hunt findings, fixed in `wf_56274b45-95e` (round 3) (MOB-R2-03) | Expected: a correct PIN opens the parent area. Actual: a device clock more than 5 minutes fast sent every correct PIN straight back to the unlock screen with no message, because the granted window was stored as the server's instant and compared with the device's own clock | — | Additive `unlockSeconds` on `adultUnlockResponseSchema` from `config.adultUnlockTtlSeconds`; `enterParentMode` sets the window to `deviceNow + unlockSeconds`, clamped to exactly that, so a slow clock cannot stretch it. A window already over at unlock returns the new `window_lapsed` outcome, persists nothing, and says so on screen ('Your PIN was correct, but the unlock had already run out. Check this device's date and time…') | `src/lib/mode-r2.review.test.ts` 'the unlock window is held on the device clock' (4) + `apps/api/tests/mobile-r2.review.test.ts`; failed before | `unlockSeconds` stays optional in the contract until three web test fakes carry it; a server that stopped sending it would silently fall back to the instant |
| BUG-168 | Medium / AC_ACCESS_07 (child space could sign the parent out) | closed | round-2 hunt findings, fixed in `wf_56274b45-95e` (round 3) (MOB-R2-04) | Expected: nothing behind the PIN is reachable from the child space. Actual: 'Grown-ups' opened the unlock screen, which offered Sign out beside the PIN field, and signing out of a paired child device dropped it to the welcome chooser as if it had never been paired | — | The unlock screen renders Lock and Sign out only when the unlock was already active on entry, so the screen a child reaches has the PIN field alone. `signOutParent` reads the stored child refresh token first: on a device that holds a pairing it writes mode 'child' and resets to the child home, leaving the pairing untouched; a parent-only device still returns to the welcome screen | `src/lib/mode-r2.review.test.ts` (4) + `src/family/screens-r2.review.test.ts` (2); failed before | — |
| BUG-169 | Medium / AC_ACCESS_07 (back navigation left a child device in parent mode) | closed | round-2 hunt findings, fixed in `wf_56274b45-95e` (round 3) (MOB-R2-05) | Expected: returning to the child's space re-enters child mode. Actual: the header back arrow and Android/Fire Back left the keychain saying 'parent', so screen privacy stayed off and the next cold start opened the welcome chooser instead of the child's home | — | `useChildModeOnFocus()` (expo-router `useFocusEffect`) in `src/family/ui.tsx`: when the child group regains focus while the keychain still says 'parent' it runs `enterChildMode` — relock, forget the unlock, persist 'child', screen privacy off. Called from `app/(child)/_layout.tsx`, so every way back goes through the mode switch | `src/family/screens-r2.review.test.ts` 'returning to a child screen re-enters child mode'; failed before | — |
| BUG-170 | Low / spec P3 (a biometric PIN outlived its owner) | closed | round-2 hunt findings, fixed in `wf_56274b45-95e` (round 3) (MOB-R2-06) | Expected: the stored PIN belongs to the signed-in parent and goes when they do. Actual: biometric unlock kept the PIN in the keychain after sign-out and after account deletion, so the next parent to sign in on that device was offered a PIN that was not theirs | — | A pure `biometricOffer({enabled, ownerUserId}, signedInUserId)` returning offer / off / other_user that fails closed for a different user, for nobody signed in, and for a PIN with no recorded owner; `save()` records the owner and refuses to store a PIN with no signed-in parent; `isEnabled()` erases someone else's. Device sign-out clears the store, and account closure routes through `signOutClosedAccountOnDevice()` so a closed account leaves no mode, unlock or PIN behind | `src/family/screens-r2.review.test.ts` 'the biometric PIN never outlives its owner' (4, including the pure table); failed before | Residual: `isEnabled()` erases the PIN whenever `userId()` reads null, and the unlock screen calls it on mount even with no parent session |
| BUG-171 | Low / AC_ECC (a render error closed the app) | closed | round-2 hunt findings, fixed in `wf_56274b45-95e` (round 3) (MOB-R2-07) | Expected: a render-time exception shows something a child or parent can act on. Actual: no route had an error boundary, so any render error took the whole app down | — | A shared calm `ErrorScreen` (no stack trace, one retry) plus expo-router `ErrorBoundary` exports on the root, child and parent layouts — 'Oops! Something went wrong. Let's go back home.' with a 'Go to my home' retry for the child. The boundary's `error` is never rendered | `src/family/screens-r2.review.test.ts` 'a render error shows a retry screen instead of closing the app' (4); failed before | — |
| BUG-172 | Low / AC_ACCESS_08 (device list showed a device that had signed itself out) | closed | round-2 hunt findings, fixed in `wf_56274b45-95e` (round 3) (API-AUTH-R2-05) | Expected: the parent's device list says which devices are connected. Actual: a device that signed itself out (`POST /v1/child/logout`) kept showing 'Connected', so the parent's only way to make the list honest was to disconnect a device that was already gone | — | `/v1/child/logout` also stamps `child_devices.revoked_at` for the session's device, but only when no other live, unexpired, unrevoked session remains on it — a shared tablet with a second session stays connected. Both writes are in the one `asService` transaction | `apps/api/tests/mobile-r2.review.test.ts` 'a device that signs itself out stops being listed as connected' (3); failed before | The list cannot yet tell a device that signed itself out from one the parent disconnected; that needs a revoke reason on `child_devices` |
| BUG-173 | **High** / AC_ACCESS (no way to sign out of the portal) | closed | round-2 hunt findings, fixed in `wf_56274b45-95e` (round 3) (WEB-R2-01) | Expected: a parent on a shared or library computer can end their portal session. Actual: the portal had no sign-out control anywhere; the session simply stayed signed in, and the only exit was clearing site data | — | A `SignOutControl` with the masked signed-in email in the portal shell, on every `/app` and `/admin` path: it POSTs `/v1/adult/lock` while the token is still valid, then signs out with scope `'local'` (never supabase-js's default global, which would end the phone's session too), then lands on `/sign-in`. A failed lock never blocks the sign-out | `apps/web/src/App.signout.test.tsx` enumerates every registered `/app` and `/admin` route from the real route table and asserts the control, the masked email, the lock-before-sign-out order, the scope and the landing; failed before | — |
| BUG-174 | Medium / AC_ACCESS (an ended session left every page on a dead end) | closed | round-2 hunt findings, fixed in `wf_56274b45-95e` (round 3) (WEB-R2-02, both halves) | Expected: a session the server has ended sends the parent to sign in. Actual: every portal page showed 'Sign in again to continue' and a 'Try again' button with no sign-in link, for up to about an hour — and signing out of the phone was what ended it, because the mobile client used the default global scope | — | `signOutOnUnauthenticated(api, auth)` wraps the client inside `createDefaultSession`: any parent call answered UNAUTHENTICATED ends this browser's session locally and rethrows unchanged, so `RequireParent` renders the sign-in prompt with `?next=` kept. Mutations too, not only page loads. The mobile half: `parentAuth.signOut()` is scope `'local'`, so the phone no longer ends the portal's session ('Sign out everywhere' stays the portal's own action) | `apps/web/src/lib/session.unauthenticated.test.tsx` (401 on a load and on a mutation, the real client driven through a stubbed 401, a 500 still retried) + `apps/mobile/src/family/screens-r2.review.test.ts` 'signing out of the phone leaves other sessions alone'; failed before | — |
| BUG-175 | Medium / spec P3, AC_SECURITY (account takeover from an unattended browser) | closed | round-2 hunt findings, fixed in `wf_56274b45-95e` (round 3) (WEB-R2-04, plus the checker's residual) | Expected: changing the account password needs the current one, unless the parent came from the emailed reset link. Actual: `/update-password` changed it from any signed-in session with no proof at all — so an unattended browser was enough to take the account over, and after a PIN reset to request exports and deletions. The weaker secret (the 6-digit PIN) was the better-protected one | "A recovery flag is enough" — rejected by the acceptance checker: the flag was set once and never cleared, and the adapter lives at module scope, so one followed link left the bare form standing for the life of the page | The bare form is offered only in a recovery session; any other session types the current password, proved on a second Supabase client with `persistSession`/`autoRefreshToken`/`detectSessionInUrl` off and signed out locally straight after, so the portal's session — and an owner's aal2 — is untouched. No proof capability means the page refuses. The recovery grant is now single-use (a successful change closes it), bounded to 15 minutes from the link, and closed by a sign-out or the auth server's SIGNED_OUT | `apps/web/src/pages/auth/UpdatePasswordPage.reauth.test.tsx`: the original cases (no change without the current password, the proof never calls the session-replacing sign-in, a wrong proof changes nothing) plus 6 grant-lifetime cases; both rounds failed before their fix, each mutation-checked | Owner action: switch ON 'Secure password change' (reauthentication) in the hosted Supabase project, so the server enforces it too |
| BUG-176 | Low / AC_ACCESS_07 (a step-up refusal threw away the form) | closed | round-2 hunt findings, fixed in `wf_56274b45-95e` (round 3) (WEB-R2-05) | Expected: a PIN refusal is answered where the parent is working. Actual: every step-up refusal outside the privacy page sent them to `/app/security`, losing a half-filled schedule, test date or reward form, with no way back | "Two copies of the notice" — rejected by the acceptance checker: there were five (SecurityPage, HomeworkPage, `components/learning/feedback.tsx`, RewardsPage, SponsorCard), and the first fix left the learning planner and Rewards on the old link-only notice | The privacy page's inline prompt became a shared `StepUpPrompt`; `StepUpNotice` renders it in place on every surface, so the PIN is typed on the page being filled in. The fallback link keeps the exact `/app/security` href other areas' tests pin, and carries the return path as router state | `apps/web/src/pages/app/StepUpNotice.test.tsx` and `apps/web/src/components/StepUpPrompt.reuse.test.tsx` (6 cases over the planner, Rewards and the shared widget); failed before | A shared widget that pages render inside their own `<form>` must not contain a `<form>` itself — the nested submit bubbled and re-fired the refused action (Lessons_Learned) |
| BUG-177 | Low / WCAG 2.2 AA (no page title, no focus move on navigation) | closed | round-2 hunt findings, fixed in `wf_56274b45-95e` (round 3) (WEB-R2-06) | Expected: each page announces itself and moves focus on a client-side navigation. Actual: portal, admin and auth pages never set a title, focus stayed where it was, scroll was not restored, and the signed-out `/app` prompt had no `h1` | — | `App.tsx` sets `document.title` from `PORTAL_PAGE_TITLES` for every `/app`, `/admin` and auth route (public pages keep their own `PageTitle`), moves focus to the new page's `h1` (or `#main`) on navigation but not first load, and renders `<ScrollRestoration/>`; the signed-out prompt gained an `h1` | `apps/web/src/App.navigation.test.tsx` asserts the title map covers exactly the registered routes and checks focus and first-render behaviour; failed before | Residual: on a page that renders `<Loading/>` first there is no `h1` yet, so focus lands on `<main>` |
| BUG-178 | Low / honesty (a dead 60-second download link) | closed | round-2 hunt findings, fixed in `wf_56274b45-95e` (round 3) (WEB-R2-07) | Expected: an expired export link can be replaced without reloading. Actual: the signed link expires after 60 s and the page kept showing 'Download file', so the parent clicked a dead URL | — | `ExportDownload` drops the link at its `expiresAt`, says it expired, and always keeps the button — labelled 'Get a new link' once one has been fetched | `apps/web/src/pages/app/PrivacyControlsPage.exports.test.tsx` with a pinned clock (link + button, expiry, a second different signed link); failed before | — |
| BUG-179 | Low / AC_SECURITY (a PIN reset silently dropped an owner's MFA) | closed | round-2 hunt findings, fixed in `wf_56274b45-95e` (round 3) (WEB-R2-08) | Expected: re-proving the password for a PIN reset leaves an owner's admin access intact. Actual: the password grant replaced the session with a new aal1 one, so an owner with two-step verification silently lost aal2 and their admin pages | — | `PinResetPage` reads the assurance level and verified factor on mount: an owner at aal2 is warned before the re-proof, then a 'Confirm your two-step code' step restores aal2 before the PIN is accepted; a refused code stops there and the PIN is never sent. The grant stays on the portal client because the API reads the re-authentication from the request token's `amr` — the check was not weakened | `apps/web/src/pages/app/PinResetPage.test.tsx` (warning up front, the code step required, a refused code sends nothing, an aal1 parent unchanged); failed before | Optional follow-up: a `POST /v1/adult/reauthenticate` nonce would let the proof happen on the throwaway client instead |
| BUG-180 | Low / first paint and bundle size | closed | round-2 hunt findings, fixed in `wf_56274b45-95e` (round 3) (WEB-R2-09) | Expected: the first load shows the brand, and the bundle does not warn. Actual: no `HydrateFallback`, so the first load was a blank off-white page until the chunk arrived, and the 643 kB main bundle tripped Vite's >500 kB warning | — | A branded `ShellLoading` hydrate fallback on the root route (skip link, brand bar, a Loading status) and a manual chunk split; the entry chunk is 70.9 kB (24 kB gzipped) and the warning is gone | `apps/web/src/App.navigation.test.tsx` (WEB-R2-09 block) plus a real production build; failed before | Residual, deliberately not in this round: the 214 kB supabase-auth chunk is still in the initial module graph because the adapter is created at module scope — lazy-loading it needs `recoveryActive()` to become async |
| BUG-181 | **High** / spec P2 (no client could edit a child or the family) | closed | round-2 hunt findings, fixed in `wf_56274b45-95e` (round 3) (WEB-R2-03) | Expected: a parent can fix a child's grade, nickname or age band, the family's name or time zone, and archive a child. Actual: no client had any way to do any of it — the archive API had no caller at all, and a wrong grade or time zone was permanent | — | `PATCH /v1/children/:childId` (nickname through `freeTextSchema`, grade 0..GRADE_LEVEL_MAX, age band from the enum; every field optional, at least one required) and `PATCH /v1/family` (display name, IANA zone), both in the existing router, both behind a recent adult unlock with an explicit ownership check under the family row lock, both auditing the changed field NAMES only. Web and mobile screens for all of it | `apps/api/tests/family-profile.review.test.ts` (8 cases), `apps/web/src/pages/app/ChildrenPage.profile.test.tsx` (5), plus the mobile children screen; failed before | — |
| BUG-182 | Medium / spec P4 (a child under deletion could be used again) | closed | round-2 hunt findings, fixed in `wf_56274b45-95e` (round 3) (API-AUTH-R2-02, migration 0860) | Expected: a child whose data deletion is pending cannot be re-activated or paired. Actual: activation answered 200 and set `status='active'`, a pairing code was issued, and the device collected new homework until the purge ran | "Hide the child from `GET /v1/family`" — rejected by the acceptance checker: hiding a row refuses nothing (the refusals live in the routes and the trigger) and it broke the only consumer of that list, the nickname lookup on both privacy screens | A `visibleChild` helper applies one rule — the child belongs to the caller's live family and has no `deletion_requests` row in ('requested','processing') — on activation and pairing-code; migration 0860 adds a BEFORE UPDATE trigger as the database backstop for the request/activation race. The child stays in `GET /v1/family` carrying an additive `deletionPending` flag, so the screens name it and offer no control | `apps/api/tests/family-profile.review.test.ts` 'a child under a pending data deletion stays unusable' (5 cases, incl. the trigger's own refusal staged through an inserted deletion row rather than raw SQL) plus the flagged-list case; both rounds failed before | Lesson: hiding a row is not the same as refusing an action on it |
| BUG-183 | Low / validation (year 0000 answered 500) | closed | round-2 hunt findings, fixed in `wf_56274b45-95e` (round 3) (API-AUTH-R2-03, plus the checker's residual ACC-FAM-05) | Expected: an implausible calendar date is a 400. Actual: `0000-01-01` passed the format-only contract and raised SQLSTATE 22008 on the `::date` cast — a 500 and an error log — on test-date and learning-schedule writes | "Map the SQLSTATE" or "bound the contract" — neither alone: a stored row can still reach a cast, and a format-only contract accepts any four-digit year | `calendarDateSchema` and `calendarMonthSchema` are bounded to 2000–2100, and `knownConstraintError` maps 22008 and 22007 to VALIDATION_FAILED. 22003 (numeric out of range) is deliberately NOT mapped (the checker's residual): money is integer cents and every caller value is contract-bounded, so a numeric overflow is our arithmetic, and a 400 would blame the parent and lose the error log | `apps/api/tests/family-profile.review.test.ts` (5 dates, zero unhandled-error logs) + `apps/api/tests/input-hardening.review.test.ts` 'maps only the datetime overflows, so a numeric overflow of ours stays a 500'; both failed before | Residual: the bound also parses stored rows, so a pre-existing implausible year would now fail its response contract |
| BUG-184 | Low / AC_LEARNING (older practice sets unreachable) | closed | round-2 hunt findings, fixed in `wf_56274b45-95e` (round 3) (API-AUTH-R2-04) | Expected: a parent can reach every practice set. Actual: the list was hard-capped at 30 with no cursor, so a family past the cap could not see older sets at all | — | `GET /v1/children/:childId/practice-sets` is keyset-paged newest first, `(created_at desc, id desc)` with an `after` cursor of `<epoch micros>_<id>` through the existing BigInt guard, an additive `nextCursor`, and questions fetched only for the rows returned. The key is the highest kept pair, never a row count (L-033), so retention cannot make a page skip or repeat | `apps/api/tests/family-profile.review.test.ts` 'older parent practice sets stay reachable' (35 sets page 30 + 5, no duplicates, malformed and out-of-int64 cursors refused); failed before | Open, stated as deliberate: `GET /v1/exports` and `GET /v1/family/promotions` are still hard-capped with no cursor |
| BUG-185 | Medium / AC_SECURITY_02 (a false alarm could not be cleared) | closed | round-2 hunt findings, fixed in `wf_56274b45-95e` (round 3) (CS-R2-01) | Expected: a guardian or reviewer can clear a flag as a false alarm. Actual: 409 and the whole clearance rolled back once job retention (BUG-139) had pruned an earlier `scan:<id>:v1` row while v2 was kept — the next key was derived from a COUNT of kept rows, so it collided with a version still held | — | `queueClearanceRecheck` derives the next key from the HIGHEST kept version (`max(substring(idempotency_key from ':v([0-9]+)$')::int)`), the same form the scan and correction routes use (L-033); the dispatcher's retention comment now states the rule for all three sites | `apps/api/tests/privacy-r2.review.test.ts` 'false-match clearing after job retention pruned an earlier scan job' (the guardian's route and the owner reviewer's); failed before | L-033 now has a second independent site: a retention-prunable derived key is audited repo-wide, not per call site |
| BUG-186 | Medium / AC_SECURITY_02 (a provider flag reached no one) | closed | round-2 hunt findings, fixed in `wf_56274b45-95e` (round 3) (CS-R2-03) | Expected: a moderation flag on a child's own words reaches the parent. Actual: a flag whose category the mapping did not know — none at all, a category added after this code, or harassment/hate/illicit — read as level 'none', so the answer was graded, coached and never reported | — | `packages/ai/src/moderation.ts` fails closed: harassment, hate and illicit map to 'violence' like their siblings, an unknown or new category takes `PROVIDER_FALLBACK_CATEGORY`, and the screen is 'severe' whenever the provider flagged the child's words. The API test whose assertion WAS the defect ('only logs a code; the answer is graded') was replaced, not weakened, with the post-fix expectation | `apps/api/tests/privacy-r2.review.test.ts` 'a provider flag with no mapped category still reaches the parent' (5 scan-level cases) and the replaced `provider-moderation.test.ts` case; failed before | Owner confirmation: the fallback category is 'violence' because migration 0760 allows only six and there is no 'other'. A distinct reviewer-queue code for 'flagged, nothing mapped' would need a migration and a child template line |
| BUG-187 | Medium / spec P4 (consent withdrawal stranded a scan) | closed | round-2 hunt findings, fixed in `wf_56274b45-95e` (round 3) (CS-R2-04 = JOBS-R2-03) | Expected: withdrawing consent settles the scans whose jobs it cancels. Actual: it cancelled the jobs and left the scans in 'queued' or 'checking' for good — a recheck stayed 'checking' so the flag could never be cleared, and a queued scan held its page allowance with no live job | — | `POST /v1/consent/withdraw` settles them in the same transaction: the cancel returns kind and payload, and `settleScansAfterConsentWithdrawal()` locks each assignment, moves a recheck in checking/verifying to `needs_parent_review` (results kept) and an initial scan to `failed_final` with `CONSENT_REQUIRED`, releasing its reservation | `apps/api/tests/privacy-r2.review.test.ts` 'consent withdrawal leaves no scan stranded' (a checking recheck and a queued scan with its reservation); failed before | Deliberately not added: a periodic 'checking with no live job' recovery sweep — the withdrawal path is settled at the source |
| BUG-188 | Low / AC_SECURITY_02 (an unresolved flag dropped off the list) | closed | round-2 hunt findings, fixed in `wf_56274b45-95e` (round 3) (CS-R2-07, plus the checker's residual) | Expected: every flag waiting on a grown-up is in the family's list. Actual: a flat `order by created_at desc limit 100` dropped an older open flag once a hundred newer reports existed (word-list false matches, the child's own reports and the parent's own add up), so it could not be marked looked into or cleared | "Lift the cap on the unresolved branch" — rejected by the acceptance checker: nothing but a grown-up resolves a report, a guardian may file 30 an hour with a 500-character note, and system flags stay escalated, so the response became unbounded | Unresolved first and never behind the resolved page, resolved rows keeping their 100 cap — and the unresolved set bounded at `UNRESOLVED_REPORTS_PAGE_SIZE` = 200 taken from the OLDEST end, so the bound drains: each report acted on makes room for the next and nothing becomes permanently unreachable, which a newest-first cap is exactly what did | `apps/api/tests/privacy-r2.review.test.ts` 'the family's report list never drops an unresolved flag' and 'bounds the unresolved rows from the oldest end, so the bound drains'; both failed before, both mutation-checked | `GET /v1/exports` keeps its cap (owner decision, stated in the ECC row) |
| BUG-189 | Medium / AC_OPS (churn could exceed 100%) | closed | round-2 hunt findings, fixed in `wf_56274b45-95e` (round 3) (BILL-R2-1, plus the checker's regression BILL-R2-1-a) | Expected: the owner's churn figure is a fraction of the families that were paying. Actual: it was measured per ledger row, so a plan upgrade counted as one lapse plus one new subscription and churn could exceed 100% | The first fix defined 'lapsed' as a base family granting no paid access AT THE REQUEST INSTANT with no requirement that the ending fall in the month — caught by the acceptance checker: a family that left in an earlier month was counted again in every later month up to its period end | Movement is measured per family: new counts families whose earliest entitlement was created in the month (a tier change is not a new subscription), the base counts distinct families whose period had not ended at the month start, and a CTE dates every row's `ended_at` by the domain grant rule (period end plus the 30-day access bound; a refund or revocation by the provider's update instant) so a departure is counted in the month access actually stopped | `apps/api/tests/billing-r2.review.test.ts` 'churn is measured per family and can never exceed 100%' (3 cases) plus the re-fix's two dating cases; both rounds failed before | Residual: `byStatus` still aggregates entitlements with no families join, so a tombstoned family's frozen row can appear in the status breakdown |
| BUG-190 | Medium / AC_OPS (a deleted family stayed 'active') | closed | round-2 hunt findings, fixed in `wf_56274b45-95e` (round 3) (BILL-R2-2) | Expected: an account deletion is visible in the owner's subscription figures. Actual: the tombstoned family's ledger row stayed 'active' for ever (its webhooks are ignored as FAMILY_DELETED and the sweep skips it) and it was never counted as churn | — | The active and subscribed queries join `families on deleted_at is null`; the base excludes families tombstoned before the month began but keeps one deleted during it, so that family is counted as lapsed | `apps/api/tests/billing-r2.review.test.ts` 'a deleted family is out of the subscription counts and counts as churn' (2 cases); failed before | Residual: a family that subscribes and deletes inside one month is in no month's churn |
| BUG-191 | Medium / AC_PROMO_12, P16 (two revenue figures disagreed) | closed | round-2 hunt findings, fixed in `wf_56274b45-95e` (round 3) (BILL-R2-3) | Expected: the overview's 'recognized this month' agrees with the P16 revenue summary. Actual: the overview summed recognized rows directly, including the network revenue the summary excludes as double-counted when a sponsor fee replaces it | — | A new `loadRecognizedRevenue(tx, month)` loads the month's entries and adjustments and returns `revenueSummary(...).recognizedCents` from the domain package, so the exclusion applies exactly as the admin endpoint reports it; the definition string says so | `apps/api/tests/billing-r2.review.test.ts` 'the overview agrees with the P16 revenue summary'; failed before | — |
| BUG-192 | Medium / AC_PROMO (a taxed renewal earned no donation) | closed | round-2 hunt findings, fixed in `wf_56274b45-95e` (round 3) (BILL-R2-4) | Expected: a full-price renewal earns the $1 school donation and books the subscription price as revenue. Actual: `amount_paid` included sales tax and pending proration lines, so a taxed full-price Stripe renewal did not look full-price — no donation — and the tax was booked as our revenue | — | `subscriptionChargeCents()` takes the subscription line's own amount net of that line's discounts, else `total_excluding_tax`, else subtotal minus invoice-level discounts, else `amount_paid` minus tax, with any proration line that states its own amount subtracted from the invoice-level fallbacks | `apps/api/tests/billing-r2.review.test.ts` 'sales tax is not part of the Stripe subscription charge' (3 cases: a taxed full-price renewal earns $1 and books 3999, a proration line excluded, a genuine discount still not full price); failed before | The primary branch is correct because Stripe's invoice-line `amount` is pre-discount; the checker recorded that as the assumption to re-check if Stripe changes it |
| BUG-193 | Medium / AC_PROMO_14 (a payout was netted against a later month) | closed | round-2 hunt findings, fixed in `wf_56274b45-95e` (round 3) (BILL-R2-5) | Expected: a payout batch nets only reversals of the accruals it pays. Actual: `POST /v1/admin/payouts/prepare` netted every open adjustment, so a September refund reduced — or withheld — an August batch | — | The adjustment query is restricted to adjustments of accruals in this batch plus carried-forward adjustments of already-batched accruals, matching the domain rule in `donations/adjustments`; a later month's reversal stays unbatched for the batch that pays its own accrual | `apps/api/tests/billing-r2.review.test.ts` 'a payout batch nets only the reversals it is allowed to' (2 cases); failed before | — |
| BUG-194 | Medium / AC_BILLING (a refused webhook was invisible and its id burned) | closed | round-2 hunt findings, fixed in `wf_56274b45-95e` (round 3) (BILL-R2-6) | Expected: a webhook body we could not parse is visible to the owner and the provider's retry is processed after a fix. Actual: it was stored as 'ignored', so no attention rule counted it, and the event id was burned — the retry after a schema fix was answered 'duplicate' and dropped | — | A refused body is stored as `failed` with `error_code` UNEXPECTED_SHAPE, so the `billing_events_failed` attention rule counts it (its definition says so) and `recordEvent`'s reopen-on-failed clause processes the retry. The body itself is still never stored, only its digest, id and type | `apps/api/tests/billing-r2.review.test.ts` 'a refused webhook body is visible and its retry is processed'; failed before | Residual: the retry does not update `payload_sha256`, so the row's digest still names the refused body |
| BUG-195 | Medium / spec P12 (a stalled store request held the whole tick) | closed | round-2 hunt findings, fixed in `wf_56274b45-95e` (round 3) (JOBS-R2-04, plus the checker's residual) | Expected: no external request can hold the scheduled tick. Actual: RevenueCat and Stripe requests had no timeout, so one stalled connection in the entitlement sweep held the tick — and with it the job ledger, where the work a family is waiting on (a scan, a safety email, an export) lives | "A per-request timeout is enough" — rejected by the acceptance checker: the sweep makes up to 25 requests one after another, so 10 s each still pushes the ledger to the end of the tick | `BILLING_REQUEST_TIMEOUT_MS` = 10 s on all four external calls through `AbortSignal.timeout` (as the storage, email and auth-admin adapters already do), and the tick now runs the job ledger BEFORE the provider sweep; entitlement staleness is measured in days, so a sweep cut short by the tick's end loses nothing | `apps/api/tests/billing-r2.review.test.ts` 'every RevenueCat and Stripe request carries a timeout' (2 cases with a fetch that never answers) + `apps/api/tests/scheduled.test.ts` 'runs the job ledger before the provider sweep, so a slow store cannot starve it'; both failed before | Optional: a `PROVIDER_REQUEST_TIMEOUT_MS` env var if the timeout should be per deployment |
| BUG-196 | **High** / spec P4, AC_PRIVACY (an export file orphaned in storage) | closed | round-2 hunt findings, fixed in `wf_56274b45-95e` (round 3) (JOBS-R2-01) | Expected: an export whose upload landed is delivered, and a family deletion removes its file. Actual: a lost answer to a successful upload ended the job 'failed' with the object left in storage, so the file outlived the deletion that was meant to remove it | — | The uploader treats a duplicate answer (409, or 400 with a Duplicate/already-exists body) on the job-owned path as this job's own earlier upload when `storage.stat`'s measured size equals the bytes just built; any other object at that path is removed and re-uploaded. Storage that cannot report a size rejects, so the job never assumes | `apps/api/tests/jobs-r2.review.test.ts` (duplicate with a matching size succeeds, a different size is replaced, a storage that cannot stat fails the job); failed before | — |
| BUG-197 | **High** / AC_AI, spend (ten billed calls for one truncated answer) | closed | round-2 hunt findings, fixed in `wf_56274b45-95e` (round 3) (JOBS-R2-02, plus the checker's regression) | Expected: an answer cut off at `max_output_tokens` is not re-sent unchanged. Actual: the same request went out ten times — ten billed calls — and the scan then ended `failed_final` with nothing for the parent | The first fix raised the budget once and then made OUTPUT_TRUNCATED a permanent failure for EVERY stage — caught by the acceptance checker: a truncated VERIFICATION answer then threw away grading the family had already paid for, where the LJA-F4 path deliberately degrades to parent review | `packages/ai/src/run.ts` raises the stage budget once (only while the cost cap admits the raised estimate) and otherwise returns OUTPUT_TRUNCATED; extraction and grading end the scan with the parent-facing `SCAN_TOO_MANY_QUESTIONS`, while a truncated verification degrades to `needs_parent_review` and keeps the paid grading | `packages/ai/src/run-truncation.test.ts` (4 cases) + `apps/api/tests/scan-process.test.ts` 'an answer the provider cut off at its output budget' incl. the verification case; both rounds failed before | Deliberately not done: splitting extraction per page or grading into question batches, so a very long facts sheet still cannot be checked — it now fails politely and cheaply instead of after ten billed calls |
| BUG-198 | Low / durable jobs (a dead-lettered export left a 'queued' row) | closed | round-2 hunt findings, fixed in `wf_56274b45-95e` (round 3) (JOBS-R2-05) | Expected: an export whose final attempt dies is marked failed and leaves nothing behind. Actual: `export_build` had no dead-letter compensation, so a worker killed on the last attempt (LOCK_EXPIRED) left the row 'queued' for ever and its paths in storage | — | `settleDeadLetteredExport(deps, job)` marks a still-'queued' row 'failed' and removes its deterministic paths; the handler is wired to it through `onDeadLetter` | `apps/api/tests/jobs-r2.review.test.ts` (a lease-expired final attempt settles the row and removes the objects); failed before | Follows L-035: a job backing a promise needs dead-letter compensation |
| BUG-199 | Low / spend (a paused scan re-encoded its pages hourly) | closed | round-2 hunt findings, fixed in `wf_56274b45-95e` (round 3) (JOBS-R2-06) | Expected: a scan refused at the spend ceiling costs nothing while it waits. Actual: every hourly attempt re-downloaded, hashed, stripped and base64-encoded every page before being refused again (288 MiB of work for one paused scan over a day) | — | `extract()` acquires the spend hold FIRST and reads the pages inside the admitted window, so a refused scan reads nothing; the wait grows (1 h doubling to a bound) and a scan paused past the bound settles with the parent-facing `AI_PAUSED_TOO_LONG` | `apps/api/tests/jobs-r2.review.test.ts` (a refused admission touches no page, the wait grows, the durable bound settles the scan); failed before | Residual: the durable count reuses `assignments.processing_attempts`, which also counts ordinary retryable cycles, so a scan that already failed retryably settles after fewer waits |
| BUG-200 | Low / dispatcher (a job claimed with no time to run it) | closed | round-2 hunt findings, fixed in `wf_56274b45-95e` (round 3) (JOBS-R2-07) | Expected: the claim budget reflects the tick's real remaining time. Actual: the 10-minute budget was measured from `runJobs`' own start, after unbounded earlier steps, so a long job could be claimed with no time left to finish and was killed mid-flight | — | `runJobs` takes the tick's start instant and measures the budget from it; `TICK_WALL_LIMIT_MS` (15 min) and per-kind worst cases (`scan_process` 7 min, default 60 s) filter the claimable kinds to those that still fit | `apps/api/tests/jobs-r2.review.test.ts` (a late tick claims only the short kinds); failed before | — |
| BUG-201 | Low / AC_CAPTURE (a sideways photo was graded sideways) | closed | round-2 hunt findings, fixed in `wf_56274b45-95e` (round 3) (JOBS-R2-08) | Expected: a photo that relies on the Exif Orientation tag is read the right way up. Actual: stripping dropped APP1 whole, orientation with it, so a photo the camera stored rotated reached the model sideways | — | `exifOrientation()` reads only tag 0x0112 from IFD0 (both endiannesses, every offset bounds-checked) and `orientationOnlyApp1()` re-emits a 34-byte APP1 carrying that tag alone; everything else — GPS, camera, timestamps, thumbnails, maker notes — is still dropped | `apps/api/tests/image-metadata.test.ts` (orientation kept, GPS and camera gone, a malformed APP1 dropped whole); failed before | Documented deviation from spec line 234 ('Strip EXIF and location metadata'): `docs/Threat_Model.md` T19 now states the exception and why (orientation is neither location nor camera identity). Owner action to confirm |
| BUG-202 | Medium / spec P4, AC_SECURITY_02 (the family could read the kind of concern) | closed | round-2 hunt findings, fixed in `wf_56274b45-95e` (round 3) (CS-R2-02) | Expected: the family learns that an answer was flagged, never which kind of concern. Actual: the family-readable audit row carried the provider's codes (PROVIDER_SELF_HARM_INTENT, …) and the screen's categories, which `audit_member_read` let any family member select with their own JWT — worst of all when the adult reading it is the subject of the child's disclosure | — | The family row carries only `{ category: 'severe_risk', screenVersion, providerModeration? }` — the same generic marker the family already reads on the report. The codes and the source layer moved to a reviewer-only `safety_screen.codes` row with `family_id` null, hung off the question, not the report. The row also follows the same `held` decision as `family_visible`, so repopulating FAMILY_HOLD_CATEGORIES cannot make a held report visible by the back door (the checker's residual) | `apps/api/tests/scan-process.test.ts` 'the family never learns which kind of concern' and the two rewritten `provider-moderation.test.ts` assertions; failed before. The `held` alignment has no reproduction: FAMILY_HOLD_CATEGORIES is empty under the owner decision, so the path is unreachable today — stated here rather than left implied | — |
| BUG-203 | Medium / spec P4 (the 'all family data' export was not all of it) | closed | round-2 hunt findings, fixed in `wf_56274b45-95e` (round 3) (CS-R2-05) | Expected: the export holds the family's data. Actual: it left out the child's homework answers, feedback, reward requests, profile notes and safety history | — | `familyData()` adds accessibility and curriculum notes and nine sections: assignments, questions (printed prompt, the child's answer, both corrected fields, page and skill data), question results with parent overrides, child feedback, target-answer attempts, rewards, redemptions, point balances and safety history | `apps/api/tests/jobs-r2.review.test.ts` (every new section present for a seeded family); failed before | Residual: the child-feedback body differs by category, so the download conveys the class of concern to whoever opens it — inherent to exporting the child's own message |
| BUG-204 | Low / honesty (the flag email claimed more than it delivered) | closed | round-2 hunt findings, fixed in `wf_56274b45-95e` (round 3) (CS-R2-06) | Expected: the flag says what was actually delivered. Actual: it said 'PencilLift emailed the guardians on this account' when only one address had accepted it — a bounced or unverified owner address read as delivered | — | `PARENT_SAFETY_FLAG_COPY.emailSent` now says at least one verified guardian address accepted it, names what was not reached and tells the parent to check their own address. `SAFETY_TEMPLATES_VERSION` is bumped to `safety-templates.v5` with a NEW pinned digest line in the approval test (v4 untouched) and the same child-template digest, since no child message changed | `apps/api/tests/safety-screening.test.ts` (both the honesty case and the approval gate, 37/37) + the domain template digest; failed before | The wording stays DRAFT: `SAFETY_TEMPLATES_STATUS`/`APPROVED` are unchanged, so Owner action #24 still gates release |
| BUG-205 | Medium / AC_AI (a repeated question number was dropped) | closed | round-2 hunt findings, fixed in `wf_56274b45-95e` (round 3) (DB-R2-06) | Expected: every printed question is checked. Actual: the unique (assignment, page, question number) constraint plus 'keep the first' silently dropped a second question printed with the same number, so a child's answer was never graded and nothing said so | — | `disambiguate()` keeps the first at its printed label and gives later ones `<number>#2`, `#3`, … (within the column's 20 characters, up to 40), logging `scan_duplicate_question_label`; extraction prompt bumped to `extraction.v3`, which asks for one row per printed number and says what to do with a repeat | `apps/api/tests/jobs-r2.review.test.ts` (a page with two '3's stores both); failed before | — |
| BUG-206 | Medium / performance (three hot queries had no index path) | closed | round-2 hunt findings, fixed in `wf_56274b45-95e` (round 3) (DB-R2-01/02/03/07, migration 0860) | Expected: per-request and per-job queries use an index. Actual: `attempt_overrides` had no `attempt_id` index (every daily-set and review job scanned all families' overrides), the inactivity sweep ran correlated MAX() subqueries over assignments, attempts and memberships, the AI spend admission summed all `ai_usage_events` while holding the single global budget row, and reward requests, test dates, study materials, devices and exports had no index path | — | Migration 0860 adds `attempt_overrides_attempt`, `assignments_family_created`, `attempts_family_created`, `family_memberships_family`, `ai_usage_events_created (created_at) include (cost_micros)`, `reward_redemptions_family_state`, `test_dates_child`, `study_materials_child_created`, `child_devices_family` and `data_exports_family`, each commented with the route or job it serves | `supabase/tests/hardening_r2_db.test.ts` (each index present and used by the shape of the query it serves); failed before | Open, stated as deliberate: the spend month sum is index-only now but still grows with the month's row count under the global lock — the durable fix is a running total in `spend-ceiling.ts` |
| BUG-207 | Medium / AC_SECURITY_04 (TRUNCATE bypassed RLS and the append-only tables) | closed | round-2 hunt findings, fixed in `wf_56274b45-95e` (round 3) (DB-R2-04, migration 0860) | Expected: no client role can bypass RLS or the append-only guard. Actual: `anon`, `authenticated` and `pl_child` held TRUNCATE (plus TRIGGER and REFERENCES) on every public table, and TRUNCATE is the one write that bypasses both RLS and `app.prevent_mutation()` on `audit_events`, `consent_records`, `points_ledger` and `ai_usage_events` | — | 0860 revokes truncate, trigger and references from all three roles on every table in `public` and from the schema's default privileges; a new `schema_invariants` case fails CI if any client role regains one of the three on any table | `supabase/tests/schema_invariants.test.ts` (2 new cases) + `hardening_r2_db.test.ts`; failed before | Owner action / runbook: `alter default privileges` is stored per granting role, so after deploying 0860 check `pg_default_acl` on the hosted project and repeat the revoke as the owning role if a client role still carries D/x/t |
| BUG-208 | Medium / AC_SECURITY_04 (API-only rules were absent on the Data API path) | closed | round-2 hunt findings, fixed in `wf_56274b45-95e` (round 3) (API-AUTH-R2-01/05, migration 0860) | Expected: a rule the API enforces holds however the row is written. Actual: rules that lived only in the routes — the 12-profile cap, the K-8 and under-13 scope, per-family write limits, archived and deletion-pending refusals — were absent for a signed-in parent writing through the Data API with their own JWT | — | Every `asParent` write was grepped first. `child_profiles` and `safety_reports` are written only with the service role, so 0860 revokes those grants from `authenticated` outright. The learning, reward and support tables ARE written as the parent role, so their grants stay and the rules moved into the database as BEFORE triggers that run only when `current_user = 'authenticated'` (SECURITY INVOKER, so jobs and service-role routes are untouched): a write for another family's child is refused, and per-family hourly bounds apply | `supabase/tests/hardening_r2_db.test.ts` (refusals and bounds per table) + rewritten `core_identity.test.ts` cases (the policy is unreachable now that the grant is gone, so the test asserts the grant's absence instead — narrower, and commented as such); failed before | Open, stated as deliberate: the DB bounds are 2x each API rule (a fixed-window limiter versus a row count over a sliding hour) and the `test_dates` DELETE grant stays ungated; moving those nine write sites to the service role would let the database hold the exact rule |
| BUG-209 | Low / spec P4 retention (pairing codes and spend holds were never pruned) | closed | round-2 hunt findings, fixed in `wf_56274b45-95e` (round 3) (DB-R2-08, migration 0860) | Expected: short-lived credentials are pruned like the session rows (BUG-155). Actual: consumed and expired pairing codes and lapsed spend holds stayed for ever | — | 0860 adds a service-only `app.prune_credential_rows(interval)` (pairing codes whose usable life ended more than the horizon ago, spend holds whose expiry passed over a day ago) plus the index its purge needs, wired into the identity-housekeeping step; the tick now reports the count as `endedCredentialRows` beside the other three | `apps/api/tests/auth-session.test.ts` (the prune count, aged by the database clock) + `supabase/tests/hardening_r2_db.test.ts`; failed before | — |
| BUG-210 | Low / honesty (two new scan endings had no copy) | closed | the round-3 acceptance checkers' residuals, fixed by the lead in the same round (JOBS-R2-02-parent-copy) | Expected: a parent or child whose scan ended is told what to do. Actual: the two new endings (`SCAN_TOO_MANY_QUESTIONS`, `AI_PAUSED_TOO_LONG`) had no copy on either surface, so the portal showed the generic 'could not be processed after several tries' and the child's screen said 'We couldn’t read this one' — untrue for both, since each scan was read perfectly well, and it sent the child to retake photos that were never the problem | "Give the child the specific advice too" — rejected: the child DTO carries no error code, and adding one needs a column grant to the child role and a migration for two lines of copy. The child's line is made true for every ending instead, and the specific advice stays with the parent, who can act on it | The portal maps the code to advice through a `FAILED_FINAL_COPY` table (split the worksheet; send the pages again), with the state's own copy as the fallback and the code never shown; the child's ended-scan line reads 'We couldn’t finish checking this one. Ask a grown-up for help, or try a new scan.' | `apps/web/src/pages/app/HomeworkPage.test.tsx` (both codes, and the raw code never rendered) + `apps/mobile/src/homework/result-view.test.ts` 'the ended-scan line is true whatever ended the scan'; both failed before | The child's specific advice needs `errorCode` on the child contract plus a column grant — a deliberate follow-up, not an oversight |
