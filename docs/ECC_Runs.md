# PencilLift ECC execution evidence (spec E5.3, AC_ECC_02/03)

ECC is not installed in this environment (`docs/ECC_Capabilities.md`). Every row is a **fallback** run with
Claude Code built-ins, labeled as such. No row claims an ECC skill or agent ran.

| Task / requirement | Capability actually used (fallback for ECC role) | Run reference | Starting → integrated revision | Files owned | Commands / exit codes | Findings / next | Usage |
|---|---|---|---|---|---|---|---|
| DOMAIN-* (9 modules: entitlements, quotas, promotions, donations, rewards, grading, answer-guard, learning, scheduling) | Workflow tool subagents: implement (for `tdd-workflow`) → fresh-context adversarial review writing failing tests (for `code-reviewer`) → fix (for `build-error-resolver`/`tdd-guide`) | Workflow run `wf_937b0690-525` | 9234b08 → b73d8ff (promotions, donations, entitlements, quotas), 69e0a31 (grading, answer-guard, rewards, learning, implementation stage); reviews and fixes integrated in 5452e23, adad1b7, a1541b6, 39ca165, 0d67577, e76b9f3 | `packages/domain/src/<module>/` per agent | Recorded per module in `docs/Test_Evidence.md`; each review test failed before its fix and passes after | 9 reviews, 70+ findings (answer-guard 16, grading 7, learning 5, donations 4, rewards 6, scheduling 3, …); all fixed; BUG-025..030, 033, 035..040 | 27 agents, 5,306,472 subagent tokens |
| Feature verticals (public site, rewards, homework, family/guardians/consent, privacy, P17 UI) | Workflow subagents: implement → adversarial review (failing regression tests) → fix | Workflow run `wf_984c4069-620` | c1590e3 (registration points pre-created) → integrated in 51df77e (public site), a1541b6 (rewards), 8fd957c (family), c7ed013 (homework), cc3d508 (privacy), d177f05 (P17 UI) | Enumerated per agent in the workflow script (`owns:`); shared files (app.ts, routes.tsx, contracts index) reserved to the lead | Per vertical: API + web + mobile suites green at integration (e.g. homework 51/51 API, family 44 API/32 web/71 mobile) | 6 reviews; 2 findings needed lead fixes (focus ring RV-public-site-5, approvals link RV-rewards-6); all fixed; BUG-031..033a, 041..045 | 18 agents, 4,966,103 subagent tokens |
| P16 monetization (DB 0640, domain, API, web, mobile) | Workflow subagents: backend → UI → adversarial review → fix | Workflow run `wf_96d96581-4a8` | a7b1de1 (registration points) → integrated in 582ef2b (build) and 52ef288 (RV-MON-01..09 fixes) | `supabase/migrations/0640_monetization.sql`, `packages/domain/src/monetization/**`, `routes/monetization.ts`, `routes/admin-monetization.ts`, contracts, pages | API 69/69, DB 36/36, domain 172/172, web 41/41, mobile 55/55 at 52ef288 | RV-MON-01..09 fixed (BUG-049); 0640 edited in place (never applied) | 4 agents, 1,484,685 subagent tokens |
| Learning vertical (question bank, daily/Thursday jobs, practice API, exports, planner UI) | Workflow subagents: backend → UI → adversarial review → fix | Workflow run `wf_b5f6dbc1-69a` | 2db9bd5 (bank registration point) → integrated in 39ca165 (backend), 92b4f45 (UI), ccb600a (12 review fixes) | `packages/domain/src/bank/**`, `routes/learning.ts`, `jobs/learning-jobs.ts`, `jobs/export-build.ts`, `services/pdf.ts`, migration 0650, pages/screens | domain 135/135, API 54/54, DB 15/15 at ccb600a | 12 findings fixed (BUG-051/052); navigation orphans found by the lead's reachability test | 4 agents, 1,779,290 subagent tokens |
| Parent billing (status/sync API, mobile plans/purchase/restore/native offer step, web subscription page) | Workflow subagents: build → adversarial review → fix | Workflow run `wf_7f250e61-991` | f11f186 (registration points) → integrated in 2f96f6c (build), 5dfbc70 (RV-billing-1..7), bd53b73 + 108ee8c (lead wiring) | `routes/billing.ts`, `contracts/billing.ts`, `apps/mobile/src/billing/**`, `app/(parent)/plan.tsx`, `SubscriptionPage.tsx` | API 66/66, mobile 103/103, web 11/11 at 5dfbc70; webhook path 77/77 at bd53b73 | RV-billing-1..7 fixed (BUG-046..048); webhook/TRANSFER and native offer wiring done by the lead | 3 agents, 1,073,197 subagent tokens |
| Independent review of lead-owned code (jobs/AI, billing/P17 API, identity/access) | Workflow subagents: three fresh-context adversarial reviewers writing failing tests (for `code-reviewer`/`security-reviewer`); the lead fixes | Workflow run `wf_c99461cc-949` | fa18393 → 48dcbe4 (billing/P17 fixes), 91fc8c2 + fc2eb8b (jobs/AI and identity fixes, via `wf_3901f5dc-57b`) | Review test files only | Each review test failed on the reviewed tree and passes after the fixes; review files kept byte-identical (diffed against snapshots) | 41 findings: billing/P17 10, jobs/AI 20 (7 high), identity/access 11; all closed (BUG-014..023, BUG-053..060) | 3 agents, 945,676 subagent tokens |
| Fix the independent review of lead code (jobs/AI 20 findings, identity/access 11) | Workflow subagents: fix per slice (disjoint file ownership) → fresh-context adversarial checker with probes → re-fix of confirmed residuals; then a second single-agent adversarial check of the identity re-fix (its pairing-limiter part was halted partway, so the lead verified that part by code review plus an added address-spelling test) | Workflow run `wf_3901f5dc-57b` + one follow-up Agent run | 71122c8-era tree → 91fc8c2 (jobs), fc2eb8b (identity), 22dba32 + 5fdea75 (lead wiring) | jobs: dispatcher, scan-process, image-metadata, spend-ceiling, 0710; identity: auth, middleware, adult/child-auth/export-download routes, config, gate.ts, mobile lib, 0720 | First check: 15/20 jobs fixes verified, 7 residuals proven with probes (2 medium: late-upload photo kept indefinitely; multi-letter keys); identity 10/11 verified, 3 problems (in-flight budget, pairing DoS, logout gap in homework routes). All fixed; full API 553/553, DB 212/212 at integration | 6 agents + 1, 1,837,012 + 92,697 subagent tokens |
| Acceptance coverage (all spec AC rows) | Workflow subagents: assessor per criteria group → skeptic that re-runs cited tests and downgrades overclaims | Workflow run `wf_e4f3bf30-05f` | f6a70bc → recorded in docs/coverage_status.json | none (read-only) | see docs/Test_Evidence.md | 16 agents, 3,404,669 subagent tokens |
| Lead-owned integration: scheduler/deletion purge, scan pipeline, jsonb fix, billing reconciliation | Lead (main loop) with real-Postgres tests; no ECC agent | Commits 1c9851d, f820986, 69e0a31 | — | `apps/api/src/jobs/*`, migrations 0620/0630, services | `vitest run tests/scheduled.test.ts` 11/11, `tests/scan-process.test.ts` 11/11, DB suite 130/130 (exit 0) | BUG-008/009/010 found and closed | — |
| Fix the concrete defects the coverage pass found (capture-ui, archived-history, p17-money, billing-gates, spend-strict, guard-expressions) | Workflow subagents: fix per slice (disjoint ownership, failing test first) → fresh-context adversarial checker with probes and mutation checks → re-fix when the check was not ok (billing-gates, guard-expressions twice) | Workflow run `wf_aea7bbc1-d46` | c71786a → integrated in c0aab46, 622d74b, b0496a8, ae9f088, e52b6a9, 375ec93, 84fc4d7, b6e9998 (lead wiring: guard on by default, call-site test) | Per slice in the workflow script (`owns:`); lead-only: app.ts, routes.tsx, contracts index, domain exports, scan-process/learning-jobs call sites | API 612→620/620, domain 2296/2296, DB and web/mobile slices green at each integration; lead mutation checks recorded per BUG | BUG-064..073, BUG-076 closed; residuals in the ledger `Next` column | 14 agents, 2,708,145 subagent tokens |
| Close unblocked coverage gaps: capture limits (AC_CAPTURE_02), release gates (AC_DEPLOY_07, AC_SECURITY_04), reward rules (AC_REWARDS_01) | Workflow subagents: fix → adversarial check → re-fix (capture-limits and release-gates; reward-rules check ok) | Workflow run `wf_882d1210-e26` | 8872b98 → integrated in 58c3d28, a5f91ee, 35c1c16 (lead applied two fixture patches, the mobile scan wiring and the serve-path filter with a new test) | Per slice (`owns:`); migrations 0750 and 0770 allocated by the lead | Reward rules API 69, DB 30, web 30, mobile 29; capture API 106, mobile 43, web 29; release gates API 176, DB 36; local release artifact scan passed with negative control | BUG-077..081 closed; follow-up BUG-082 (staging storage/email mocks) fixed by the lead | 8 agents, 1,998,628 subagent tokens |
| AC_SECURITY_02 child-safety screen (moderation before/after generation, templates, escalated reports, family hold) | Workflow subagents: fix → fresh-context adversarial check (not ok: 3 high, 5 medium) → re-fix | Workflow run `wf_47dc67b7-ae8` | 88ac44c-era tree → integrated in 4b50914 (BUG-084..087, 0760) | `packages/domain/src/safety/**`, 0760, scan-process safety steps, privacy report routes, web/mobile notices | Domain safety 460, DB 12, API 21 at integration | BUG-084..087 closed; approvals pending (Owner action #24) | 3 agents, 1,196,597 subagent tokens |
| Final lead review: child-safety pass 2, lead jobs/AI changes, lead runtime/data changes (+ unchecked capture-limits and release-gates second passes) | Workflow subagents: three fresh-context reviewers (for `code-reviewer`/`security-reviewer`) → one fixer per area → adversarial checker with mutation checks; interrupted once by a container restart and resumed from cache | Workflow run `wf_2e80c3c2-fd2` | aef28f5 → integrated with rounds 2–4 in 1231198, 2bb51f8, 441eb6a, a6e15c1, dd30937 | Per area (`owns:` in the script); lead-only registration points untouched | Checks recorded per finding in the ledger | RV-child-safety-1..15, LJA-F1..F11, LRD-1..6; checks CHK-CS-1..9 and the jobs/runtime residuals → round 2 | 9 agents, 1,631,898 subagent tokens |
| Final review round 2 (residuals and regressions from the round-1 checks) | Workflow subagents: re-fix per area → adversarial check | Workflow run `wf_7f4e9851-8cb` | uncommitted round-1 tree → same commits as above | Per area | Suites per area green; checks: runtime/data ok, jobs/AI residual (rubric labels) and child-safety CHK2-CS-1..7 → round 3 | BUG-090..105 (BUG-090 found by the round-2 checkers' time bomb) | 6 agents, 1,570,076 subagent tokens |
| Final review round 3 (child-safety regressions and misses plus a human false-match clearing path; rubric-label subordinate-clause bypass) | Workflow subagents: fix → adversarial check | Workflow run `wf_f4c2e9e6-b25` | uncommitted round-2 tree → same commits as above | Child safety (domain, 0760, privacy routes, runbook 5.1) and rubric labels | Checks not ok: CHK3-CS-1..8, R3-RL-1..5 → round 4 | BUG-110 (clearing path) | 4 agents, 1,431,361 subagent tokens |
| Final review round 4 (recall-first corrections under a written lead policy; fail-closed rubric labels) | Workflow subagents: fix → acceptance-criteria check; the first attempt (2 fixers) was lost to a container restart just before it reported, and the resumed fixers reconstructed it from its transcripts | Workflow run `wf_6cfbbb0d-fb7` (resumed) | uncommitted round-3 tree → 441eb6a, dd30937 | Same as round 3 | Checks not ok: CHK4-CS-1..9 (7 high: round-4 guards cancel round-3 catches, held codes on printed prompts, 55/126 canonical misses), R4-RL-A..G (1 high regression) → round 5 | BUG-109, BUG-091 remain open; lesson L-022 | 4 agents, 1,221,176 subagent tokens (resumed run; the lost attempt's usage was not reported) |
| Final review round 5 (recall-only child-safety corrections with an automated no-regression sweep; fail-closed rubric labels; provider moderation) | Workflow subagents: three fixers with disjoint files → acceptance-criteria checkers classifying problems as regression / listed / residual → one bounded re-fix and recheck for the area not ok (rubric labels); the recheck's one low regression was fixed by the lead with a failing test | Workflow run `wf_d45926ba-696` | e123e5c → 8a84780, a46132c, 5f02621 | child safety: `packages/domain/src/safety/**`; rubric: `rubric-feedback.ts` + tests; moderation: `packages/ai/src/moderation.ts`, scan-process/learning-jobs wiring, config, runtime | Local gate on the combined tree: 5,583 tests, exit 0 | Checks: child safety ok, provider moderation ok, rubric labels ok after re-fix except R5F-RL-1 (fixed by the lead); BUG-091, BUG-109 closed with residuals, BUG-111, BUG-112 | 8 agents, 2,595,716 subagent tokens |
| Brand, platforms and the Amazon Appstore channel (traced logo assets on every screen, icons and splash, iPad/Fire column; channel `amazon_appstore`; BUG-113 webhook store names) | Workflow subagents: four fixers with disjoint files (brand assets, web brand, mobile brand + tablet column, Amazon stores) → checkers classifying problems as regression / listed / residual → one bounded re-fix and recheck (mobile brand); the lead widened the channel type across API/web/mobile, added the Amazon "no offer codes" branch with a regression test, and split the verified tree into two commits checked in an isolated worktree before committing | Workflow run `wf_b0522f4a-5e0` | df7f3a5 → 60955b5, efb1142 | brand: `brand/**`, `scripts/brand/export-assets.mjs`, `apps/web/public/**`, `apps/web/src/{App.tsx,components/Logo*,styles.css,brand.test.ts}`, `apps/mobile/src/brand/**`, `apps/mobile/assets/brand/**`, `apps/mobile/src/family/ui.tsx`, layouts and screens; Amazon: `packages/contracts/src/common.ts`, `packages/domain/src/shared/billing.ts`, migration 0800 + `amazon_channel.test.ts`, `apps/api/src/{providers/billing.ts,services/billing-sync.ts,routes/webhooks.ts,routes/billing.ts,services/p17-jobs.ts}`, `apps/mobile/src/billing/**`, `eas.json` | Isolated worktree gate on the brand+Amazon tree (`scripts/verify.sh` + test-count audit): domain 3,611, ui-tokens 4, db 308, contracts 15, mobile 452, web 425, ai 49, api 856 (5,720 tests, 0 failed), release-artifact scan with negative control, exit 0 | Checks: brand-assets ok, web-brand ok, mobile-brand-ipad ok after re-fix, amazon-stores ok; BUG-113 | 10 agents, 1,715,265 subagent tokens |
| Owner decision: the parent is the sole safety recipient (no family hold; flag email; parent actions addressed / false match) | Workflow subagents: two fixers with disjoint files (policy-no-hold: domain templates/screen, API and DB safety tests; parent-recipient: migration 0790, contracts, PATCH route, flag-email job, web and mobile privacy screens) → acceptance checkers classifying problems as regression / listed / residual → one bounded re-fix and recheck per area; the run was interrupted once by the user and resumed with `resumeFromRunId` and prior transcripts; the lead applied the lead-owned lines (the moderation-outage hold in scan-process.ts, the ai moderation test, readiness wording, records) and gated the slice in an isolated worktree before committing. Two fixers were flagged by the permission classifier as 'Security Test Removal': the lead diffed every flagged test against HEAD — hold assertions were inverted to the decided policy with the decision named, none deleted; answer-text and category leakage assertions kept; the five 0760 DB tests retitled with assertions byte-identical; one test count up (API 37 → 37 with 7 more assertions, DB 21 → 22) | Workflow run `wf_6b64e4ae-006` | db114c7 → c49a7e7 | `packages/domain/src/safety/**`, `packages/ai/src/moderation*.ts`, `supabase/migrations/0790_parent_flag_review.sql` + tests, `packages/contracts/src/privacy.ts`, `apps/api/src/{routes/privacy.ts,jobs/dispatcher.ts,jobs/scan-process.ts,providers/index.ts,config.ts}` + tests, `apps/web/src/pages/app/PrivacyControlsPage*`, `apps/mobile/app/(parent)/privacy.tsx`, `apps/mobile/src/privacy/**`, records | Isolated worktree gate (`scripts/verify.sh` + test-count audit): domain 3,613, ui-tokens 4, db 317, contracts 15, mobile 459, web 431, ai 49, api 876 (5,764 tests, 0 failed), exit 0 | Checks: policy-no-hold ok after the lead's line landed (recheck listed only lead-owned items, all applied); parent-recipient ok with three low residuals (the admin FALSE_MATCH_NOT_RELEASABLE branch keeps only its DB test; the mobile unlock line now reads the contracts constant; migration 0790 sorts before the committed 0800 — runbook note added) | 8 agents, 1,642,011 subagent tokens, 402 tool uses |
| Owner super-admin dashboard: company overview, subscriptions and revenue, support cases (complaints, refund requests) with parent intake on web and mobile | Workflow subagents: three fixers with disjoint files (data/API: migrations 0810, contracts, domain `ops`, routes, real-Postgres tests; web: owner pages and parent support page with screenshots against stubbed responses; mobile: parent Support screen and pure view-model) → three acceptance checkers classifying problems as regression / listed / residual, each with mutation checks; no re-fix round was needed. The lead pre-created the registration points (routes, contracts index, domain subpath, web routes, Support link), added migration 0820 (family purge deletes support cases; test red without it), the navigation assertion, an honest attention row for failed provider events, and the records; the slice was gated in an isolated worktree before committing | Workflow run `wf_f4a66218-84d` | 8c2dc42 → 99166da | `supabase/migrations/0810_support_cases.sql`, `0820_support_cases_purge.sql` + tests, `packages/contracts/src/{support,admin-ops}.ts`, `packages/domain/src/ops/**`, `apps/api/src/{routes/support.ts,routes/admin-ops.ts,services/ops-metrics.ts}` + tests, `apps/web/src/pages/admin/{AdminHomePage,SupportAdminPage,RevenueAdminPage}*`, `components/{admin-ui,RevenueChart,overview-sections}.tsx`, `apps/web/src/pages/app/SupportPage*`, `FamilyDashboardPage*`, `apps/mobile/app/(parent)/support.tsx`, `apps/mobile/src/support/**`, records | Isolated worktree gate (`scripts/verify.sh` + test-count audit): domain 3,650, ui-tokens 4, db 335, contracts 15, mobile 507, web 461, ai 49, api 902 (5,923 tests, 0 failed), release-artifact scan with negative control, exit 0; the first gate run of this slice failed on four purge tests and exposed BUG-114 (the lead's 0820 had been generated from stale function text), fixed before the commit | Checks: ops-data-api ok (5 low residuals: the local gate is shadowed by the admin-promotions router's gate at the same prefix; a dead FK set-null action beside a check; churn base is an approximation, stated in its definition; fixed row caps without a "more" flag; the case rate limit is consumed before period validation), ops-web ok (4 low residuals: failed-events row fixed by the lead; /admin/revenue loads the overview for three lines; chart labels small at 390 px; "N replies" counts the parent's own), ops-mobile ok (latest reply shown on expand; no render-level tests by mobile convention) | 6 agents, 1,559,271 subagent tokens, 390 tool uses |
| Store-readiness code gaps (account closure, parental gate and legal links, subscription disclosures, native config, web hardening) | Workflow subagents: four fixers with disjoint files (account-close: route, Auth Admin provider, migration 0830, job, web and mobile screens, real exports; mobile-store: ParentalGate, legal links, purchase disclosures, fixed device names; native-config: permissions, privacy manifest, export compliance, push stack removed, EAS linkage and loud variables, Amazon receiver; web-store: legal pages final wording, Amazon named, IP/notices copy, loud env, security headers) → acceptance checkers with mutation checks → one bounded re-fix each for account-close (a test that did not protect the closed-user rule) and web-store (two lead-owned pages) → recheck; the lead then applied the lead actions (auth-admin test wiring, duplicate export handler, stale export sentence, unlock-screen gate, runbook step, a build-time env gate for the portal) and fixed the clock-drift test the checkers exposed (BUG-115) before gating the combined tree in an isolated worktree | Workflow run `wf_b0de02d8-ca2` | 8ac47ea → 6d0d5d0 | see `docs/Release_Readiness.md` store checklist and the commit | Isolated worktree gate (`scripts/verify.sh` + test-count audit): domain 3,656, ui-tokens 4, db 341, contracts 15, mobile 554, web 526, ai 49, api 931 (6,076 tests, 0 failed), release-artifact scan with negative control, exit 0; the first gate run failed only on formatting of one lead-edited page | Checks: mobile-store ok (residuals: unlock link — fixed by the lead; no render-level gate test), native-config ok (residuals: https rule test gap — fixed by the lead; template permissions candidate; dev-client usage string), account-close ok after re-fix (residuals: adapter untested live; scheduled clock drift — fixed), web-store ok after the lead's page changes (residuals: Referrer-Policy kept at no-referrer; build-time gate — added by the lead) | 12 agents, 1,813,047 subagent tokens, 421 tool uses |
| Hardening round 1 fixes (API input validation, keyset paging, per-family limits, K-8 scope; mobile sign-out, sign-in errors, timeouts, upload memory, honest reminder copy, pairing recovery, child navigation, signed-out path, in-memory PIN gate, store naming) | Workflow subagents: two fixers with disjoint files (api-validation: API-AUTH-R1-01..05 with a failing regression test per finding first; mobile-runtime: MOB-R1-01..10 likewise) → one acceptance checker per area with mutation checks (predicate off, page limit without +1, ignored cursor, band restored, timeout removed, …) and byte-for-byte restore; the lead then applied the lead actions (staff free-text fields through `freeTextSchema`, a per-user budget on PUT /v1/adult/pin, the parent privacy screen behind the shared unlock gate, timeout wording in every child copy layer, a 120 s timer on the storage PUT, the web schedule section's honest reminder copy, the retention line naming all three stores, Threat_Model T38 note and a runbook data check) and gated the combined tree in an isolated worktree | Workflow run `wf_8b66cd30-e71` (its inputs: the round-1 confirmed findings of `wf_2a9a8aa6-7c4`) | c38311d → 54ca934 | see `docs/Bug_Ledger.md` BUG-116..130 and the commit | Isolated worktree gate (`scripts/verify.sh` + test-count audit) on the exact tree of `54ca934`: domain 3,656, ui-tokens 4, db 341, contracts 20, mobile 591, web 530, ai 49, api 952 (6,143 tests, 0 failed), release-artifact scan with negative control, exit 0 at the first run; before it, typecheck of the four touched packages, eslint and prettier on all 71 files, secret scan | Checks: api-validation ok (residuals: response schema narrower than the database check — release data check added; POST /v1/children inserts as service with membership and unlock checked in code, same as activation), mobile-runtime ok (residuals: privacy screen outside the gate — fixed by the lead; unlock lapses at the server TTL in the foreground and re-entry lands on home — recorded in BUG-129; web client's 20 s default with no long call found; child copy wording — fixed by the lead) | 4 agents, 796,975 subagent tokens, 195 tool uses |
| Store-readiness audit and adversarial bug hunt (Apple, Google Play, Amazon, native build config; API auth/validation, billing, safety-privacy, database, web, jobs-AI, mobile) | Workflow subagents, read-only: four store auditors against the fetched store guidelines (items marked doc-verified or candidate where a page was unreachable) → seven area finders → three independent verifiers per finding with distinct lenses (a finding survives with at least two confirmations) → a second finder round capped at two rounds; the lead routed the audits' code gaps to `wf_b0de02d8-ca2` and the confirmed findings to three fix workflows | Workflow run `wf_2a9a8aa6-7c4` (resumed once after a script change; the second finder round did not run: all seven round-2 finders failed on the account's session limit) | 811f210 → read-only | `docs/Release_Readiness.md` store checklist; confirmed findings in `docs/Bug_Ledger.md` BUG-116..142 and the round-2b rows | 48 round-1 findings verified: 44 confirmed (finder severity high 6, medium 21, low 17), 4 refuted (API-AUTH-R1-06, CS-R1-04, CS-R1-05, WEB-R1-05) | Honest limit: a single finder round per area; a second round is still owed on a stable tree | 162 agents (155 done, 7 failed on the session limit), 7,939,904 subagent tokens, 1,290 tool uses |
| Hardening round 2a fixes (billing settlement and ledger ordering, consent withdrawal, database indexes, retention and membership release) | Workflow subagents: three fixers with disjoint files (billing BILL-R1-1..5; safety-privacy CS-R1-01..03; db DB-R1-01..04 with migration 0840), failing regression test first → acceptance checker per area with mutation checks and byte-for-byte restore → one bounded re-fix and re-check each; checkers found a regression of the first billing fix (a won dispute kept the money refunded) and the stuck `fetched_at` case, both re-fixed; the lead applied the lead actions (consent test reorder, deletion-view, account-closure and account-close job predicates, the non-USD donation rule, the 90-day job retention tick step) and then fixed the two regressions the database re-check found in those lead changes (scan version keys from a count; a live second family not blocking closure), each with a test proven to fail without its fix | Workflow run `wf_81f5d47c-d55` | ca40ce7 → 77c13b1 | see `docs/Bug_Ledger.md` BUG-131..142 and the commit | Isolated worktree gate (`scripts/verify.sh` + test-count audit) on the exact tree of `77c13b1`: domain 3,656, ui-tokens 4, db 355, contracts 20, mobile 592, web 530, ai 49, api 982 (6,188 tests, 0 failed), release-artifact scan with negative control, exit 0 at the first run | Checks: billing ok after re-fix (residuals: webhook won-dispute amount untested — test added by the lead; null-amount edge unreachable from Stripe), safety-privacy ok after re-check (residuals: a family with no consent record is not blocked, unreachable in production; out-of-band consent changes rely on refresh refusal), db not ok on re-check (two lead regressions — fixed by the lead with failing-first tests; residuals: PostgREST timezone grant kept, indexes built without CONCURRENTLY) | 12 agents, 1,758,552 subagent tokens, 425 tool uses |
| Hardening round 2b fixes (web portal invitations, auth links, error boundary, contrast, Amazon channel, launch switch; child-bound subject links and session housekeeping; durable purge, bounded scans, metered timeouts, pipeline codes) | Workflow subagents: three fixers with disjoint files (web WEB-R1-01..04/06..11; db2 DB-R1-05..07 with migration 0850; jobs JOBS-R1-01..04), failing regression test first → acceptance checker per area with mutation checks and byte-for-byte restore (all three ok at the first check); the lead then wired `app.prune_session_rows` into identity housekeeping (30 days, with a test), switched the safety-flag email handler to coded failures, counted retry successors in account closure, fixed an API-only typecheck break in the fixer's request encoder, and wrote the runbook, readiness, owner-action and cost notes; the web and mobile follow-ups the fixers reported went to round 2c | Workflow run `wf_90c8faf6-975` | 77c13b1 → 6728bea | see `docs/Bug_Ledger.md` BUG-143..159 and the commit | Isolated worktree gate (`scripts/verify.sh` + test-count audit) on the exact tree of `6728bea`: domain 3,656, ui-tokens 4, db 366, contracts 20, mobile 596, web 574, ai 59, api 997 (6,272 tests, 0 failed), release-artifact scan with negative control, exit 0 at the first run | Checks: web ok (residuals: nav survival after a page error not pinned by a test; Supabase redirect allow-list — Owner action #40; web-billing flag is a constant; mobile contrast and mobile-started reset — round 2c), db2 ok (residuals: session prune wiring — done by the lead; 0850 refuses to apply over a cross-child row — runbook note; prune test counts rows file-wide), jobs ok (residuals: safety-email codes — done by the lead; configuration dead letters re-queue every six hours — alert on the codes; retry successors in account closure — done by the lead; web client downscale and mobile child copy — round 2c; platform limits unconfirmed) | 6 agents, 1,053,874 subagent tokens, 306 tool uses |

## Concurrency deviation (AC_ECC_04)

The ECC guide's resource default is at most two concurrent helpers. From commit a7b1de1 onward up to four
workflows ran at once (each capped at two concurrent agents by the tool, so up to eight helpers). Reason: the
owner enabled multi-agent orchestration for this session and asked for all phases to proceed without
waiting. Safeguards actually used: every agent's writable files are enumerated in its prompt and disjoint
from every other agent's; shared registration points (app.ts route mounts, web route table, contracts
index, domain package exports) were pre-created and committed by the lead before launch; migrations use
reserved numbers (0640 monetization, 0650 learning) and the lead applies/integrates them; agents never run
git or repo-wide formatters; the lead commits only after re-running checks on the combined tree.
Known cost of the deviation: the build machine has 4 CPUs, so timing-sensitive tests run slower while agents
execute; the final verification is therefore run sequentially after all workflows finish, and any
timing failure is investigated rather than retried until green.
