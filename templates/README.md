# Project records to initialize

These are blank templates, not completed evidence. Claude should merge them into existing equivalent project records. Do not overwrite an existing CLAUDE.md or a defect/history file. Replace placeholder command names only after inspecting the repository. The master prompt's section E5 is authoritative.

Use the Bug_Ledger, Lessons_Learned, ECC_Capabilities and ECC_Runs templates under `docs/`. Use `CLAUDE.fragment.md` as a small project-instruction addition. Keep secrets, production child data and raw sensitive transcripts out of every file.
