# Historical reference only

These files preserve the previous package, original product documents and calculators. They contain older costs, duplicate checklist IDs and earlier launch instructions. Do not use them as a competing build prompt or financial source of truth.

Use `../PencilLift_Claude_Code_Master_Prompt.md` (Revision 4) and `../finance/` for the current instructions and transparent illustrative cost model. The original logo remains in the active `brand/` folder. Historical files are retained for traceability; do not re-import their old prices, workload assumptions or policy claims into current configuration.
